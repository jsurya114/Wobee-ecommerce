# Decisions Pending Client/Business Confirmation

Items here are built with an explicit, clearly-labeled placeholder so development isn't blocked. Every placeholder must be swapped for a confirmed value before production launch — grep the codebase for `DECISIONS_PENDING` to find every call site.

| # | Decision | Current placeholder | Where it lives | Needs confirmation from |
|---|---|---|---|---|
| 1 | GST rate applied at checkout | Flat 5% (placeholder — apparel GST commonly 5% or 12% by price slab in India) | `apps/api/src/modules/orders` tax calculation (Day 4) | Client / accounting |
| 2 | Standard shipping fee for carts between 1,000g–1,499g (below the 1,500g free-delivery threshold, ADR-021) | Flat ₹50 (`ShippingRule.standardFeePaise` seed value) — flat vs. weight-tiered not yet decided | `packages/database` seed (`ShippingRule`), `apps/api/src/modules/shipping` | Client |
| 3 | Default ₹/kg pricing rate | ₹1,200/kg demo value (`PricingSetting.defaultRatePerKgPaise` seed) — for seeded demo products only | `packages/database` seed (`PricingSetting`) | Client (real catalogue pricing) |
| 4 | Razorpay test-mode credentials | Stub values in `.env.example` (`RAZORPAY_KEY_ID` etc.) | `.env` (local, gitignored) | User will provide before Week 1 Day 5 |
| 5 | Return window (days after delivery a customer may request a return) | 7 days (constant, `RETURN_WINDOW_DAYS` in `apps/api/src/modules/returns/domain`) — common apparel-return-window default, not a stated business rule | `apps/api/src/modules/returns` (Week 2 Day 6) | Client |
| 6 | Low-stock threshold (admin inventory dashboard indicator) | 10 units (constant, `LOW_STOCK_THRESHOLD` in `packages/types/src/enums.ts` — moved 2026-09-10 from a per-app duplicate to one shared value, and corrected from 5 to 10 at the same time) — still an arbitrary round number, not a stated business rule | `packages/types` (single source; `apps/api`'s inventory domain re-exports it, `apps/admin`'s inventory table imports it directly — neither keeps its own copy anymore) | Client |
| 7 | Email/SMS/WhatsApp provider for customer notifications **and the registration + forgot-password email OTPs** | Notifications: `StubEmailProvider` proves out the queue/worker/retry pipeline without contacting a real provider. OTP email (both `register/start` and `forgot-password`): **`SmtpOtpNotifier` / `SmtpPasswordResetNotifier` (nodemailer) are fully wired** — `auth.module.ts` uses them automatically once `SMTP_HOST` is set (any SMTP endpoint: Gmail app password / SES SMTP / Mailtrap / …; see `.env.example`; both share one `createSmtpTransport()`). With no SMTP configured, the `Dev*` notifiers log the code in `development` and the API returns it as `devCode` outside `production`. **In production with no SMTP configured, `/auth/register/start` and `/auth/forgot-password` mint codes nobody receives — those flows are effectively blocked until the SMTP_* vars are set.** Still open: whether to use a dedicated transactional-email service instead of raw SMTP. | `apps/api/src/modules/notifications/infrastructure/providers/stub-email.provider.ts`; `apps/api/src/modules/auth/infrastructure/services/{smtp,dev}-otp-notifier.ts`, `{smtp,dev}-password-reset-notifier.ts`, `smtp-transport.ts`; `.env.example` SMTP_* block | Client (SMTP credentials, or a chosen provider) |
| 8 | Item-level order cancellation (cancel a single line out of a multi-item order) | Not built. Cancellation today is order-level only and admin-only by deliberate design (`POST /admin/orders/:id/cancel`, staff-initiated) — there is no customer-facing cancel of any kind, and `Order.status` has no per-item column to partially cancel against. Building this would need a new per-item cancellable state, partial-refund logic distinct from returns' existing calc, partial-restock logic, and a definition of what "the order's status" means once some items are cancelled and others aren't — a genuine product decision, not a bug fix (2026-09-10 admin+user bug-fix sprint, Workstream 3). Individual **returns** (not cancellation) already work per-item today via the existing `Return`/`ReturnItem` model and needed no change. | `apps/api/src/modules/orders` (cancellation), `apps/api/src/modules/returns` (the already-working item-level comparison) | Client (whether partial/item-level cancellation is wanted at all, and if so what the order-level status should show while it's partially cancelled) |

**Rule:** nothing on this list may be silently "finalized" by assumption. When a real value arrives, update the placeholder, remove the row (or mark it resolved with a date + link), and note it in `journal.md`.
