/** DOM id shared between both hamburger triggers (mobile + desktop rows) and the single drawer instance they open, so `aria-controls` resolves correctly regardless of which trigger was pressed. */
export const NAV_DRAWER_ID = "woobe-nav-drawer";
