import { Router } from "express";

/**
 * coupons module — placeholder composition root.
 * Owns (ADR-010): Coupon, CouponRedemption.
 * Explicitly deferred to Week 2+ — Day 4's plan says "skip entirely this
 * week (deferred, not stubbed)". Schema exists now; no routes this week.
 */
export const router = Router();
