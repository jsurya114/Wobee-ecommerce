import { Router } from "express";

/**
 * products module — placeholder composition root.
 * Owns (ADR-010): Product, ProductImage.
 * Built out: Week 1 Day 3 (catalogue).
 * Full domain/application/infrastructure/interface layering lands with the
 * real work — see project_planning/architecture.md §3.2 for the shape
 * (the auth module has the reference scaffold).
 */
export const router = Router();
