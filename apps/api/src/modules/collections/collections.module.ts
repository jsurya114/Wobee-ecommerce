import { Router } from "express";

/**
 * collections module — placeholder composition root.
 * Owns (ADR-010): Collection, ProductCollection.
 * Built out: Week 2+ (homepage New Drops / Most Loved rails).
 */
export const router = Router();
