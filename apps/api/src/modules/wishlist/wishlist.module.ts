import { Router } from "express";

/**
 * wishlist module — placeholder composition root.
 * Owns (ADR-010): Wishlist, WishlistItem.
 * Explicitly deferred to Week 2+ (project_planning/week1_excecution_prompt.md
 * "Explicitly Deferred to Week 2+"). Schema exists now; no routes this week.
 */
export const router = Router();
