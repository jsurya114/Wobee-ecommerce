import { Router } from "express";

/**
 * categories module — placeholder composition root.
 * Owns (ADR-010): Category.
 * Built out: Week 1 Day 3 (catalogue, category filter only this week).
 */
export const router = Router();
