apiVersion: 1
providers:
  - name: woobe
    orgId: 1
    folder: Woobe
    type: file
    # Dashboards are owned by Git, not by whoever last clicked Save.
    disableDeletion: true
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
