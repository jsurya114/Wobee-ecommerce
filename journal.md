# Woobe Development Journal

Append-only log, most recent entry at the bottom. One entry per completed change — this exists because multiple developers work on this repo; read the latest entries before you start work to know what state things are in.

Entry format:
```
## YYYY-MM-DD — <short title>
**Branch/commit:** ...
**What changed:** ...
**Why:** ...
**Follow-ups / known gaps:** ...
```

---

## 2026-08-25 — Week 1 Day 1: Foundation

**Branch/commit:** `dev1` (not yet committed — see note at end of this entry)

**What changed:**
- **Monorepo scaffolding**: pnpm workspace (`apps/{web,admin,api}`, `packages/{database,types,validation,ui,config,utils}`), root scripts, corepack-pinned pnpm 11.23.0.
- **Full Prisma schema** (`packages/database/prisma/schema.prisma`) — every domain from `plan.md` §3 modeled now: auth (User/AuthCredential/Address), catalogue (Category/Collection/Product/ProductVariant/ProductImage), inventory (Warehouse/Inventory, ADR-015), pricing (PricingSetting), cart/wishlist (ADR-011), coupons, orders/order items (state machine per `plan.md` §4), payments + WebhookEvent (idempotency dedup, ADR-014), shipping (ShippingRule, ADR-021), returns/refunds (item-level, separate from `Order.status`), reviews, notifications. Money = `Int` paise, weight = `Int` grams throughout, no exceptions.
  - `Product.minPricePaiseCache` / `ProductVariant.effectivePricePaiseCache` added as **denormalized display/sort caches only** (ADR-012's category+price index) — checkout/cart pricing never reads them, always recomputes live. Clearly commented in the schema so this isn't mistaken for a price source of truth later.
  - Deferred to Day 3: the `pg_trgm` GIN index on `Product.name` (ADR-012) — didn't want to guess at untested Prisma `postgresqlExtensions` syntax on Day 1 with no catalogue-search code yet to validate it against. Noted as a TODO in the schema.
- **Migration applied + seeded** against local Postgres: `20260825102735_init`. Seed creates 1 warehouse, 1 admin (`admin@woobe.in` / `Admin@12345`, dev-only), 1 `PricingSetting` (₹1,200/kg placeholder), 1 `ShippingRule`, 5 categories, 2 collections, **10 demo products / 22 variants / 22 inventory rows**. Verified queryable via `psql` and Prisma Studio.
- **`apps/api` foundation**: env validation (zod, fail-fast), Redis client, `DomainError` hierarchy + `Result<T,E>`, `error-handler`/`request-id`/`validate` middleware, Express app + graceful shutdown. **All 17 modules** from `architecture.md` §3.3 registered as composition roots (`src/modules/*/*.module.ts`) mounted at `/api/v1/<name>`; only `auth` has its full domain/application/infrastructure/interface layering built out (per architecture.md's reference shape) — routes are real and validate against the real Zod schemas, but handlers return `501` with a "lands Day 2" message. No auth business logic (password verify, token issuance) written yet — that's Day 2, not started early.
- **Module boundary enforcement (ADR-010)**: `apps/api/.dependency-cruiser.cjs`, wired into `pnpm run boundaries:check` and CI. Verified it actually fires — not just present — by deliberately adding a violating import, confirming it failed, then confirming a clean tree passes.
- **CI** (`.github/workflows/ci.yml`, ADR-013): Postgres + Redis service containers, install → destructive-migration check → generate → migrate deploy → `migrate diff` drift check → lint → typecheck → boundary check → test → build. Not yet validated by an actual GitHub Actions run (needs a push) — reasoned through carefully but flag this as first-push-verify.
- **Destructive-migration guard** (`scripts/check-destructive-migration.mjs`, ADR-013): fails CI on any newly-added `migration.sql` containing DROP TABLE/COLUMN/TRUNCATE/column-TYPE-change unless it carries a human-added `-- accept-data-loss: <reason>` comment.
- **`apps/web` / `apps/admin`**: minimal Next.js 15 (App Router, React 19) skeletons, Tailwind wired to the design tokens from `woobe_ui_design_plan.md` §3-5 (`packages/ui/src/tokens`, mirrored in `packages/config/tailwind/preset.cjs`), Playfair Display + Inter via `next/font/google`. `apps/web` has a real (if placeholder) mobile-first homepage under the `(storefront)` route group; `apps/admin` has a placeholder landing page. No real pages/features yet — that starts Day 2.
- **`packages/utils`**: `calculateWeightBasedPricePaise` (the core pricing formula) + money/weight display helpers, unit tested (12 tests).
- **`packages/validation`**: `registerSchema` / `loginSchema` (Zod, shared client+server per ADR-020), unit tested (5 tests) — ready for Day 2's actual register/login pages to consume.
- **Governance docs added**: `DEVELOPMENT_RULES.md` (9 non-negotiable rules, referenced from ADR-016/§5), `DECISIONS_PENDING.md` (GST rate, shipping fee tier, default ₹/kg rate, Razorpay keys — all placeholder'd, not silently assumed).
- **Verified, not just written**: `pnpm install`, `db:generate`, `typecheck` (9/9 projects), `lint` (9/9, zero warnings), `test` (21/21 passing), `boundaries:check`, `build` (all apps), `prisma migrate dev`, `db:seed`, Prisma Studio boot, and a live `apps/api` server boot against real Postgres+Redis with a real HTTP round-trip (`/health`, validation-rejected register, validation-passed register hitting the Day-2 501 stub).

**Why:** Per `week1_excecution_prompt.md` Day 1 — foundation everything else depends on. Sequenced so Day 2 (auth) can start immediately without any scaffolding decisions left open.

**Environment notes for other developers on this machine/repo:**
- **Ports remapped from Docker defaults** — this dev machine already runs an unrelated project's Postgres (native, port 5432) and Redis (Docker container `version-vault-redis`, port 6379). Woobe's `docker-compose.yml` maps **Postgres to host `5433`** and **Redis to host `6380`** instead (container-internal ports unchanged). `.env.example` reflects this. If you're on a clean machine with no port conflicts, these mappings still work fine — just non-default.
- `pnpm` runs via **corepack** (`corepack enable`), not a separate global install — pinned via root `package.json`'s `packageManager` field.
- Local `.env` created from `.env.example` with real random dev JWT/cookie secrets (not committed — gitignored).
- `pnpm-workspace.yaml` now has an `allowBuilds` block approving postinstall scripts for `prisma`, `@prisma/client`, `@prisma/engines`, `esbuild`, `sharp` — all legitimate, needed for Prisma's engine binary and Next.js's image optimizer.

**Follow-ups / known gaps:**
- CI workflow written but not yet exercised by a real GitHub Actions run — verify on first push.
- `pg_trgm` GIN search index deferred to Day 3 (see schema TODO).
- `package.json#prisma` config is deprecated as of this Prisma version (v6.19) in favor of `prisma.config.ts` — not migrated, not urgent, Prisma 7 isn't out yet.

**Update:** committed and pushed to `dev1` as `519829e` (see Day 2 entry below).

---

## 2026-08-25 — Week 1 Day 2: Auth (API + UI together)

**Branch/commit:** `dev1` (not yet committed — pending go-ahead, see chat)

**What changed:**
- **Schema**: added `RefreshToken` model (`packages/database/prisma/schema.prisma`, migration `20260825105515_add_refresh_tokens`). ADR-018's "rotating refresh token" implemented as a DB-backed **opaque** token (crypto-random, sha256-hashed at rest), not a second JWT — this is what makes logout and reuse-detection real instead of "wait out the expiry."
- **`apps/api` auth module — fully implemented** (domain/application/infrastructure/interface, replacing Day 1's `501` stubs):
  - `register` / `login` / `refresh` / `logout` / `me` (new — protected, used by the frontend to restore a session).
  - Access token: short-lived JWT (15m default), verified by signature only, sent via `Authorization: Bearer`.
  - Refresh token: opaque, httpOnly + signed cookie, scoped to `/api/v1/auth`, **rotated on every refresh** (old row revoked, new row issued) and **reuse-detected** — presenting an already-revoked token revokes every session for that user (covered by an integration test).
  - Login timing-equalized between "no such user" and "wrong password" (same error, same rough latency via a dummy bcrypt compare) — prevents account-enumeration via response content or timing.
  - Deactivated-account check on both login and refresh.
  - `RBAC` middleware (`auth-guard.ts`, `rbac-guard.ts`) — deliberately duplicates a 2-line JWT verify instead of importing the auth module's `JwtService`, to keep ADR-010's boundary intact (this middleware protects every module's routes, not just auth's).
- **Correctness fix caught before it shipped**: Express 4 doesn't catch rejected promises from async route handlers — an unhandled use-case error would have bypassed `error-handler.ts` entirely. Added `middleware/async-handler.ts`, wrapped every async route.
- **Bug caught by writing the integration tests, not by inspection**: `packages/validation`'s `registerSchema` rejected a *blank* (not omitted) phone field — exactly what an untouched HTML input submits — because `""` doesn't match `.optional()`'s "field absent" case, only Zod's own `undefined`. Fixed with an explicit `"" -> undefined` transform; regression test added.
- **Integration tests** (`apps/api/src/modules/auth/auth.integration.test.ts`, 8 tests) against a **real** `woobe_test` Postgres database (migrated once via `prisma migrate deploy`, see below) — register, duplicate-email conflict, login, wrong-password (enumeration-safe), `/me` protected route, full refresh+logout lifecycle, and refresh-token reuse detection. All pass.
- **`packages/ui` primitives** (Button, Input, Label, `FormField`, `cn()` via clsx+tailwind-merge+cva) — first real primitives, styled with Day 1's design tokens. Deliberately not using Base UI yet (ADR-022) — nothing built so far needs accessible primitive *behavior* (focus trap, listbox), just styled HTML elements.
- **`apps/web` auth feature**: `lib/api-client.ts` (the sole HTTP boundary to `apps/api`, ADR-019), `features/auth/{api,hooks,components}` — `AuthProvider`/`useAuth` (access token in memory only, never localStorage; silent `/auth/refresh` on mount to restore a session across reloads), `LoginForm`/`RegisterForm` (react-hook-form + Zod resolver against the *same* schemas apps/api validates with), `AccountView` (the protected-route proof), `SiteHeader` (minimal nav for manual testing — not the real mobile nav, that's Day 3+).
- **Environment**: Next.js only auto-loads `.env*` from an app's own directory, not the monorepo root — added `apps/web/.env.local` and `apps/admin/.env.local` (+ `.env.example` siblings) for `NEXT_PUBLIC_API_URL`/`NEXT_PUBLIC_ADMIN_API_URL`.
- **Verified in a real browser** (chrome-devtools-mcp, 375px mobile viewport, not just desktop per plan.md §5's Definition of Done): register → lands on `/account` → **hard reload** → session restored from the httpOnly cookie alone → logout → `/account` now redirects to `/login` (deep-link genuinely blocked, not just hidden client-side) → wrong-password login shows the real server-driven error message. Zero console errors/warnings throughout. Screenshots not retained; re-run via chrome-devtools-mcp against `localhost:3000` to reproduce.
- Fixed a nondeterministic UI race caught during that browser pass: `AccountView`'s logout handler was racing its own `router.push("/")` against the auth-guard `useEffect`'s `router.replace("/login")` — same underlying security property either way, but two navigations competing on microtask timing is worth removing regardless. Now single-path via the guard.

**Why:** Per `week1_excecution_prompt.md` Day 2 — auth is the foundation every other Week 1 day depends on (checkout, orders, etc. all need a real user). Built API+UI together per the plan's explicit instruction, so the contract was validated against real client code immediately, not integrated at the end.

**Definition of Done, checked against plan.md §5:**
- Zero TypeScript errors (9/9 projects), zero ESLint warnings (9/9), zero module-boundary violations (verified firing on a real violation, not just passing).
- Unit tests: password/schema edge cases. Integration tests: full transactional auth flow, including both mandatory-style concurrency-adjacent cases for this module (duplicate email race handled at the DB constraint, not just the pre-check; refresh-token reuse detection).
- Mobile viewport (375px) verified via chrome-devtools-mcp — done above, not skipped.
- No secrets/PII in logs — nothing new logged besides the existing bootstrap/error lines from Day 1.

**Follow-ups / known gaps:**
- `RefreshToken` rows are never pruned — expired/revoked rows accumulate forever. Fine at Week 1 scale; a cleanup job is a Week 4 observability/ops concern, not urgent now.
- No rate limiting on `/login` or `/register` yet (brute-force / registration-spam protection) — `ARCHITECTURE.md` §3.4 lists a `rate-limiter` middleware using Redis; not built this week, flagging so it isn't forgotten before production.
- `SiteHeader` is a manual-testing stand-in, not the real mobile bottom nav (design plan §10) — expected to be replaced, not extended, when Day 3's cart/wishlist give it something real to link to.
- Two manually-created browser-test accounts were cleaned up from `woobe_dev` after verification; the automated integration suite cleans up after itself via its `test.woobe.internal` email pattern.

---

## 2026-08-25 — Week 1 Day 3: Catalogue + Cart (API + UI together) — ADR-011

**Branch/commit:** `dev2` (not yet committed — see chat)

**Pre-work: SOLID / Clean Architecture audit of Day 1–2 code (requested before starting Day 3).** Reviewed the `auth` module (the only fully-built reference module) against `architecture.md` §3.1 layering and SOLID: every use-case has a single responsibility, `application` depends only on `AuthRepositoryPort` (DIP) — never Prisma directly — and `infrastructure/repositories/auth.repository.ts` is verifiably the only file touching `@woobe/database` (dependency-cruiser, not just convention). Controllers stay thin. One defect found and fixed: `JwtService.signRefreshToken()`/`verifyRefreshToken()` were dead code (refresh tokens are opaque, sha256-hashed strings, never JWTs — leftover from an earlier design). Deleted both; nothing referenced them. No other violations found. Day 3 replicates the exact same module shape.

**What changed:**
- **Four new/built-out backend modules**, each following auth's `domain/application/infrastructure/interface` shape (ADR-010, only each module's own `infrastructure/repositories/*.ts` imports `@woobe/database`):
  - **`pricing`** (built out from Day 1's placeholder): `resolveEffectiveRatePerKgPaise` pure domain function (variant override wins, else the current admin default) + `CalculateEffectivePriceUseCase`, which is the single path every other module prices a variant through — combines that with `packages/utils`' `calculateWeightBasedPricePaise`. No HTTP surface yet (not needed this week); exported from `pricing.module.ts` for in-process cross-module use.
  - **`inventory`** (read-only slice — reservation/row-locking is Day 4, ADR-015, same repository this extends): `GetAvailableQuantitiesUseCase`, batched, sums `quantityAvailable - quantityReserved` across warehouse rows per variant.
  - **`categories`** (category-filter-only per the plan): `GET /api/v1/categories` (active, sorted) + an exported `findCategoryBySlugUseCase` for products' listing filter.
  - **`products`**: `GET /api/v1/products` (category slug filter, pagination, sorted by the `(categoryId, minPricePaiseCache)` index from ADR-012 — listing intentionally reads the **display/sort cache**, not live pricing, per the schema's own comment on that column) and `GET /api/v1/products/:slug` (detail — recomputes price **live** per variant via `pricing`, plus per-variant stock via `inventory`, since a single product's few variants make that cheap and it's the natural place to exercise the same live-pricing path `cart` uses). Also exports `getVariantsForCartUseCase` for `cart`.
  - **`cart`**: guest identity via a **signed httpOnly `cart_id` cookie** scoped to `/api/v1/cart` (ADR-011, same treatment as the refresh-token cookie), 30-day TTL. `GetCartUseCase` recomputes weight → price → subtotal **live on every read** from `products`/`pricing`/`inventory` — `CartItem` stores only `variantId` + `quantity` (schema.prisma is explicit: no price/subtotal columns), so there's no stored value for a tampered client input to ever override. `AddItemUseCase`/`UpdateItemQuantityUseCase` reject (409) a quantity exceeding live available stock. `MergeGuestCartUseCase` implements ADR-011 exactly: union items, **higher quantity wins on conflict** (`resolveMergedQuantity`, pure domain fn), then re-validates the merged result against live stock — caps or drops lines that no longer fit. Routes: `GET /`, `POST /items`, `PATCH /items/:itemId`, `DELETE /items/:itemId` (all via a new `optionalAuthGuard` middleware — never rejects, just sets `req.user` when a valid token is present, since guest and logged-in customers share these endpoints) and `POST /merge` (strict `authGuard` — the one cart endpoint that isn't guest-accessible).
- **Cross-module reads done via ports, not direct imports of Prisma or of another module's infrastructure** — `products`/`cart` each define narrow interfaces (`PricingReaderPort`, `InventoryReaderPort`, `CategoryReaderPort`/`VariantCatalogPort`) in their own `application/ports/`, wired in the composition root (`<module>.module.ts`) to a one-line pass-through calling the other module's exported use-case instance. Keeps DIP intact (application layer only knows the interface) without inventing adapter files for what's genuinely a trivial pass-through — matches `architecture.md`'s "other modules trigger transitions through its ports" pattern already used for orders/payments/returns.
- **New Zod schemas** (`packages/validation`, ADR-020): `productListQuerySchema` (category, page, limit — capped at 50), `addCartItemSchema`/`updateCartItemSchema` (deliberately **only** `variantId`/`quantity` — no price field exists to tamper with; Zod strips anything else a tampered request body includes).
- **`apps/web` catalog + cart features**: `features/catalog` (`ProductGrid`, `CategoryFilter` — plain links, works without JS — `ProductDetail` server component composing a `ProductPurchasePanel` client island for variant selection + add-to-cart), `features/cart` (`CartProvider`/`useCart` — mirrors Day 2's `AuthProvider` shape, replaces `cart` wholesale from the server response on every mutation rather than updating optimistically, since the server is the only legitimate source for price/subtotal; merges the guest cart via `POST /cart/merge` whenever auth status becomes `"authenticated"`, which is idempotent and safe to call on every session-restore too). Pages: `/products` (category filter via `?category=` query param, Server Component), `/products/[slug]`, `/cart`. `SiteHeader` extended (not replaced — full mobile bottom nav needs wishlist too, still Week 2+) with Shop/Bag links, Bag showing a live item count.
- **New unit tests** for the domain layer added this week (Definition of Done, `plan.md` §5): `resolveEffectiveRatePerKgPaise` (3 tests), `resolveMergedQuantity` (2 tests), `computeCartTotals` (2 tests) — all pure functions, no I/O.
- **Local dev environment gap found and fixed**: Day 2's `RefreshToken` migration (`20260825105515_add_refresh_tokens`) had never actually been applied on this machine's dev/test databases — only Day 1's `init` migration had run, so `apps/api` failed to typecheck (`Property 'refreshToken' does not exist on PrismaClient`) and the auth integration suite 500'd until `prisma migrate dev`/`migrate deploy` were re-run against `woobe_dev`/`woobe_test`. Not a code defect, a stale-local-env issue — flagging in case another machine hits the same thing.

**Why:** Per `week1_excecution_prompt.md` Day 3 — catalogue and cart are what turn the Day 2 walking skeleton into something a guest can actually shop with, and both need to prove the same non-negotiable: price/weight/subtotal are computed server-side, never trusted from the client (`DEVELOPMENT_RULES.md` #1).

**Definition of Done, checked against `plan.md` §5 and the day's own bar ("verify by tampering with a client value and confirming the server ignores it"):**
- Zero TypeScript errors, zero ESLint warnings, zero module-boundary violations across all 9 workspace projects (`pnpm run typecheck` / `lint` / `boundaries:check`).
- Unit tests: pricing/merge/totals domain logic (7 new tests). Full suite (18 tests) green, including Day 2's untouched auth integration tests.
- **Tampering verified directly**: `POST /api/v1/cart/items` with a spoofed `"pricePaise":1` in the body — Zod's schema only recognizes `variantId`/`quantity`, so the field is silently dropped; the response's `unitPricePaise`/`subtotalPaise` came back server-computed (`74400`/`148800` for 2× a 620g variant at ₹1,200/kg) regardless.
- **Stock enforcement verified**: adding past available quantity returns `409 CONFLICT` with the real remaining count, not a silent clamp.
- **Merge-on-login verified**: guest adds 2 units → registers a new account → `POST /cart/merge` → the 2 units land in the new account's cart, guest cart marked `MERGED`, cookie cleared.
- Full guest flow (browse → filter by category → open a product → select colour/size, live price updates → add to bag → adjust quantity → remove) walked in a real browser via `chrome-devtools-mcp` at **375px mobile viewport** — zero unexpected console errors (only the pre-existing, expected guest `/auth/refresh` 401 and a missing favicon, neither related to this week's work).
- `pnpm run build` clean across `apps/api`, `apps/web`, `apps/admin`.

**Follow-ups / known gaps:**
- `pg_trgm` GIN search index (flagged as a Day 1 TODO, tentatively "Day 3" in the schema comment) is **still** deferred — the actual day plan only calls for category filtering ("skip advanced search/sort for now"), and ADR-012's explicit trigger (catalogue > ~50k products, or p95 filter latency > 300ms, or a typo-tolerant/faceted UX need) isn't met yet. Re-flagging so it isn't mistaken for forgotten.
- Cart has no server-side enforcement of ADR-021's 1kg checkout minimum / 1.5kg free-delivery threshold yet — that's explicitly `shipping` + Day 4 (checkout-blocking) scope, not cart-display scope this week.
- `RefreshToken`-style pruning doesn't apply here, but the equivalent gap exists for abandoned guest carts (`ACTIVE` carts with no activity) — no cleanup job; fine at Week 1 scale, a Week 4 ops concern like the refresh-token one.
- `SiteHeader` still isn't the real mobile bottom nav from `woobe_ui_design_plan.md` §10 — that needs wishlist (Week 2+) to be worth building as a single unit; extended minimally instead of replaced.

---

## 2026-08-25 — Pre-Day-4 Patch: Settings Schema + RBAC (ADR-021/023/024)

**Branch/commit:** `dev2` (not yet committed — see chat)

**What changed** (per `project_planning/pre-day-4-patch.md`, three items):

1. **Settings schema** — added `GstSlab` (`id`, `maxPricePaise` nullable/top-slab, `ratePercent`, `createdAt`), one additive migration (`20260825150106_add_gst_slabs`, pure `CREATE TABLE`), seeded per ADR-023 (5% ≤ ₹2,500, 18% above). Audited `ShippingRule` and `PricingSetting` per the patch's own escape hatch: both were **already correct** from Day 1 — `ShippingRule` already had `minWeightGramsForCheckout`/`freeDeliveryThresholdGrams` (equivalent to the patch's `minOrderWeightGrams` naming, just not identical), `PricingSetting` was already DB-backed. Left both alone rather than adding a duplicate/renamed column — see Deviations below.
2. **Cart weight-threshold audit** — grepped `apps/api/src/modules/cart`, `apps/api/src/modules/shipping`, and `apps/web` for hardcoded `1000`/`1500` or a `WeightThresholdBanner`. Found neither. The `shipping` module is still Day 1's placeholder (`Built out: Week 1 Day 4`) and Day 3's cart module never implemented checkout-blocking at all — it was correctly deferred, not built with hardcoded values that needed fixing. **Zero code changes for this item.**
3. **RBAC replacement** — `Role` enum extended (additive `ALTER TYPE ... ADD VALUE`, migration `20260825150147_extend_role_enum`) with `SUPER_ADMIN`/`ORDER_PROCESSING_STAFF`/`PRODUCT_MANAGEMENT_STAFF`; seeded admin user migrated to `SUPER_ADMIN` via the seed script's upsert `update` clause (so re-running seed is the data migration, not a one-off SQL statement). New `apps/api/src/config/permissions.ts` — `PERMISSIONS` map + `ROLE_PERMISSIONS: Record<Role, Set<Permission>>` per ADR-024 (customer: none; super_admin: all five; order_processing_staff: `MANAGE_ORDERS` only; product_management_staff: `MANAGE_CATALOG` + `MANAGE_INVENTORY` only — confirmed disjoint, neither a subset of the other), 4 new unit tests. `rbac-guard.ts`'s `requireRole` replaced with `requirePermission(...permissions)` (passes if the caller holds ANY of the listed permissions — same ergonomics as the old variadic `requireRole`); zero route-level changes needed since nothing used `requireRole` yet. Single source of truth for the `Role` TS type moved to `packages/types` (`ROLE` const updated to the four contracted roles) — every hand-rolled `"CUSTOMER" | "ADMIN"` union across `auth-guard.ts`, `optional-auth-guard.ts`, `jwt.service.ts`'s `AccessTokenPayload`, `user.entity.ts`, and `apps/web`'s `auth.client.ts` now imports `Role` from there instead.

**Why:** Day 4 (checkout/GST/shipping) was designed against `GstSlab`/`ShippingRule` and ADR-024's role model — this patch is the small additive catch-up so Day 4 lands on the right foundation instead of needing its own schema/RBAC rework mid-stream.

**Migration safety:** both new migrations are purely additive (`CREATE TABLE`, `ALTER TYPE ... ADD VALUE`) — neither matches `scripts/check-destructive-migration.mjs`'s patterns (DROP TABLE/COLUMN, TRUNCATE, column TYPE change), confirmed by manual inspection of both `migration.sql` files. No `--accept-data-loss` marker needed. Applied to `woobe_dev` and `woobe_test` (the auth integration suite's database).

**Deviations from `pre-day-4-patch.md`, both deliberate:**
- **Did not rename/duplicate `ShippingRule`'s existing columns** to match the patch's `minOrderWeightGrams` naming — `minWeightGramsForCheckout` already exists from Day 1 with identical semantics (seeded `1000`) and `freeDeliveryThresholdGrams` already existed too. Renaming would be a destructive column change for zero functional gain; adding a second, differently-named column with the same meaning would just create confusing duplicate settings. Applied the patch's own stated principle for `PricingSetting` ("if already correct, leave it alone") to `ShippingRule` as well.
- **Kept `ADMIN` in the `Role` enum as an unused legacy value** rather than removing it to leave exactly the four contracted roles. Postgres can't cheaply drop an enum value without recreating the type (new type, migrate column via `USING`, drop old type) — meaningfully more invasive DDL for a Week-1 dev database with a single affected row. Nothing in the codebase issues `ADMIN` anymore (the TS-level `Role` union in `packages/types` no longer includes it, so it's unreachable from application code); the DB enum keeping a harmless extra value is the additive, expand-only choice consistent with the rest of this patch and with ADR-013's expand-contract migration discipline.

**Verified against `plan.md` §5 Definition of Done:** zero TypeScript errors, zero ESLint warnings, zero module-boundary violations across all 9 workspace projects; 22/22 tests passing (4 new permission-mapping tests, all prior tests untouched and still green); `pnpm run build` clean for `apps/api`/`apps/web`/`apps/admin`; live smoke test — `admin@woobe.in` logs in, `/auth/me` and the login response both return `"role":"SUPER_ADMIN"`, JWT payload carries the new role correctly.

**Follow-ups / known gaps:**
- `ADMIN` remains a dead value in the Postgres `Role` enum (see Deviations) — worth a one-time cleanup migration if the team ever wants full enum purity, not urgent.
- No admin-only routes exist yet to exercise `requirePermission` against a real request (expected — the admin settings/staff UI is Week 2+ scope per `week1_excecution_prompt.md`'s explicit exclusions). The middleware and permission mapping are unit-tested in isolation instead.

---

## 2026-08-25 — HANDOFF: Razorpay config research done, not yet implemented — Day 4 next

**Branch/commit:** `dev2` @ `6f8c17b` (pre-Day-4 patch) is pushed and clean. One uncommitted change on top: `razorpay` added to `apps/api/package.json` (package installed, nothing built with it yet) — committing that alongside this entry so nothing is lost mid-session.

**State of the repo right now (read this before doing anything else):**
- Days 1–3 (foundation, auth, catalogue+cart) — done, verified, pushed.
- Pre-Day-4 patch (`project_planning/pre-day-4-patch.md`) — done, verified, pushed as `6f8c17b`: `GstSlab` table + seed, `ShippingRule`/`PricingSetting` audited (already correct, untouched), RBAC replaced with the 4 ADR-024 roles (`customer`/`super_admin`/`order_processing_staff`/`product_management_staff`), `apps/api/src/config/permissions.ts` + `requirePermission()` middleware, seeded admin migrated to `SUPER_ADMIN`. Full report is two journal entries up.
- **Razorpay config — started, not finished.** The user asked to "search and implement razorpay configurations" ahead of Day 4 (real API keys to be provided later — `.env`'s `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET`/`RAZORPAY_WEBHOOK_SECRET` stay stub values for now, that's expected and fine, `env.ts` already has them as optional). Session ended on usage limit before implementation — **only the npm package is installed, no code written.**

**Research already done — use this instead of re-researching (saves the next session a WebFetch round-trip):**
- Package: `razorpay` (npm, v2.9.8, already in `apps/api/package.json` — official Node SDK, ships its own `.d.ts`, no `@types/razorpay` needed).
- Client: `new Razorpay({ key_id: env.RAZORPAY_KEY_ID, key_secret: env.RAZORPAY_KEY_SECRET })`.
- Create order (ADR-014): `instance.orders.create({ amount, currency: "INR", receipt })` → returns `Promise<Orders.RazorpayOrder>` when called without a callback. **`amount` is in paise** (subunits) — matches this codebase's Int-paise convention exactly, no conversion needed.
- Client-redirect verification (NOT authoritative — ADR-014 says never trust this alone): `Razorpay.validatePaymentVerification({ order_id, payment_id }, signature, secret)` from `razorpay/dist/utils/razorpay-utils` — HMACs `order_id|payment_id` with SHA-256 against `key_secret` (not the webhook secret).
- **Webhook verification (the authoritative one)**: `Razorpay.validateWebhookSignature(rawBody, signature, secret)` — HMACs the **raw request body string** (not parsed JSON) against `RAZORPAY_WEBHOOK_SECRET`, compares to the `X-Razorpay-Signature` header. **Important implementation gotcha found during research: the webhook route MUST use `express.raw()` for its body parser, not the global `express.json()` already mounted in `app.ts`** — signature verification will silently fail if the body has already been JSON-parsed/re-serialized, because the HMAC is computed over the exact bytes Razorpay sent. Mount `express.raw({ type: "application/json" })` on that one route specifically before `validateWebhookSignature` runs.

**What's NOT done yet (next session's first task):**
- `apps/api/src/modules/payments/infrastructure/services/razorpay.service.ts` — wrap `createOrder()`/`verifyPaymentSignature()`/`verifyWebhookSignature()` around the calls above, following the same shape as `auth`'s `BcryptService`/`JwtService` (plain class, no Prisma access — this service doesn't touch `Payment`/`WebhookEvent` itself, the checkout/webhook use-cases will).
- Wire it into `payments.module.ts`'s composition root (still no HTTP routes — those need Day 4's `Order` model/checkout flow to exist first; `payments` stays a placeholder router until Day 5 per the original plan, this config work was just pulled forward).
- Unit tests for the two verification methods using real `crypto.createHmac` in the test (no network/real keys needed) — same pattern as Day 3's `resolve-effective-rate.test.ts`.
- Run full Definition of Done verification (typecheck/lint/boundaries/tests/build) before committing.

**Then: Day 4 itself** (`week1_excecution_prompt.md`) — checkout page, checkout endpoint with `SELECT ... FOR UPDATE` inventory reservation (ADR-015, the concurrency-critical part — verify with two near-simultaneous checkouts on a stock=1 item, only one should succeed), order creation with a full price/tax/shipping snapshot, GST via the now-live `GstSlab` slabs, shipping fee/threshold via `ShippingRule`, order state machine (`PENDING_PAYMENT → ...`). Coupons explicitly skipped this week. Confirmed ready in the prior session — no blockers, both settings tables and RBAC are in place and correctly read live (never hardcoded).

**Environment note for whoever continues this:** local Postgres/Redis are running natively (not Docker — this machine doesn't have Docker installed), ports 5433/6380 per `.env`. `woobe_dev` and `woobe_test` are both migrated current as of the pre-Day-4 patch. `apps/api`/`apps/web`/`apps/admin` dev servers were running on 4000/3000/3001 in the previous session's shell — they won't survive a new session, restart with `pnpm --filter <pkg> run dev` (remember to `rm -rf apps/web/.next apps/admin/.next` first if a `pnpm run build` was run in between, it collides with the dev server's cache — bit us twice this session).

---

## 2026-08-25 — Correction: Day 4 was NOT actually done; SOLID/Clean Architecture audit + Day 4 built for real

**Branch/commit:** `dev1` (== `dev2`, identical histories) — pending commit, see chat.

**Correcting the record:** the user believed Day 3 and Day 4 were both complete and asked to audit for SOLID/Clean Architecture violations before starting Day 5. They were not — the previous journal entry above is titled "HANDOFF... Day 4 next" and says so itself; this got lost somewhere between sessions. Verified directly before touching anything: `orders`, `payments`, `shipping` were each still a single-file placeholder `<name>.module.ts` (no `domain/application/infrastructure/interface` layers, unlike `auth`/`cart`/`products`), and there was no checkout endpoint, no inventory reservation logic, no order creation. Only Days 1–3 + the pre-Day-4 settings/RBAC patch + the razorpay package install were real. Flagged this to the user and got explicit go-ahead to audit first, then build Day 4 for real before Day 5 (which depends on Day 4's order/checkout flow existing).

**SOLID / Clean Architecture audit (Days 1–3 + pre-Day-4 patch) — no violations found.** Reviewed every built-out module (`auth`, `cart`, `categories`, `products`, `pricing`, `inventory`) against `architecture.md` §3.1 layering and SOLID, plus the RBAC/permissions patch:
- Every module's `application` layer depends only on its own `*.port.ts` interfaces, never Prisma directly (DIP) — verified both by reading every repository/use-case file and by running `pnpm --filter @woobe/api run boundaries:check` for real (`dependency-cruiser`, not just eyeballing): passed clean, 121 modules / 251 dependencies cruised, before any Day 4 code existed.
- One `grep` hit for `@woobe/database` outside an `infrastructure/` folder (`categories/application/use-cases/find-category-by-slug.use-case.ts`) turned out to be a false positive — the string only appears inside a doc comment, not an import. No actual violation.
- Cross-module reads go through narrow, single-purpose ports (ISP) wired as one-line pass-through adapters in each module's composition root (`*.module.ts`) — `VariantCatalogPort`, `PricingReaderPort`, `InventoryReaderPort`, `CategoryReaderPort`, etc. — never a module reaching into another's `infrastructure/` or Prisma models directly.
- Controllers stay thin (parse → call use-case → map response), domain logic stays in pure, dependency-free functions (`resolveEffectiveRatePerKgPaise`, `resolveMergedQuantity`, `computeCartTotals`), RBAC's `ROLE_PERMISSIONS` map is genuinely open for extension (new role = new map entry, no route-guard rewrites — OCP).
- Also caught (unrelated to the audit itself, found while verifying): the local Prisma client was stale — `packages/database`'s generated client didn't know about `GstSlab`/`SUPER_ADMIN` from the pre-Day-4 patch, failing `packages/database`'s own typecheck. Ran `prisma generate`; not a code defect, a stale local artifact.
- **Conclusion: nothing to fix.** Day 4 replicates the exact same module shape the audit confirmed clean.

**What changed (Day 4 itself, per `week1_excecution_prompt.md`):**
- **`shipping` module — built out** (was a placeholder): `resolveShippingEvaluation` pure domain function (ADR-021's minimum-order/free-delivery weight bands) + `EvaluateShippingUseCase`, reading `ShippingRule` live, never hardcoded. No HTTP surface — consumed in-process by `cart` (progress display) and `orders` (checkout-blocking + fee snapshot), per the placeholder's own composition-root comment describing exactly this split.
- **`pricing` module — extended** with GST (ADR-023): `resolveGstRatePercent` pure domain function (picks the slab by ascending `maxPricePaise`, nulls-as-unbounded last — matches `GstSlab`'s own schema comment) + `CalculateGstUseCase`, reading `GstSlab` rows live.
- **`inventory` module — extended** with ADR-015's reservation: `InventoryRepository.reserveForCheckout` does `SELECT ... FOR UPDATE` (raw SQL, Prisma has no query-builder API for row locks) inside the caller's transaction, sums locked rows' available quantity, and only then decides whether to increment `quantityReserved` — all-or-nothing across every line in one checkout attempt. Exposed via `ReserveInventoryForCheckoutUseCase`.
- **`cart` module — extended**: `GetCartUseCase`'s `CartView` now carries a `shipping` field (meets-minimum / free-delivery / fee / grams-remaining) computed via the same `shipping` module checkout uses, so the cart page and checkout can never silently disagree on the thresholds. Added `sku` to `CartLineView` (needed for order-item snapshots) and `MarkCartConvertedUseCase` (cart → `CONVERTED` at checkout, called inside the checkout transaction).
- **`orders` module — built out.** `CheckoutUseCase` orchestrates: resolve the caller's cart (guest cookie or logged-in user) → read it live (price/weight/stock, same path the cart page renders) → reject empty/unavailable-item/below-minimum-weight carts → calculate GST per line → **inside one Unit-of-Work transaction**: row-lock-reserve inventory, create the `Order`+`OrderItem` rows with a full price/tax/shipping snapshot, mark the cart `CONVERTED`. All three commit or roll back together.
  - **The cross-module-transaction problem, and how it was solved without breaking ADR-010:** `Order`, `Inventory`, and `Cart` are owned by three different modules, each restricted to touching only its own Prisma models from its own `infrastructure/`. A real correctness requirement (reservation + order creation + cart conversion must be atomic) needs one database transaction spanning all three. Solution: a **Unit-of-Work port** (`TransactionPort.run(fn)`) that hands every participating write method an **opaque `tx: unknown` handle** — the `application` layer never inspects it, only threads it through; only each module's own `infrastructure` file (which already imports Prisma per ADR-010) ever casts it back to a real `Prisma.TransactionClient`. `orders/infrastructure/repositories/transaction.repository.ts` starts the transaction (`prisma.$transaction`); `inventory`'s and `cart`'s repositories gained `tx`-accepting write methods that participate in it. Verified this actually works under real concurrent load, not just in theory — see the integration test below.
  - Order numbers (`WOOBE-YYYYMMDD-<random>`) generated via a small `OrderNumberGeneratorService` (infrastructure, injected via a port — same pattern as auth's `BcryptService`/`JwtService`), with a bounded retry-on-collision loop in the use-case (collisions are astronomically unlikely but the repository can tell a genuine `orderNumber` unique-constraint hit apart from any other by inspecting Prisma's `meta.target`).
  - Coupons deliberately skipped (per the day's own scope). Address book / saved `Address` rows deliberately **not** wired up this week — no module in the architecture owns that table, and the schema's own `shippingSnapshot` JSON column is the actual snapshot of record regardless; `Order.addressId` stays `null` for every order this week. Flagging as a deliberate simplification, not an oversight.
- **`packages/validation`**: new `checkout.schema.ts` (`checkoutAddressSchema` + `checkoutSchema` — contact email, delivery address, `paymentMethod`; price/tax/shipping are never client fields, matching cart's existing schema philosophy). Extracted the Indian-phone regex out of `auth.schema.ts` into a new `shared.ts` so `checkout.schema.ts` didn't duplicate it.
- **`apps/api/src/shared/errors`**: added `UnprocessableEntityError` (422) for business-rule rejections (below minimum weight, empty cart) distinct from `ValidationError` (400, malformed request shape).
- **`apps/web`**: `features/checkout/{api,components}` (`CheckoutForm` — react-hook-form + the same `checkoutSchema`, pre-fills from the logged-in user's profile but stays fully editable, shows an inline "Order placed!" summary on success rather than navigating to a not-yet-built confirmation route), `/checkout` page (routing only). `CartPageContent` now shows the live shipping-progress banner and a real "Checkout" link (replacing the "Shipping and checkout land Day 4" placeholder text), disabled until the cart clears the minimum weight.

**Bug found and fixed during verification, not by inspection:** the guest `cart_id` cookie (`cart-cookie.ts`) was `Path`-scoped to `/api/v1/cart` only, set on Day 3 when only cart's own routes read it. Checkout's `POST /api/v1/orders/checkout` needs the same cookie and never received it — every guest checkout attempt failed with "Your bag is empty" even with items in the cart, because the browser (and supertest's cookie jar, which correctly respects `Path`) never sent the cookie outside its original scope. Widened to `/api/v1` (still httpOnly + signed, so no new exposure) — `orders`' controller imports the exact same `setCartIdCookie`/`clearCartIdCookie` functions from `cart`'s interface layer rather than duplicating the cookie config, so set and clear can never drift apart on `Path` again.

**Test-database gap found and fixed, not assumed away:** `woobe_test` (the integration-test database) turned out to be missing the two pre-Day-4-patch migrations entirely (`gst_slabs` table didn't exist) and had zero seed data — no warehouse, no category, no `PricingSetting`/`GstSlab`/`ShippingRule` rows — despite the prior session's journal entry claiming it was "migrated current." Ran `prisma migrate deploy` and the seed script against it directly. Flagging because the earlier claim was wrong, not just stale.

**New integration test** (`orders.integration.test.ts`, 4 tests, against real Postgres — not mocked): creates real product/variant/inventory rows per test, cleans up after itself.
- **The mandatory concurrency test** (`week1_excecution_prompt.md`'s own bar): two guest agents each add the last unit of a stock=1 item to their own cart, then checkout **simultaneously** (`Promise.all`) — asserts exactly one gets `201`, the other gets `409 CONFLICT`, and the database ends up with `quantityReserved: 1` (not 0, not 2) and exactly one `Order` row. This is what actually proves the `SELECT ... FOR UPDATE` unit-of-work works under real contention, not just in a single-threaded read of the code.
- Full snapshot correctness: independently recomputes the expected subtotal/tax/shipping/total from the *same* live settings rows checkout reads (not hardcoded numbers, so the test doesn't silently rot if seed data changes), then asserts both the HTTP response and the persisted `Order` row match.
- Below-minimum-weight rejection (422) and empty-cart rejection (422).

**Definition of Done, checked against `plan.md` §5 and the day's own bar** ("a checkout attempt correctly reserves stock... and the order row has a full price/tax snapshot independent of current product state"):
- Zero TypeScript errors, zero ESLint warnings, zero module-boundary violations across all 9 workspace projects (154 modules / 329 dependencies cruised, up from 121/251 pre-Day-4).
- Full test suite green: 35/35 (`apps/api`), including the new concurrency test proving the mandatory "only one wins" property against real Postgres row locks.
- `pnpm run build` clean across `apps/api`, `apps/web`, `apps/admin` — `/checkout` compiles and is routable.
- Live browser walkthrough **not done this session** — a `chrome-devtools-mcp` browser instance from another/prior session was already holding the shared Chrome profile lock (`~/.cache/chrome-devtools-mcp/chrome-profile`), and killing an unknown process without knowing whether another active session owns it wasn't a safe call to make unilaterally. Relied on the integration suite (which exercises the real HTTP API end-to-end, including the concurrency scenario) instead. Flagging honestly rather than claiming a browser check that didn't happen — worth a manual pass next session.

**Follow-ups / known gaps:**
- ADR-015's "release reservation on payment failure/timeout via a BullMQ delayed job" is **not** built — BullMQ isn't wired into `apps/api` at all yet. Reservations made this week accumulate `quantityReserved` with no release path until Day 5's payment confirmation (or later) deducts it or a cleanup job reclaims it. Fine at Week 1 scale, matches this project's established pattern of flagging deferred cleanup jobs (`RefreshToken` pruning, abandoned-guest-cart cleanup) rather than building them speculatively.
- No `GET /orders/:id` or "my orders" list yet — Day 5's explicit scope, alongside the real order-confirmation page. Checkout returns the full order in its own response, which is what today's UI uses for the inline success view.
- Razorpay order creation and COD's immediate `CONFIRMED` transition are **not** wired — every order this week lands at `PENDING_PAYMENT` regardless of `paymentMethod`, exactly per Day 4's scope boundary with Day 5.
- No saved-address book (`Address` model unused this week) — see the "deliberate simplification" note above.
- Live browser verification skipped this session (see above) — do a manual mobile-viewport pass through browse → cart → checkout → COD next session before or alongside Day 5.

---

## 2026-08-25 — Week 1 Day 5: Payments (Razorpay + COD) + Order Confirmation — ADR-014

**Branch/commit:** `dev1` (== `dev2`) — pending commit, see chat. Same session as the Day 4 correction above; continued straight through on explicit go-ahead.

**What changed, per `week1_excecution_prompt.md` Day 5:**

- **`payments` module — built out** (was a placeholder, per its own comment: "Built out: Week 1 Day 5"): `CreateRazorpayOrderUseCase` (ADR-014's Orders API integration — creates the Razorpay-side order + a `Payment` row, idempotent on retry, never confirms anything), `ConfirmCodOrderUseCase` (COD's "no gateway step" — confirms the order and records a Payment row for accounting consistency, in one transaction), `HandleRazorpayWebhookUseCase` (the authoritative confirmation path — see below). `RazorpayService` (infrastructure) wraps the `razorpay` SDK exactly as the prior handoff session researched (`orders.create()`, `Razorpay.validateWebhookSignature()`), with one correction — see "Two things the prior research got wrong" below.
- **`orders` module — extended**: `ConfirmOrderUseCase`/`MarkOrderPaymentFailedUseCase` (the `PENDING_PAYMENT → CONFIRMED`/`PAYMENT_FAILED` transitions, plan.md §4 — the only place either is written, exported for `payments` to call through a port, never by `payments` writing to `Order` itself), both **idempotent by construction** (a repeat call for an already-transitioned order is a no-op `{changed: false}`, not an error) via a new `transitionStatus(id, from, to, tx)` repository method built on a conditional `UPDATE ... WHERE status = ?`. Also added `GetOrderUseCase` (order-confirmation page), `ListMyOrdersUseCase` (My Orders), `GetOrderForPaymentUseCase` (payments' read view), and `GET /orders/:id` / `GET /orders` routes.
- **`inventory` module — extended**: `finalizeReservation` (a `CONFIRMED` order turns its hold into a real deduction — both `quantityReserved` and `quantityAvailable` drop) and `releaseReservation` (a `PAYMENT_FAILED` order gives the hold back — only `quantityReserved` drops). Both row-lock first, same as Day 4's `reserveForCheckout`, and share its locking helper — refactored the allocation logic into two correctly-directional private methods (`incrementReservedAcrossRows` for reserve, `decrementReservedAcrossRows` for finalize/release) after an early draft of this tried to share ONE method between reserve and finalize/release and got the direction wrong (increment vs. decrement, bounded by *available* capacity vs. bounded by *existing reserved* amount) — caught before it ever ran, not after.
- **Unit-of-Work pattern reused, not reinvented**: `payments` defines its own `TransactionPort` (same shape as `orders`', per this codebase's established one-port-shape-per-module-pair convention) and starts its own `prisma.$transaction` in its own infrastructure — COD confirmation and webhook processing each need Payment + Order-status + Inventory writes to commit atomically, exactly the same cross-module problem Day 4's checkout solved, solved the same way.
- **Frontend**: `features/payments` (Razorpay Checkout script loader + client, COD/Razorpay-order API calls), `features/orders` (`OrderConfirmation` — drives COD to auto-confirm and Razorpay to open Checkout then **poll** the order rather than trust the widget's own success callback; `MyOrdersList`). Routes: `/order-confirmation/[id]` (routing only), `/account/orders` (routing only). `CheckoutForm` now redirects to the confirmation page on success instead of showing an inline summary (Day 4's placeholder — flagged then as "confirmation page is Day 5 scope," now built for real). `SiteHeader` gained a "My orders" link.

**ADR-014's idempotency, layered (this is the part with an explicit mandatory test — see below):**
1. **`(provider, eventId)` unique constraint** on `WebhookEvent` — catches Razorpay literally resending the same delivery. Keyed off the `X-Razorpay-Event-Id` **header**, not a field inside the payload — see the research correction below.
2. **The conditional order-status transition** (`changed: false` when the order already left `PENDING_PAYMENT`) — catches the rarer case of two *different* deliveries both trying to move the same order (e.g. a webhook retry racing layer 1's own dedup-row creation). Every downstream effect (Payment update, inventory finalize/release) is gated on `changed`, so double-processing is safe by construction, not just usually safe.

**Two things the prior session's Razorpay research got wrong — found by verifying, not by trusting the handoff note:**
1. **The webhook's dedup key isn't in the payload.** Fetched Razorpay's own webhook-payload docs directly (`https://razorpay.com/docs/webhooks/payloads/payments/`) — a `payment.captured` event has no `id`/`event_id` field anywhere in its JSON body. The actual per-delivery unique identifier is the `X-Razorpay-Event-Id` **HTTP header** (confirmed via Razorpay's own webhook-validation docs, which explicitly recommend deduping on it). The prior handoff note never mentioned this header at all — it would have led to building the dedup table against the wrong (nonexistent) field.
2. **The suggested `express.raw()`-on-one-route fix for signature verification doesn't work as described.** The handoff note said to mount `express.raw({type:"application/json"})` on just the webhook route, ahead of the global `express.json()`. But `app.use(express.json())` is already mounted globally in `app.ts`, before any module router — it runs first for every request including the webhook route and fully drains the body stream, so a route-specific raw parser registered afterward would see nothing. Fixed with the standard pattern for this exact problem instead: `express.json()`'s own `verify` option (`capture-raw-body.ts`) captures the exact raw bytes into `req.rawBody` *during* parsing, before `JSON.parse` touches them — both `req.body` (every other route) and `req.rawBody` (only the webhook route needs it) come out of the same single parse, no second body parser involved.

**Definition of Done, checked against `plan.md` §5 and Day 5's own "full slice" bar:**
- Zero TypeScript errors, zero ESLint warnings, zero module-boundary violations across all 9 workspace projects (182 modules / 410 dependencies cruised, up from 154/329 after Day 4).
- Full test suite green: 40/40 (`apps/api`) — 9 new tests this session (5 payments integration, 4 orders/domain from Day 4 untouched).
- **The mandatory duplicate-webhook test, done for real** (`payments.integration.test.ts`): signs a synthetic `payment.captured` payload with a real HMAC (test webhook secret), POSTs it to `/api/v1/payments/razorpay/webhook`, asserts the order confirms and inventory finalizes — then **resends the identical signed payload with the same event id**, asserts the second delivery returns `"deduped"`, and asserts the order status, inventory numbers, and Payment row count are all *unchanged* by the resend (not just "didn't error").
- COD idempotency verified the same way: calling `/payments/cod/confirm` twice for the same order results in exactly one Payment row and one inventory deduction, not two.
- `payment.failed` verified to release the reservation (quantity given back) without deducting stock, distinct from the success path.
- `pnpm run build` clean across `apps/api`, `apps/web`, `apps/admin` — `/order-confirmation/[id]` and `/account/orders` compile and are routable.
- **Not achievable in this environment, flagged honestly rather than faked:** a real end-to-end Razorpay Checkout run (register → browse → cart → checkout → pay via Razorpay test mode → confirmed order) needs real Razorpay test-mode keys (still stubbed, `DECISIONS_PENDING.md` #4) and, for the webhook specifically, a publicly reachable HTTPS URL (Razorpay can't call `localhost`) — `.env.example` now documents exactly what's needed (an ngrok-style tunnel) for whoever adds real keys. **COD's full slice works end-to-end today with the existing stubs** — no real keys needed, verified by the integration tests above (a live browser click-through of the COD path specifically is still worth doing manually, see below).
- Live browser walkthrough **still not done this session** — the same shared `chrome-devtools-mcp` Chrome profile lock from the Day 4 entry above was still held at the end of this session; same reasoning for not force-killing an unfamiliar process applies. `apps/api` (4000) and `apps/web` (3000) dev servers are left running for a manual pass.

**Follow-ups / known gaps:**
- ADR-015's BullMQ-delayed-job release is **still** not built (Day 4's flagged gap, now narrower): a `CONFIRMED` or `PAYMENT_FAILED` order now correctly finalizes/releases its reservation, but an order that reaches `PENDING_PAYMENT` and then **never** gets a webhook or a COD confirm call (abandoned Razorpay checkout, browser closed mid-payment) still leaks a permanent `quantityReserved` hold with no automatic release path. BullMQ isn't wired into `apps/api` at all yet — a real fix needs that infrastructure, not just an inventory-module method.
- `Payment.orderId` has no DB-level unique constraint — `CreateRazorpayOrderUseCase`'s idempotency check (read-then-create) is a soft guard, not airtight under true concurrency (two simultaneous "create razorpay order" calls for the same order could theoretically both pass the check and create two Payment rows / two Razorpay orders). Low-risk in practice (needs a double-click racing itself within the same request round-trip) and not something this week's scope calls for a schema migration over; flagging so it isn't mistaken for solved.
- Returns, refunds, reviews, coupons, wishlist, and admin product/order-management UI remain explicitly out of scope this week (`week1_excecution_prompt.md`'s own exclusion list) — nothing here changes that.
- The Razorpay "retry a failed/pending payment" flow (`PAYMENT_FAILED → PENDING_PAYMENT` in plan.md §4's state diagram) isn't built — `OrderConfirmation`'s "Try payment again" button re-attempts from the same `PENDING_PAYMENT` state via `payWithRazorpay`'s own retry path, but there's no UI/API path back from an already-`PAYMENT_FAILED` order. Not called for by this week's plan; worth knowing before Week 2.

---

## 2026-08-25 — Week 1 Completion Audit: 2 corrective fixes, verified end-to-end

**Branch/commit:** `dev1`, commit `4ff22c064f69623f9ba33a9861466ccf85413d5e` — pushed to `origin/dev1`.

**What changed:** an independent, live-browser re-verification of every Week 1 day (Days 1–5), not a re-read of this journal's own claims. Found and fixed two real bugs; everything else audited clean.

1. **Cart reactivation / duplicate-cart 500.** `GetOrCreateCartUseCase`'s `userId` branch only ever checked for an *active* cart; a logged-in user whose cart had already `CONVERTED` (i.e. anyone who had ever completed a checkout) had no active cart, but also couldn't get a fresh one because `createCart` hit the schema's one-cart-per-user constraint against their old, converted row — every `GET /cart` after a first order 500'd. Fixed by adding `CartRepositoryPort.findCartByUserId` (any status) and `reactivateCart` (clears old items, flips status back to `ACTIVE`) — a returning customer now gets their old cart row reactivated empty rather than a doomed insert. New regression test: `cart.integration.test.ts` (register → checkout → `GET /cart` / `POST /cart/merge` / add-item all still work post-conversion).
2. **Order-confirmation authentication race.** `OrderConfirmation`'s initial fetch fired before `AuthProvider`'s silent-refresh resolved; a logged-in user's own order, fetched while `authStatus === "loading"`, looked exactly like a guest request to `GetOrderUseCase`'s ownership check and 404'd permanently (no retry). Fixed by gating the fetch effect on `authStatus !== "loading"` — same guard `CartProvider` already used for the identical class of race.

**Why:** the user's audit prompt required independent, live verification (browser walkthrough + full test suite, not trust in prior journal entries) before Week 1 could be called done, and explicitly allowed only minimal corrective fixes for verification failures — no new features, no refactors, no Week 2 work.

**Verified, not just fixed:** full suite green (typecheck/lint/boundaries/tests/build) plus a live `chrome-devtools-mcp` walkthrough of register → browse → cart → checkout → COD confirm → reorder. Razorpay's real-keys/webhook-tunnel gap (flagged in the Day 5 entry above) was correctly classified as **blocked** (external dependency, not a code defect) per the audit's own instructions, not counted as a failure.

**Follow-ups / known gaps:** none new — this entry only fixes what it found, all pre-existing gaps listed in the Day 4/5 entries above still stand.

---

## 2026-08-25/26 — UI redesign pass: mobile-first, feminine editorial restyle (ADR-022)

**Branch/commit:** `dev1` — uncommitted (styling work, paused for review before Week 1 is called finished; see chat).

**What changed:** Week 1's functional slice was verified working (see the audit entry above) but the UI itself was still bare — `packages/ui` had only `Button`/`Input`/`Label`/`FormField`, pages were plain-Tailwind, no cards/motion/skeletons, a cramped single mobile header doing both branding and navigation. The user asked for the pages to be professionally styled — mobile-first, feminine editorial tone — before Week 1 is considered finished. Two scope calls made with the user up front: (1) a focused, real-data-only homepage rather than the full design doc's blueprint (skipping sections that need content that doesn't exist yet — UGC photos, video, "Shop by Vibe," "Build Your Look" — flagged Week 2+ in the design doc itself); (2) adopt real `@base-ui/react` + `motion` + `embla-carousel-react` now (ADR-022), hand-authored on top of Base UI in `packages/ui/src/primitives` rather than shadcn's own CLI/scaffolding (which assumes a monorepo layout this repo doesn't have).

- **New dependencies:** `@base-ui/react`, `lucide-react`, `@woobe/utils` added to `packages/ui`; `motion`, `embla-carousel-react`, `lucide-react` added to `apps/web`.
- **New primitives** (`packages/ui/src/primitives/`): `Card`/`CardHeader`/`CardTitle`/`CardContent`, `Badge` (neutral/success/error/outline), `Spinner`, `Skeleton`, `RadioGroup`/`RadioGroupItem` (wraps Base UI's real `Radio`/`RadioGroup`, bridged into react-hook-form via `Controller` since it's a controlled component, not a native input). `Button` now also exports `buttonVariants` for styling non-`<button>` elements (this Button has no `asChild`/Slot support).
- **New composed components** (`packages/ui/src/components/`): `PriceTag` (price + optional weight/rate-per-kg line), `ProgressBar` (wraps Base UI's `Progress`, powers the cart's two-stage weight-threshold banner from data the API already returned — `cart.shipping.meetsMinimum/isFreeDelivery/gramsToMinimum/gramsToFreeDelivery` — no new API fields needed).
- **Navigation restructured** to fix the audit-flagged cramped mobile header: `SiteHeader` is now a slim logo + cart-shortcut bar; a new sticky `BottomNav` (`md:hidden`) carries the real mobile navigation (Home/Shop/Bag/Account — honestly 4 tabs, not the design doc's 5, since Search and Wishlist aren't built features). Desktop keeps `SiteHeader`'s horizontal nav at `md:` and up.
- **Every storefront page restyled** on the new primitives: homepage (`Hero`/`TrustStrip`/`CategoryTiles`/`ProductRail`, with `Reveal`'s Motion-based scroll-in respecting `prefers-reduced-motion`), PLP (`ProductGrid`/`CategoryFilter`), PDP (`ProductDetail`/`ProductPurchasePanel`, including a `position:fixed` mobile buy bar), cart (`WeightThresholdBanner`, `CartLineItem`), checkout (`CheckoutForm`'s `Card`-sectioned layout + `RadioGroup` payment method), order confirmation, my orders, login/register/account.
- **Explicitly out of scope, stated not silently skipped:** wishlist hearts, search, a size-chart dialog, image gallery/zoom, "Build Your Look," UGC/video sections, `apps/admin` styling — no backend/data exists for any of these yet.

**Why:** functional-but-unstyled was correct for proving Week 1's flows worked, but not what the user wants shipped as "Week 1 done" — the explicit ask was a professional, mobile-first, feminine-editorial pass before moving to Week 2.

**Verified:** typecheck/lint clean on both `apps/web` and `packages/ui`; live `chrome-devtools-mcp` walkthrough at 375px through every redesigned page; `apps/api`'s own test suite re-run untouched (frontend-only pass, confirmed no backend contract changed).

**Follow-ups / known gaps:** not yet committed — paused for the user's review. See the two follow-on entries below for bugs found *after* this pass (both while dogfooding the new pages, not part of this entry's own verification).

---

## 2026-08-26 — Desktop nav icons + PDP quantity stepper

**Branch/commit:** `dev1` — uncommitted, same pending styling work as the entry above.

**What changed:**
1. **`SiteHeader`'s desktop nav (`md:` and up) gained icons** (`Store`/`ShoppingBag`/`User`/`Package`/`LogIn`/`LogOut`/`UserPlus`, all `lucide-react`) next to each text label — Shop, Bag, account name, My orders, Log out, Log in, Register. The mobile `BottomNav` already used an icon+label pattern (`Home`/`Store`/`ShoppingBag`/`User`); the desktop text-only nav was the one place that didn't match it. Purely additive — no layout/behavior change, verified at 1024px/1440px in both logged-in and logged-out states.
2. **A quantity stepper (`-`/count/`+`) added to the product detail page**, next to "Add to bag" in both its placements (desktop inline row, mobile sticky buy bar) — previously `ProductPurchasePanel` always added exactly 1 unit regardless of what the shopper wanted. Clamped to the selected variant's `availableQuantity` (same clamping `CartLineItem`'s existing stepper already does), resets to 1 on color/size change and after a successful add. `useCart().addItem` already accepted a `quantity` param — this was a UI gap, not an API gap.

**Why:** both were user-requested UI gaps found by using the redesigned pages, not planned scope from the design doc.

**Verified:** typecheck/lint clean; live click-through (increment → add to bag → cart shows the correct merged quantity) at 1024px and 375px.

**Follow-ups / known gaps:** none.

---

## 2026-08-26 — Mobile responsiveness fixes: sticky-bar/BottomNav gap, category-row overflow bug

**Branch/commit:** `dev1` — uncommitted, same pending styling work as the two entries above.

**What changed:** the user reported gaps/clipping on real mobile devices (screenshot) that hadn't shown up in `chrome-devtools-mcp`'s viewport emulation. Two real bugs found and fixed, plus a full page-by-page mobile re-sweep confirming nothing else was wrong.

1. **A visible gap between the PDP's mobile sticky buy bar and `BottomNav`, on real devices only.** `ProductPurchasePanel`'s buy bar was pinned with a hardcoded `bottom-20` (5rem), a guess at `BottomNav`'s height that didn't actually match it (measured: nav ≈65.5px, guess assumed 80px) — leaving an ~15px sliver of exposed page background between the two fixed bars, worse again on notched/home-indicator devices where `BottomNav`'s `env(safe-area-inset-bottom)` padding grows the nav taller still (that padding was already silently doing nothing in emulation, and everywhere else, because the app never set `viewport-fit=cover`, without which `env(safe-area-inset-*)` always resolves to `0`). Fixed properly rather than re-guessing a bigger magic number:
   - Added `apps/web/app/layout.tsx`'s `export const viewport = { viewportFit: "cover", ... }` so the safe-area env vars actually resolve on real devices.
   - Added a single shared source of truth, `apps/web/src/lib/layout-constants.ts` (`MOBILE_BOTTOM_NAV_HEIGHT_REM`, `ABOVE_MOBILE_BOTTOM_NAV_STYLE`), so the two components can never drift apart again. `BottomNav` now sets its own height to `calc(4.25rem + env(safe-area-inset-bottom))` (border-box, with the safe-area amount carved back out as padding so its visible content area stays a constant height); the PDP buy bar now offsets `bottom: calc(4.25rem + env(safe-area-inset-bottom))` from the same constant — the two always sit flush regardless of device safe-area.
2. **Homepage "Shop by category" row clipped its first tile on mobile, unrecoverably.** The horizontally-scrollable tile row used `justify-center`; centering a flex row that overflows its container is a known CSS trap — the browser centers the overflow symmetrically on both sides, but `scrollLeft` can't go negative, so the portion of the first item pushed off the left edge is permanently unreachable by scrolling (not just off-screen — actually unrecoverable). Fixed with `justify-[safe_center]` (CSS Box Alignment's `safe` keyword), which centers only when content fits and falls back to start-alignment the moment it overflows — mobile now scrolls to reveal every tile from the true start; desktop (where the row doesn't overflow) still looks centered.

**Why:** both bugs were invisible in `chrome-devtools-mcp`'s viewport emulation — it doesn't emulate a nonzero `safe-area-inset-bottom`, and the category-row clipping is only ~15px, easy to miss without a real screenshot. Found by direct `getBoundingClientRect()` measurement of the fixed elements (not just eyeballing screenshots) once the user's real-device screenshot flagged something was off, then fixed at the root cause rather than nudging pixel values.

**Verified:** typecheck/lint clean. Re-swept every storefront page (home, PLP, PDP, cart, checkout, order-confirmation, my-orders, login, register, account) at 375px after the fix — confirmed zero horizontal document overflow anywhere, and confirmed (via precise bounding-rect math, not just a screenshot glance) that no in-flow page content sits behind either fixed bar on cart/checkout/PDP. Also re-confirmed the *previous* "not responsive in laptop view" report from earlier this session was a stale Next.js dev-server CSS cache after a long hot-reload session (fixed by clearing `apps/web/.next` and restarting) — not a code defect, no source changes were needed for that one.

**Follow-ups / known gaps:** none found. All of this session's UI-redesign work (this entry plus the three above it) is still uncommitted, pending the user's review.

---

## 2026-08-26 — Full startup + end-to-end browser verification: one real bug found and fixed (order-confirmation stuck on "Loading")

**Branch:** `dev2`, uncommitted.

**Context:** asked to start all three servers fresh in this session (new container/session, nothing was running) and walk the real app in a browser end-to-end, fixing anything broken along the way.

**Infra note for whoever continues this:** this session has no Docker at all (not just "Docker not running" — the `docker` CLI itself isn't installed), and the machine's native Postgres (`postgresql@15`)/Redis only listen on the stock ports 5432/6379 for an unrelated project. Stood up a second, dedicated native Postgres cluster on port 5433 (`initdb`'d at `~/.local/share/woobe-dev/pgdata`, started via `pg_ctl ... -o "-p 5433"`) and a second Redis on port 6380 (`redis-server --port 6380 --daemonize yes`, data dir `~/.local/share/woobe-dev/redis`) to match `.env`'s expected ports — created the `woobe` role + `woobe_dev`/`woobe_shadow` databases, ran `migrate:deploy` (4 migrations, clean) and `db:seed`. Neither instance is registered with `brew services` — they need to be started manually next session (commands above) since nothing in the repo persists this. Worth a `/run-skill-generator` pass so this doesn't have to be re-derived cold again.

**Bug found and fixed:** `apps/web/src/features/orders/components/OrderConfirmation.tsx` — every order (COD and Razorpay alike) got stuck forever on "Loading your order…" in dev, even though the backend created the order fine and the confirmation-fetch API calls succeeded (visible 200s in the network tab). Root cause: an `isMountedRef` guard —
```ts
const isMountedRef = useRef(true);
useEffect(() => () => { isMountedRef.current = false; }, []);
```
— only ever *sets it false* (in the cleanup) and never sets it back to `true`. `next.config.mjs` has `reactStrictMode: true`, so React's dev-mode intentional mount→cleanup→remount cycle runs that cleanup once before the "real" mount, permanently flipping the ref to `false` for the component's entire remaining lifetime. Every subsequent `setOrder(fresh)` in `refetch()` is guarded by `if (isMountedRef.current)` (line 49), so it silently no-ops forever — the fetch succeeds, the state update is just discarded. Fix: reset `isMountedRef.current = true` inside the effect body itself, not just at `useRef` init, so the second (real) mount re-arms it:
```ts
useEffect(() => {
  isMountedRef.current = true;
  return () => { isMountedRef.current = false; };
}, []);
```
Confirmed via repro: before the fix, both a guest COD checkout and a logged-in COD checkout hung on "Loading your order…" indefinitely despite the order existing server-side (had to load `/account/orders` to see it actually went through). After the fix, both COD flows show "Order confirmed!" immediately, and the Razorpay "Pay online" flow correctly reaches "Order placed" / pending-payment (grep'd the whole web+admin tree for the same `isMountedRef` shape first — this was the only occurrence).

**Verified, end-to-end in a real browser (chrome-devtools-mcp, isolated contexts to avoid stale-cookie pollution from a prior session's leftover `cart_id` cookies against the freshly-seeded DB):**
- Home, PLP category links, PDP (variant/size selection, live price) — clean, no console errors beyond a benign missing `/favicon.ico` (404) and an expected silent-refresh 401 for guests with no session.
- Cart: add/increase/decrease quantity, live weight/price/shipping-fee recalculation, minimum-order-weight gate (checkout link disabled under 1kg, enabled + ₹50 shipping fee above it, correct free-delivery countdown) — all correct (ADR-021).
- Guest checkout → COD → order-confirmation → auto-confirms → "Order confirmed!" with GST-inclusive total.
- Register → auto-login → account page → "My orders" (empty, correctly not showing the guest order) → placed a second, logged-in COD order → shows up correctly in order history with the right status/total.
- Logout → login with the same credentials → session restored correctly.
- Razorpay "Pay online" path: order created in `PENDING_PAYMENT`, "Pay now" click correctly surfaces a "Something went wrong" toast and switches to "Try payment again" rather than crashing — expected, since `.env`'s Razorpay keys are still the Day-4 stub placeholders (`rzp_test_stub_replace_before_day5`), not real test credentials. Not a code defect; noted `error-handler.ts`'s `console.error` logs `"Unknown error"` with no stack for this case because the Razorpay SDK rejects with a plain object, not an `Error` — harmless today (correctly surfaces as a generic 500 to the client either way) but worth improving if Razorpay error debugging comes up for real.
- `apps/admin` boots clean on :3001 but is still exactly the Week-1 placeholder homepage ("Basic order view lands Week 1 Day 5...") — no actual order-view route exists yet despite the Day 5 commit message; confirmed by reading `apps/admin/app/page.tsx` and finding no other routes. Not a bug, just an honest gap between the commit message and what's actually built — flagging so it isn't assumed done.

**Verified (non-UI):** `apps/web` typecheck and `eslint --max-warnings=0` on the fixed file both clean post-fix. All three dev server logs (`api`, `web`, `admin`) scanned for the full session — zero unexpected errors/exceptions outside the one deliberately-provoked Razorpay-stub 500.

**Follow-ups:** real Razorpay test keys needed to exercise the actual payment-widget path; admin order view is still unbuilt; consider a `/run-skill-generator` pass to capture the Docker-less Postgres/Redis bootstrap as a reusable project skill.

---

## 2026-08-27 — HANDOFF: Admin order-management feature — all 20 plan tasks complete, final whole-branch review is the only thing left

**Branch:** `dev2`, all work below is committed (nothing uncommitted from this effort). If you're picking this up cold, start by reading the two documents below — they are the actual spec and plan, this journal entry is just a pointer + status snapshot.

**Read these first, in order:**
1. `docs/superpowers/specs/2026-08-26-admin-order-view-design.md` — the approved design spec (what's being built and why, including several deliberate architecture decisions: refund-on-cancel, the `audit`/`refunds` module split to avoid a circular dependency, the separate stricter admin session cookie).
2. `docs/superpowers/plans/2026-08-26-admin-order-view.md` — the 20-task implementation plan, executed via Claude Code's subagent-driven-development skill (fresh implementer subagent per task, independent reviewer subagent per task, fix loop on findings). Every task in it is done.

**Status: all 20 tasks implemented, reviewed, and merged.** Commits, in order (all on `dev2`, starting right after `c87165b` "feat(ui): update layout, navigation and product components"):

```
92bc358  feat(database): add order fulfillment fields and admin audit log table         [Task 1]
604a58a  feat(api): add leaf audit module for admin activity logging                    [Task 2]
4ecb783  feat(payments): expose narrow refund-lifecycle seams for the refunds module    [Task 3]
b81d70d  feat(refunds): build out admin-cancellation refund path (Week 4, pulled fwd)   [Task 4]
90e6180  fix(refunds): don't shadow a completed refund with a duplicate FAILED row      [Task 4, fix round 1]
367ddb8  feat(orders): extend entity/repository for fulfillment fields + admin listing  [Task 5]
b699970  feat(orders): wire inventory-release, refund-issuer, and audit-logger ports    [Task 6]
d71db30  feat(orders): add start-processing, ship, and deliver use-cases                [Task 7]
31eeb7a  feat(orders): add CancelOrderUseCase with refund-on-cancel                     [Task 8]
b963d30  feat(orders): export admin order-management use-cases                         [Task 9]
fe3d842  feat(validation): add admin order-action schemas                              [Task 10]
acfe243  feat(auth): export use-case singletons; add admin refresh cookie              [Task 11]
8c691f3  feat(admin): add staff auth controller and routes                             [Task 12]
a78f7d8  feat(admin): add order-management routes and composition root                 [Task 13]
af488bf  feat(database): seed order-processing and product-management staff accounts   [Task 14]
c2c2d75  test(admin): integration tests — auth, RBAC, order lifecycle, cancel-refund    [Task 15]
ab5e092  feat(admin): staff login (API client, session hook, login page)               [Task 16]
683c55c  feat(admin): dashboard shell (sidebar, top bar, session guard, nav config)     [Task 17]
1092161  feat(admin): order-management API client and data hooks                       [Task 18]
f02f0cf  feat(admin): orders list page with status filter and search                   [Task 19]
aa2a268  (Task 20's files — see note below)                                            [Task 20]
4ce5b30  fix(admin,api): handle set-cookie typing and order-fetch rejection             [post-Task-20 fix]
```

**What got built, end to end:** staff login (`apps/admin`, separate `admin_refresh_token` cookie, `sameSite: strict`, distinct from the storefront's customer cookie); an admin order list with status filter + search; an order detail page with a live status timeline; full order-lifecycle actions gated by RBAC (`CONFIRMED → PROCESSING → SHIPPED → DELIVERED`, tracking number/carrier captured on ship); and — the one piece of real correctness work pulled forward from Week 4 — cancelling a paid order now actually attempts a Razorpay refund (`refunds` module, new this effort) rather than just releasing inventory, with every transition writing an `AdminAuditLog` row. Seeded demo accounts: `orders@woobe.in` (order-processing staff), `catalog@woobe.in` (product-management staff), both password `Staff@12345`; existing `admin@woobe.in` / `Admin@12345` is super-admin.

**One git-state wrinkle worth knowing about, not a code problem:** Task 20's implementer hit a session-limit error after writing its code but before its own commit. Before a fresh agent could pick it up, an unrelated external commit (`aa2a268`, made outside this process while cleaning up other pending changes) swept in Task 20's already-correct files alongside some unrelated bystander changes (a storefront fix, this journal file, `project_planning/plan.md`, an eslint-plugin addition). A second agent verified byte-for-byte that Task 20's actual files were untouched and correct, then ran the full manual verification and found two small, real bugs in **earlier** tasks' code (not Task 20's) — a `set-cookie` header type mismatch failing `apps/api`'s typecheck (Task 15's integration test file), and a missing `catch` causing an unhandled promise rejection in `useAdminOrder.ts` (Task 18). Both were fixed and re-verified in `4ce5b30`. Everything is accounted for; nothing was lost.

**Full session ledger** (if the machine/session that ran this is still around): `.superpowers/sdd/2026-08-26-admin-order-view/progress.md` — this is git-ignored scratch, so it does NOT travel with a fresh clone. It has the complete blow-by-blow (every task's dispatch, every reviewer's findings, every ruling made along the way) if you have access to that filesystem. If not, this journal entry plus the plan/spec docs above are self-contained enough to continue without it.

**What's left — exactly one step:** the plan's final whole-branch review (dispatched on the most capable available model, since it's judging the whole feature holistically rather than one task at a time), covering the full diff from `c87165b` to `4ce5b30`. If you're continuing this with a fresh Claude Code session, the simplest instruction to give it is:

> Read `docs/superpowers/plans/2026-08-26-admin-order-view.md` and this journal entry (2026-08-27, "HANDOFF"). All 20 tasks are done and committed. Using the `superpowers:subagent-driven-development` skill's "Final Review" step, dispatch a whole-branch code reviewer (per `superpowers:requesting-code-review`) over the diff from `c87165b` to `4ce5b30` on `dev2`. If it comes back clean, use `superpowers:finishing-a-development-branch` to wrap up. If it finds issues, fix them in one batched dispatch (not one fixer per finding) and do one scoped re-review before finishing.

**Known, deliberately-deferred items (not bugs, just scope boundaries — see the spec for the reasoning):**
- The customer-initiated return/exchange request flow (`Return` entity, its own UI) is explicitly Week 4 scope — not built here. Only the narrower admin-cancellation refund path was pulled forward.
- `packages/database/prisma/seed.ts`'s `PricingSetting`/`ShippingRule`/`GstSlab` blocks use `.create` not `.upsert` (pre-existing, Week-1 code, unrelated to this effort) — re-running `db:seed` against a non-empty DB will accumulate duplicate rows there. Harmless today (current read paths tolerate duplicates) but worth a small follow-up fix at some point.
- The "refund needs manual follow-up" message on a cancelled order is transient UI state — it only shows in the same browser session right after clicking Cancel, and won't reappear on a page reload. If that turns out to matter in practice, it'd need `refundIssued` (or a real refund-status field) added to the order-detail API response.
- Real Razorpay test-mode keys are still not configured in `.env` (stub values) — the refund path has only ever been exercised against the honest "gateway unreachable" branch, never a real successful refund. Worth testing for real once keys are available.
- `apps/admin` has zero automated frontend tests (matches `apps/web`'s current state too — a pre-existing gap in this codebase, not introduced here).

---

## 2026-08-27 — Independent review (Days 1–5 + admin effort) + Week 2 Day 0 Part A: inventory-restock bug fixed

**Branch:** `dev1` — uncommitted (per this session's instruction: only commit/push when explicitly asked).

**Independent review first.** Before touching any code, ran a from-scratch verification of everything the journal claims — new local Postgres (`:5433`)/Redis (`:6380`) stood up in a clean session, real `typecheck`/`lint`/`boundaries:check`/`test`/`build`, and a live `chrome-devtools-mcp` walkthrough of both the storefront (guest COD checkout) and, for the first time, the admin app (login → orders list → order detail → status transition → cancel-with-refund) — the admin HANDOFF entry above never had one. Full findings in `review.md` at the repo root. Two real issues found:

1. **High: cancelling a `CONFIRMED`/`PROCESSING` order didn't restock inventory.** `CancelOrderUseCase` released stock via inventory's `releaseReservationUseCase` — correct for `PENDING_PAYMENT → PAYMENT_FAILED` (a hold that was never sold), wrong here, since `finalizeReservation` already ran by the time an order is `CONFIRMED` (`quantityReserved` is already 0). The "release" found nothing to release and silently no-opped; `quantityAvailable` never went back up. Reproduced live: placed a real order, watched `quantityAvailable` drop `20 → 18`, cancelled it via the admin UI, and it stayed at `18`. Traced to the design spec itself (`docs/superpowers/specs/2026-08-26-admin-order-view-design.md`), which explicitly specified wiring the new port to the reservation-release use-case — a genuine spec-level mix-up between "release a hold" and "restock a finalized sale," not just an implementer slip. The one existing test whose *name* claimed to cover this ("releases inventory") never actually re-queried inventory after cancelling.
2. **High: `apps/web`'s production build silently depends on a live API server at build time**, and CI has never once run (`GET /repos/.../actions/runs` → `total_count: 0`) — so this was never caught. Not fixed yet, see Day 0 Part B below.

`review.md` fed directly into a revised `project_planning/week2 (1).md` (§34–40, "Mandatory Pre-Week-2 Remediation") that the user had prepared before this session — both issues above are its Day 0 blockers, confirming the review's findings were taken as-is rather than needing re-litigation.

**What changed (Part A — the inventory fix):**
- **`inventory` module**: new `restockFinalizedSale(items, tx)` on `InventoryRepositoryPort`/`InventoryRepository` — increments `quantityAvailable` only (no `quantityReserved` bound, unlike `releaseReservation`), row-locks first via the same `lockRowsForVariants` helper the other three operations share. New `RestockFinalizedSaleUseCase`, exported from `inventory.module.ts` alongside (not replacing) `releaseReservationUseCase`, which `payments` still correctly uses for its own `PAYMENT_FAILED` path.
- **`orders` module**: `InventoryReleasePort` renamed/replaced with `InventoryRestockPort` (`{ restock(items, tx) }`) — the old name was itself part of how this slipped past review, since "release" reads as correct for both the hold-release and the sale-restock case when it isn't. `CancelOrderUseCase` now depends on this port instead. `orders.module.ts` wires it to the new `restockFinalizedSaleUseCase`. Idempotency needs no new guard — it rides the same `changed: false` conditional-transition check that already made the old code idempotent (a second cancel of an already-`CANCELLED` order never reaches the restock call).
- **Tests**: unit test (`cancel-order.use-case.test.ts`) updated for the renamed port. `admin.integration.test.ts`'s COD-cancel test now actually re-queries `inventory` **after** cancelling (the exact assertion its old name promised but never made) and asserts `quantityAvailable` returns to the pre-sale level; the Razorpay-cancel test gained the same check. New test: cancelling an already-`CANCELLED` order twice restocks exactly once, not twice (`quantityAvailable` stays put on the second call, and only one `ORDER_CANCELLED` audit row exists).

**Verified, not just written:**
- Full suite green: **70/70** (`apps/api`, up from 69 — one new idempotency test), typecheck/lint/boundaries clean across all 9 projects (222 modules / 558 deps).
- **Live, against the running dev API, independent of the test suite**: placed a real 17-unit COD order for Silk Scarf/Blush via curl (`quantityAvailable` 40→23 on confirm), cancelled it via the real admin endpoint as `orders@woobe.in` (`quantityAvailable` 23→**40**, restocked correctly), then cancelled the same order a second time and confirmed it stayed at **40** (not 57) — idempotency holds outside the test suite's mocked/transactional context too.

**Why:** Week 2's plan (§34.1) marks this a Day 0 blocker — building returns/exchanges or any further inventory-dependent Week 2 work on top of a silently-broken restock path would only compound the problem.

**Follow-ups / known gaps:** none new. Day 0 Part B (web build/CI — §35 of the plan) and Part C (bootstrap script — §36) are next, before Day 1 feature work starts.

---

## 2026-08-27 — Week 2 Day 0 Parts B + C: homepage rendering fix, fresh-environment bootstrap — Day 0 complete

**Branch:** `dev1` — uncommitted (per this session's instruction: only commit/push when explicitly asked; see "What's still open" below).

**Part B — `apps/web`'s build-time API dependency (plan §35).** Root cause confirmed exactly as `review.md` described: the homepage (`app/(storefront)/page.tsx`) is a plain async Server Component with no rendering-mode export, so `next build` tries to statically generate it — which needs a live `apps/api` reachable *during the build itself*. Chose **Option A (dynamic rendering)** over the plan's other two options and recorded it as **ADR-026** (`project_planning/plan.md`): a fallback/error-handling approach would let the build succeed while silently shipping frozen product/price data, which directly conflicts with `DEVELOPMENT_RULES.md` #1 (price/stock must be live, never stale) — the same rule already governing why cart/checkout never trust a cached price applies to the homepage that's a customer's first look at one. CI-starts-the-API would fix CI but not the identical failure for any other developer's local `pnpm build`. Fix: `export const dynamic = "force-dynamic"` on the homepage, matching `/products` (which already renders dynamically, because it reads `searchParams`).

**Verified, not assumed:** killed the running `apps/api` dev server entirely, `rm -rf apps/web/.next`, ran `pnpm --filter @woobe/web run build` — succeeds with zero live services running (`/` now shows `ƒ Dynamic` in the route table, not `○ Static`). Then ran the full `pnpm run build` (all three apps) in the same zero-services state — clean. This is the actual CI-equivalent condition the plan's own "CI acceptance" checklist calls for.

**Part C — fresh-environment bootstrap (plan §36).** Three real gaps closed, all previously tribal-knowledge-only (this is the third time the journal records rediscovering the stale-Prisma-client half of this; see Day 3, the Day 4 correction entry, and this session's own review):
1. **`postinstall: "pnpm --filter @woobe/database run generate"`** added to root `package.json` — every `pnpm install` now regenerates the Prisma client automatically, closing the exact gap that broke `apps/api` typecheck at the start of this session (and twice before it, per the journal).
2. **`apps/api/src/config/env.ts`** now loads the monorepo root `.env` itself, via Node's built-in `process.loadEnvFile` (stable since Node 20.6, this repo already requires Node ≥22 — no new dependency). Previously `apps/api` had no `.env` of its own and no loading mechanism at all; every session (including this one) had to discover by trial and error that it expects `.env`'s values already exported into the shell. Verified empirically (not assumed from docs) that an already-set `process.env` value always wins over the file's — confirmed both directly (`node -e` test) and by re-running the full 70-test suite unchanged (`vitest.config.ts`'s injected `woobe_test` env is untouched by this).
3. **`scripts/with-root-env.mjs`** — the same loader, applied to `db:migrate`/`db:migrate:deploy`/`db:seed`/`db:studio` (the Prisma CLI has no env-loading of its own; `db:generate` doesn't need it, confirmed it runs fine with no `DATABASE_URL` set at all). **`scripts/bootstrap.mjs`** (`pnpm run bootstrap`) ties it together — idempotent, creates `.env` from `.env.example` if missing, generates the Prisma client, checks Postgres/Redis reachability with actionable next-steps (Docker vs. native — deliberately doesn't try to auto-start either, since this repo has genuinely been run both ways on different machines and guessing wrong would fight an existing setup), then applies migrations. Does **not** auto-seed (plan §36: "seed when explicitly requested").
4. **`Readme.md`** (previously a placeholder, literally the text "abc") rewritten with the actual fresh-environment path, env-var loading behavior, migration/seed commands, and repo layout.

**Verified, not just written:**
- Ran `pnpm run bootstrap` for real against this session's Postgres/Redis — first run correctly failed at the migrate step (caught its own gap: the Prisma CLI wrapper didn't exist yet), fixed, re-ran clean end to end.
- Simulated the CI condition directly: moved `.env` out of the way entirely, ran the migrate-deploy wrapper with only manually-exported env vars (exactly what `.github/workflows/ci.yml` does) — succeeds identically, confirming the loader's "already-set env wins" behavior doesn't regress CI.
- Started `apps/api`'s dev server from a **genuinely fresh shell with zero manually-exported variables** (this session's Bash tool doesn't persist shell state between calls, so this wasn't simulated) — `/health` responded correctly on the first try.
- Full suite re-run clean after every change in this entry: typecheck (9/9), lint (9/9, zero warnings), `boundaries:check` (222 modules / 559 deps), `test` (70/70), `build` (all three apps, zero live services running).

**Why:** plan §38 marks Day 0 (Parts A+B+C) as the gate before any Week 2 feature work starts — "Only then start feature development."

**Day 0 acceptance, per plan §38:**
```
Inventory cancellation     PASS  (previous entry — restock verified live + regression-tested)
Web clean build            PASS  (this entry — verified with zero live services running)
CI actually executed       NOT DONE — needs a push/PR; this session only commits/pushes when explicitly asked (see below)
Bootstrap documented       PASS  (this entry)
Week 1 regression          PASS  (70/70 tests, full build, throughout both entries)
```

**What's still open, deliberately not done without being asked:** plan §35's "CI acceptance" checklist also asks to push to a branch/PR and confirm GitHub Actions actually executes (it has literally never run once — `review.md`). Everything provable **without** pushing has been proven (identical CI-equivalent conditions reproduced locally, twice, per above). Actually triggering GitHub Actions needs a push, which this session doesn't do unless asked. All Day 0 code changes are complete, verified, and sitting uncommitted in the working tree, ready for that decision.

---

## 2026-08-27 — Week 2 Day 1: Catalogue Discovery (search, filters, sort, collections listing) — ADR-012

**Branch:** `feature/catalog-search`, off `dev1`'s tip (`d8c4429`). Commits: `fe93f5d` (API), `a902c51` (web UI), `5b99bc1` (two real bugs found live, fixed). Not merged, not pushed — per this task's instructions, the orchestrating session reviews/merges.

**Scope:** exactly `plan.md` §3 (Module 1) + the Day 1 bullets in §27/§38 — search, category/collection/size/color/in-stock/price filtering, sort, pagination, clear/reset, result counts, empty states. Collections got listing-only (`GET /api/v1/collections`), not detail pages/admin CRUD (Day 2, §4) — the `collections` module composition root was a Week 1 placeholder (`// Built out: Week 2+`) that I built out to exactly that boundary.

**API (`apps/api`):**
- `GET /api/v1/products` extended: `q` (search), `collection`, `size`, `color` (comma-separated, multi-value), `inStock`, `minPrice`/`maxPrice` (paise), `sort` (`price_asc`|`price_desc`|`newest`), plus the existing `category`/`page`/`limit`. All server-side, combinable, bounded (limit still capped at 50).
- **Search**: re-read ADR-012 carefully before choosing an approach — it wasn't "decide if pg_trgm is warranted," it already decided pg_trgm is part of the **launch** setup (the OpenSearch/Meilisearch trigger — 50k products, p95>300ms, typo-tolerant UX — is a separate, higher bar this catalogue is nowhere near). The schema even had a dangling `// TODO (Day 3, ADR-012): pg_trgm GIN index... once the catalogue-search query path is actually built` comment from Week 1 pointing at exactly this day. Added migration `20260827092111_add_catalogue_search_indexes`: `CREATE EXTENSION pg_trgm` + a GIN trigram index on `products.name`, plus standalone B-tree indexes on `minPricePaiseCache` and `createdAt` (ADR-012's own explicit list, now actually load-bearing for the price-only-sort and newest-sort paths this day adds). The query itself stays a normal Prisma `name: { contains, mode: "insensitive" }` — Postgres' planner uses the trigram index to accelerate that ILIKE shape, so there's no raw SQL in the query path and it composes cleanly with every other filter/sort/pagination Prisma builds in one query. Verified live: `q=urora` (mid-string, not prefix) matches; `q=SCARF` case-insensitive matches; a typo'd substring correctly finds nothing (this is substring-accelerated ILIKE, not typo-tolerant fuzzy ranking — that's the OpenSearch-tier feature explicitly deferred).
- **Availability filter**: had to cross a real module boundary question. `inventory` owns the `Inventory` table (ADR-010); `products`' own repository can't touch it directly, and "in stock" has to be a *live* read (DEVELOPMENT_RULES.md #1), not a cached column. Added `InventoryReaderPort.findInStockVariantIds()` (new use-case `FindInStockVariantIdsUseCase`, one `groupBy` query, catalogue-wide) — `ListProductsUseCase` resolves it once per request and passes the resulting id set into the products query as an `id IN (...)` variant filter; if the set is empty it short-circuits to an empty page without a second query. Verified live, not just by reading the code: zeroed out both of Silk Scarf's variant rows in the real dev DB mid-session, confirmed `inStock=true` correctly excluded it (10→9 total), restored the rows, confirmed it reappeared. Also added an automated integration test that does the same live-flip inside the test itself.
- **Size/color**: independent facets — `size=M&color=Red` matches a product that has *some* M variant and *some* Red variant, not necessarily the same row. Deliberately not the stricter "one variant matches both" reading; documented in `ListProductsUseCase` and proved with a fixture product whose Red and M attributes are on two different variants (the naive "AND on one variant" interpretation would have wrongly excluded it — first draft of that test actually had the wrong expected count until I traced through why, see the products.integration.test.ts fixture comments).
- **Price filtering** — the plan's own bullet list said "where approved" without pointing at an explicit approval anywhere I could find in `plan.md`. The concrete signal I did find: ADR-012 explicitly calls out a `(category_id, price)` composite index for "the common category + price range filter path" — the architecture already assumed this filter would exist. Built it against `minPricePaiseCache` (paise, inclusive bounds), same cache Week 1 already uses for listing sort, not a new live-recompute path — this is a listing/discovery filter, not a checkout figure, so ADR-012's own "display/sort cache is fine for listing" reasoning applies unchanged. Flagging this as a narrowly-scoped interpretation call, not a rubber-stamped requirement.
- **Sort/pagination**: added an `id` tiebreaker to every sort branch (`price_asc`/`price_desc`/`newest`) — Week 1's original `orderBy: { minPricePaiseCache: "asc" }` had no secondary key, so two products tied on price had no guaranteed order between pages, which fails "stable ordering." Kept offset pagination (`page`/`limit`, capped at 50) rather than moving to cursor-based — nowhere near ADR-012's own scaling trigger, and the plan's "infinite loading" bullet is satisfiable as a UI-only "load more" layered on top (see web section).
- **Collections module**: built out from the Week 1 placeholder — `domain/application/infrastructure/interface` shape mirroring `categories` exactly, `GET /api/v1/collections` (listing only), plus a `FindCollectionBySlugUseCase` exported for `products` to resolve `?collection=<slug>` the same way it already resolves `?category=<slug>` (ADR-010: `Collection`/`ProductCollection` stay owned by `collections`, `products`' repository never touches them directly).
- Tests: 9 new unit tests (`ListProductsUseCase`, mocked ports — category/collection-not-found, in-stock short-circuit, filter/sort pass-through) + 29 new integration tests against the real `woobe_test` Postgres (search, every individual filter, combined filters, all three sorts, pagination stability/no-overlap/past-the-end, empty results, invalid parameters, the live in-stock flip, plus `GET /api/v1/categories`/`GET /api/v1/collections`). Full suite: **108/108** (was 70/70 at session start). Fixtures live in their own throwaway category/collection with a per-run random suffix, cleaned up via cascade deletes in `afterAll` — verified the `woobe_test` product/category/collection counts are back to exactly the seeded baseline (10/5/2) after the run, not just that the tests passed.

**Frontend (`apps/web`):** `/products` extended in place — `SearchBar`, `CollectionFilter` (new, mirrors `CategoryFilter`'s plain-link pattern), `FiltersPanel` (new: sort, size pills, in-stock toggle, color text input, price range, clear), `ProductResults` (new: result count, empty-state messaging, "Load more"). One shared `buildProductsHref()` helper so every filter control preserves every other active filter and always drops `page` on change. No new `packages/ui` primitives invented — reused `Button`/`Input`/`Label`, plain `<select>`/`<fieldset>` for the two things that needed real semantics `packages/ui` doesn't have yet.

**Deliberate scope calls (flagging prominently, not burying):**
1. **Mobile filter UI is a plain inline disclosure, not the design plan's literal bottom sheet** (`woobe_ui_design_plan.md` §9). `packages/ui` has no Modal/Sheet primitive yet — its own `index.ts` says Dialog/Select "arrive when a real feature needs that specific behavior." Building one from scratch wasn't in this day's scope. Same filtering behavior, different chrome; an easy swap later.
2. **Color filter is free-text, exact-match — not a curated swatch/pill list.** `ProductVariant.color` is a freeform string, not an enum (seed data alone has 15 distinct values: Rose, Sage, Ivory, Terracotta, Indigo, Blush, Charcoal, Mustard, Teal, Dusty Rose, White, Olive, Natural, Gold, Silver). There's no facet-values endpoint in Day 1's scope to source real options from, and hardcoding a palette risked silently not matching whatever the actual catalogue uses. Size got a curated pill list instead (S/M/L/XL/XXL/One Size) because apparel sizing genuinely is a small, standard set.
3. **"Load more" is layered on the existing offset pagination**, not a cursor-based rework — justified above.

**Bugs found and fixed, live in the browser (chrome-devtools-mcp against this worktree's own dev servers on ports 3100/4100, `WEB_ORIGIN` overridden so CORS wasn't a false signal):**
1. **Stale search box after "Clear filters."** `SearchBar` held local input state seeded once at mount; clearing filters updated the URL but the visible text stayed. First fix attempt (`key`-remount) was *worse*, verified in the DOM — `document.querySelectorAll('input[type=search]')` showed **two** `#product-search` inputs simultaneously after the click, one stale, reproducible across multiple checks and a hard reload, not a transient render artifact. Root-caused enough to know it was real, then abandoned that approach rather than chase the exact React/RSC reconciliation mechanics — switched to a `useEffect` re-sync on `currentParams.q`, verified clean (exactly one input, correct value) afterward. `ProductResults`, which also uses a `key` remount (intentionally, to drop accumulated "load more" pages on filter change), was separately checked for the same symptom and does *not* reproduce it — noting the asymmetry in case it matters for whoever touches this next, but didn't chase why.
2. **Real a11y issue, not hypothetical** — Chrome DevTools live-flagged "No label associated with a form field" the first time the Filters panel was opened. Traced to the "Size" `<Label>` (from `packages/ui`, renders a `<label>`) grouping six toggle buttons with no single field to associate with. Fixed with `<fieldset>`/`<legend>`. Also added `aria-pressed` to the size/in-stock toggle buttons and `aria-current` to the active category/collection pills — neither exposed active state to assistive tech before; not required to reproduce a specific complaint, but cheap and correct while already in this code.

**Verified live, in order:** search (partial + case-insensitive match, empty-result term), size filter, color filter (single + comma multi-value), in-stock filter (including the live-flip), price range (rupee-to-paise conversion at the UI boundary), sort (all three, cross-checked against direct `curl` output), category+collection preservation across every other filter, clear-filters full reset, empty state message, result count text, PDP navigation still working, homepage still working — all at 375/768/1024/1440px via `resize_page` + screenshots, both via `curl` against the raw API (port 4100) and by clicking through the actual rendered page (port 3100). "Load more" itself: the seed catalogue (10 products) never exceeds the 24-per-page limit, so the button never renders naturally — verified the exact code path instead via a direct cross-origin `fetch()` from the browser console to `GET /api/v1/products?page=2&limit=3`, confirming CORS/pagination work end-to-end from a real browser context, plus the pagination integration tests and a code read of `ProductResults.loadMore()`. Flagging this as the one piece of the UI not clicked live, and why.

**Verification commands, all clean:** `pnpm run lint`, `pnpm run typecheck`, `pnpm run test` (108/108), `pnpm run build` (all three apps, zero live services running — `/products` still `ƒ Dynamic`, ADR-026 untouched), `pnpm run boundaries:check` (233 modules / 590 deps, zero violations).

**Follow-ups / known gaps:**
- Bottom-sheet mobile filter UI, curated color facets, and cursor pagination are all explicitly deferred, not forgotten (see "Deliberate scope calls" above) — revisit if/when their respective triggers are actually hit.
- `ProductResults`' `key`-remount vs. `SearchBar`'s bug: didn't root-cause why one reproduced the stale-node issue and the other didn't. If a future `key`-remounted client component under this same Server Component boundary shows a similar symptom, start here.
- Collection/category listing endpoints have no pagination of their own — fine at 5/2 rows today, would need it if collections/categories ever grow into the dozens.
- `minPricePaiseCache`/`effectivePricePaiseCache` are still only ever set by the seed script (Week 1 gap, not touched this session) — admin product CRUD (Week 2 Day 7) will need to actually recompute these on write, or the price filter/sort this day added will silently drift from real pricing once admin-created products exist.

---

## 2026-08-27 — Week 2 Day 1: independently re-verified and merged to dev1

**Branch/commit:** `feature/catalog-search` (built by a worktree-isolated subagent, entry above) merged `--no-ff` into `dev1`.

Per this session's own orchestration approach (subagent per day, isolated worktree, agent's own report never trusted blindly): re-ran the full verification suite myself, independently, before merging — `pnpm install` + `pnpm run bootstrap` in the agent's worktree, `typecheck`/`lint`/`boundaries:check`/`test`/`build` all clean (108/108 tests, matching the agent's own report exactly), migration confirmed additive-only both by reading it and by `check:migrations`. Spot-checked the actual implementation (not just its tests) — `ListProductsUseCase`/`ProductRepository` correctly follow ADR-010 layering, the price filter correctly uses the display-cache for listing (matching Week 1's own documented "cache is fine for listing, never for checkout" rule) rather than a DEVELOPMENT_RULES.md #1 violation. Live curl checks against a freshly-started API instance (search, combined size+inStock+sort filters, invalid-param rejection, the new collections endpoint) all matched the reported behavior exactly, including the price_desc ordering.

Merged clean, no conflicts (Day 0's inventory-restock work and Day 1's new `findInStockVariantIds` composed without collision — both touch `inventory.module.ts`/`inventory.repository.ts` but additively). Re-ran the full suite again on `dev1` post-merge, not just trusted that each side was clean independently: typecheck/lint/boundaries/108-tests/build (zero live services) all clean.

**Follow-ups / known gaps:** none new beyond what the agent's own entry above already flagged.

---

## 2026-08-27 — Week 2 Day 2, attempt 1: FAILED — subagent hit its own usage limit mid-exploration, nothing built

**Branch:** `feature/collections-wishlist` — deleted after this entry, nothing on it worth keeping (see below).

Dispatched the same way as Day 1 (worktree-isolated subagent, scope = `project_planning/week2 (1).md` `# 4` Collections & Merchandising + `# 5` Wishlist). The agent terminated early: `"Agent terminated early due to an API error: You've hit your session limit · resets 6:30pm (Asia/Calcutta)"`. This is the subagent's own usage limit, not this orchestrating session's.

**Checked before deleting anything, not assumed:** the agent's worktree had **zero commits** beyond the inherited base (`7c6fc0a`) and **zero uncommitted/untracked changes** — `git status --short`, `git diff --stat`, and `git status --porcelain --untracked-files=all` were all empty. Its last logged action was still read-only exploration ("check `useCart.tsx`, `lib/api-client.ts`, `SiteHeader.tsx`, `BottomNav.tsx`, `ProductCard.tsx`, and the app router directory structure") — it failed before writing a single line of implementation. Nothing was lost; there was nothing to lose. Worktree removed, empty branch deleted.

**What this means for the "reduce the session limit via subagents" approach:** it doesn't straightforwardly work the way it sounds — a subagent has its own usage budget, separate from (but apparently drawn from the same account-level pool as) the orchestrating session's, and a large, exploration-heavy task can burn through it before producing anything, as happened here. This is worth weighing before dispatching the next attempt at Day 2: retry as another subagent once the limit resets (6:30pm IST), or have the orchestrating session do Day 2's work directly instead.

**Also noted this session, unrelated to the failure:** `git reflog show origin/dev1` shows the remote was pushed up through this session's latest local commit at 15:39, roughly 10 minutes after that commit was made locally — nothing in this session ever ran `git push`. Flagging plainly rather than silently: something in this environment appears to sync to GitHub automatically; it wasn't a deliberate push by this session, and none is planned going forward per this session's instruction to stop pushing.

**Process change starting with attempt 2 below, per user instruction:** this session no longer commits or pushes anything — journal.md gets updated as work happens, but git commits (including merging a finished feature branch into `dev1`) are left for the user to do themselves after their own review.

---

## 2026-08-27 — Week 2 Day 2, attempt 2: dispatched

**Branch:** `feature/collections-wishlist` (recreated), worktree-isolated subagent, same scope as attempt 1 (`project_planning/week2 (1).md` `# 4` Collections & Merchandising + `# 5` Wishlist) — not yet complete as of this entry.

---

## 2026-08-27 — Week 2 Day 2, attempt 2: Collections + Wishlist — built, tested, verified live

**Branch:** `feature/collections-wishlist`, off `dev1`'s tip (`4821eff`, the commit recording attempt 1's failure). Commits: `9944c17` (migration), `4d32503` (collections API), `d76d762` (wishlist API), `54e05ef` (web UI), plus this entry. Not merged, not pushed — per this task's instructions, the orchestrating session reviews/merges.

**First thing found, before any feature work:** this worktree's own `feature/collections-wishlist` branch had been cut from a stale point weeks before `dev1`'s actual tip — `git log` showed only 5 commits, `apps/api`/`apps/web`/etc. didn't exist in the checkout at all, only `project_planning/`. `dev1` itself was correctly at `4821eff` and fully visible from this worktree (git worktrees share refs), so this wasn't a missing-fetch problem — the branch pointer was just wrong. Fixed with `git reset --hard dev1` before touching anything else; flagging this because if it happens again to a future attempt, the fix is the same one-liner, not a bigger investigation.

**Scope:** exactly `project_planning/week2 (1).md` §4 (Module 2 — Collections & Merchandising) + §5 (Module 3 — Wishlist) + the Day 2 bullets in §27/§38, following `collections`/`cart`'s existing Clean Architecture shape (domain/application/infrastructure/interface, ADR-010).

**Collections (API):**
- Customer: `GET /api/v1/collections/:slug` (metadata detail — 404 for unknown or inactive). Deliberately did **not** duplicate a product-rail query here: `GET /api/v1/products?collection=<slug>` already exists from Day 1 with its own tested pagination/sort/availability handling, so the collection detail page's rail just calls that directly. Documented in `get-collection-detail.use-case.ts`.
- Admin (RBAC-gated, `MANAGE_CATALOG`): create/update/activate/deactivate, assign/remove products (idempotent — re-assigning or re-removing is a no-op, not an error), reorder products. Mounted as a thin gateway at `/api/v1/admin/collections` inside the `admin` module (ADR-025), exact same shape `AdminOrdersController` already established — collections' own composition root exports the use-cases, admin just wires a controller onto them.
- **Small additive migration**: `ProductCollection.sortOrder Int @default(0)` — the schema had no ordering column for products within a collection, and "reorder products" needs one. `check-destructive-migration.mjs` passes.
- **Real bug caught building this, not by luck**: `prisma migrate dev`'s auto-generated diff for the sortOrder migration also proposed `DROP INDEX "products_name_trgm_idx"` — the Week 2 Day 1 pg_trgm trigram index, created via raw SQL because Prisma's schema DSL has no first-class syntax for it, which means the diff engine has no record of it in schema.prisma and treats it as drift on every subsequent `migrate dev` run. Caught by reading the generated `migration.sql` before applying it rather than trusting the CLI's plan blindly. Hand-edited the file to drop only the `DROP INDEX` line, then had to unwind the damage: the flawed version had already run once against `woobe_dev` (dropping the real index), so fixed it by restoring the index directly (`CREATE INDEX ... USING GIN`), deleting the stale `_prisma_migrations` checksum row, and re-marking the corrected file applied via `prisma migrate resolve --applied`. Verified via `\di products_name_trgm_idx` on both `woobe_dev` and `woobe_test` that the index survived, and re-ran the full Day 1 test suite (108/108, including the search tests that exercise this exact index) to confirm nothing regressed. **Flagging this as a real, reusable finding**: any future migration touching `products` will hit the same `migrate dev` diff behavior against that raw-SQL index — always read the generated SQL before applying.
- Duplicate slug (`P2002`) and unknown-product-on-assign (`P2003`) both map to clean `409`/`404`s in the repository, never a raw 500 from the DB constraint — same pattern `auth`'s `createUserWithPassword` already established for duplicate email.
- 18 new integration tests against the real `woobe_test` Postgres (customer detail incl. inactive-hides-it, admin RBAC by role, CRUD, duplicate-slug 409, assign/remove idempotency + 404s, reorder including the two rejection cases — partial list and a foreign product id — and one check that the reordered rail is what `?collection=` actually returns).

**Deliberate scope call, flagged prominently:** apps/admin has **no UI** for collection management — the API is complete, RBAC-gated, and tested, but there's no admin-frontend page for it. `apps/admin` has no product-picker/list UI yet at all (that's Week 2 Day 7, Module 16 — Admin Product Management); building a one-off product-selection UI today, ahead of and disconnected from that day's real one, would be exactly the kind of untethered scope creep §32 (Scope Protection) warns against. Documented in `collections.module.ts`'s and `admin.module.ts`'s own doc comments so Day 7 knows where to plug in.

**Wishlist (API), built out from the Week 1 placeholder (`Router()`, no routes):**
- `GET /api/v1/wishlist` (view), `POST /items` (add), `DELETE /items/:itemId` (remove), `GET /state/:productId` (PDP's "is this saved" check), `POST /items/:itemId/move-to-cart`. Every route `authGuard`-mounted — no guest wishlist, per the schema (`Wishlist.userId` non-null/unique) and the task's own instruction.
- **Duplicate prevention**: `WishlistItem`'s own `@@unique([wishlistId, productId])` is the real guard, not reinvented in application code — the repository catches its `P2002` violation and throws a clean `ConflictError` (409), verified by a test that re-POSTs the same product and confirms both the 409 *and* that only one row exists afterward (not just that the second call failed).
- **Authorization** (tested explicitly, per the task's own instruction): every use-case resolves `wishlistId` from the caller's own `req.user.id`, never from a request param — there's no URL a customer could hand-edit to reach another wishlist. `removeItem`/`findItemById` are additionally scoped by *both* `itemId` and `wishlistId` in the same query (`deleteMany`/`findFirst` with both conditions), so another account's item id simply doesn't match rather than needing a separate ownership check. Three explicit tests: list stays empty, remove 404s (not silently succeeds), move-to-cart 404s.
- **Inactive/out-of-stock handling** (decided and documented, not left implicit): a wishlisted item whose product later goes inactive, or whose variant sells out, stays in the view — flagged via `isProductActive`/`isVariantActive`/`isAvailable`/`availableQuantity` rather than dropped or crashing the endpoint. A no-variant item (shopper saved "the product" without picking a size) reports `isAvailable` from product-activeness alone; stock isn't evaluated until a variant is chosen. Both cases have their own integration test.
- **Server-authoritative availability** reuses the exact `InventoryReaderPort`/`getAvailableQuantitiesUseCase` shape Day 1's `findInStockVariantIds` and cart both already use — no new inventory read path invented. `move-to-cart` re-checks live stock at click time even though the view's own `isAvailable` is necessarily a moment stale by then (another shopper could buy the last one in between) — two tests cover both failure modes (no variant chosen; live stock can't cover the requested quantity), confirming the wishlist item survives a failed conversion rather than being destructively removed anyway.
- **Cross-module wiring**: exported `GetProductsByIdsUseCase` from `products` (bulk lookup by id, deliberately *not* active-only-filtered — a deactivated product must still resolve, with `isActive: false`, not vanish) and exported `addItemUseCase` from `cart` (previously module-private) so move-to-cart adds through the identical path the cart page's own "Add to bag" uses, not a shortcut around it.
- **Interpretation flagged**: the plan's "Move/add to cart where approved" is ambiguous between a destructive move and a non-removing add. Built **move** only (adds to cart, then removes the wishlist item) — the more common wishlist convention, and nothing in the plan or its own test list (§5) distinguishes a separate add-without-removing action. Documented in `move-wishlist-item-to-cart.use-case.ts`; narrow scope call, not a rubber-stamped requirement.
- 16 new integration tests (add/remove incl. variant-mismatch and unknown-product 404s, duplicate-prevention 409, three authorization tests, two inactive/out-of-stock tests, three cart-conversion tests).

**Frontend (`apps/web`):**
- `/collections/[slug]` — real `generateMetadata` per collection (title/description/OpenGraph sourced from the collection's own name/description, not a static fallback), product rail via the same `listProducts({ collection })` Day 1 already built, "View all N items" linking through to `/products?collection=` for full filter/sort/pagination. Renders `ƒ Dynamic` in the build's route table (confirmed in the build output), consistent with ADR-026.
- `WishlistProvider` (`useWishlist`) mirrors `CartProvider`'s exact shape — every mutation refetches the server's whole view rather than merging a client-guessed state, same "server is the only source of truth" reasoning cart already established, extended here to availability/status flags, not just price. Membership check (`isSaved`) is derived from the one whole-list fetch already in memory, not a per-product API call, so a PLP grid of N products costs one request, not N (the dedicated `GET /wishlist/state/:productId` endpoint stays available server-side, just not called by this session's own frontend).
- `WishlistButton` (heart toggle) wired into `ProductCard` (PLP grid + collection rails) and `ProductDetail` (PDP). A guest click redirects to `/login` rather than showing a disabled control — saving requires an account, but the affordance itself should still be discoverable pre-login.
- `/wishlist` page: loading/logged-out/empty states, line items with remove + move-to-cart (or "Choose a size" linking to the PDP, for a no-variant item), unavailable-reason messaging matching the API's own flags.
- **Nav** (small, natural addition, not a redesign): added Wishlist as BottomNav's 5th tab (`Home/Shop/Wishlist/Bag/Account` — BottomNav's own Week 2 Day 1 comment had explicitly deferred this exact tab pending the feature existing) and to `SiteHeader`'s desktop nav, both with a live item-count badge sourced from the same provider.
- One deliberate scope call not in the task's explicit list: the PDP's heart always saves at the **product** level (no `variantId`), matching the PLP card's own behavior — `ProductDetail` is a Server Component and the selected-variant state lives in the client-only `ProductPurchasePanel` beneath it; threading that selection back up to drive the heart wasn't worth the prop-plumbing for this day's scope, and product-level saves are already a fully supported, tested wishlist shape (the "no size selected" / "Choose a size" path exercised live below).

**Verified live** (chrome-devtools-mcp, this worktree's own dev servers on ports 3000/4000, not asserted from code alone): registered a real account, clicked the PLP heart on Silk Scarf — badge and card heart updated in the same render, no reload; navigated to `/wishlist`, saw the item with "No size selected" / "Choose a size" (the no-variant path); opened the PDP, confirmed its heart independently reflected the saved state (context-driven, not a fresh fetch); clicked Remove, confirmed the empty state and badge disappearing. Checked `/collections/new-drops` and `/wishlist` at 375/768/1024/1440px — rail grid reflows correctly, desktop nav shows the new Wishlist link, mobile bottom nav shows all 5 tabs with the badge. Zero console errors at any point (`list_console_messages`, `types: ["error"]`, checked after each navigation). Separately confirmed via direct `curl` with a real staff login (`catalog@woobe.in`) that `/api/v1/admin/collections` returns data with a `MANAGE_CATALOG`-holding token. Cleaned up the live-test account from `woobe_dev` afterward (`DELETE FROM users WHERE email = 'wishlist-live-test@test.woobe.internal'`) so it doesn't linger as stray seed-adjacent data.

**Test counts:** 108 (Day 0/1 baseline) → **142** (108 + 18 collections + 16 wishlist).

**Verification commands, all clean, run for real:** `pnpm run lint`, `pnpm run typecheck`, `pnpm run test` (142/142), `pnpm run build` (all three apps, zero live services running — confirmed by killing both dev servers before the final build run), `pnpm run boundaries:check` (263 modules / 699 dependencies, zero violations), `node scripts/check-destructive-migration.mjs` (clean).

**Follow-ups / known gaps, not forgotten:**
- Admin UI for collection management (apps/admin) — deferred to Day 7, see above.
- PDP wishlist save is always product-level, never variant-level — see above.
- `GET /api/v1/wishlist/state/:productId` exists and is tested but isn't called by this session's own frontend (the full-list-derived `isSaved` covers both PLP and PDP more cheaply) — still real, tested API surface for any other client.
- Collection pills on `/products` (Day 1's `CollectionFilter`) still behave as filters, not links to the new `/collections/[slug]` detail pages — left untouched deliberately (existing, tested Day 1 UX; the task asked for wishlist nav wiring, not a collections navigation redesign).

---

## 2026-08-27 — Week 2 Day 2: independently re-verified (one real finding), not yet merged

**Branch:** `feature/collections-wishlist` (entry above) — re-verified in its own worktree by the orchestrating session. **Not merged into `dev1`** — per this session's current instruction, merging/committing is left for the user to do after their own review, so this entry documents verification only, not a merge.

Same discipline as Day 1 — the agent's report was checked, not trusted outright:
- **Migration**: read the migration file directly (matches the agent's own account of the raw-SQL-index gotcha), ran `check-destructive-migration.mjs` (clean), and independently confirmed via `psql \di` that `products_name_trgm_idx` genuinely exists on **both** `woobe_dev` and `woobe_test` post-fix, plus checked `_prisma_migrations` shows all 7 migrations `finished_at`-set with no `rolled_back_at` — the manual `migrate resolve` dance left the history in a clean, consistent state, not a stuck one.
- **Full suite, typecheck, lint, boundaries, build**: all re-run from scratch in the agent's worktree (`pnpm install` + `pnpm run bootstrap` first, then each command independently) — typecheck/lint/boundaries clean (263 modules / 699 deps), build clean with zero live services running.
- **Live**: registered a real account, added/duplicate-added (409, not 500)/viewed a wishlist item via direct API calls, confirmed unauthenticated wishlist access 401s, confirmed collection detail returns real data, confirmed admin collection creation works with a real `catalog@woobe.in` RBAC token — all matched the agent's report. Test data cleaned up afterward.

**One real finding: `pnpm run test` is flaky at the full-suite level, ~25% of the time in this session's sample (1 failure in 4 runs).** The failure isn't consistent — different tests failed each time it happened (an admin-login 403-instead-of-200, a "socket hang up," a wishlist unique-constraint collision) — and re-running the two new test files (`collections.integration.test.ts`/`wishlist.integration.test.ts`) **in isolation, repeatedly, passed 34/34 clean every time**, as did three consecutive full-suite reruns after the one failure. This points to cross-file interference under vitest's default parallel file execution against the one shared live `woobe_test` Postgres instance (likely contention over the same seeded singleton rows — `admin@woobe.in`/`orders@woobe.in`/`catalog@woobe.in`, or shared category/warehouse rows — now that there are 24 integration test files running concurrently instead of Day 1's 20), not a bug in Day 2's actual implementation. Not investigated further or fixed this session (root-causing vitest's exact concurrency/isolation behavior is its own piece of work, not part of verifying Day 2) — flagging clearly as a new, real gap: **the test suite itself needs either serialized integration-test execution or per-file-isolated fixtures before it can be trusted to fail only on real regressions as more days/modules are added.** Worth addressing soon given the trend (108 → 142 tests in two days), not urgent enough to block Day 2's own correctness.

**Follow-ups / known gaps:** the flaky-full-suite finding above, plus everything the agent's own entry already flagged (admin collection UI deferred to Day 7, PDP wishlist is product-level only, `/wishlist/state/:id` unused by the frontend, collection pills still behave as filters not links).

---

## 2026-08-28 — Week 2 status audit: journal + `week2 (1).md` reconciled, one journal defect fixed, Day 0–2 independently re-verified again (clean)

**Branch:** `dev1` @ `4005416` (clean, pushed) — one fix in this entry (`journal.md` itself) is the only file changed.

**Task:** re-read this entire journal end-to-end plus `project_planning/week2 (1).md` (the governing plan, including its §34–40 Day-0 remediation and revised execution order) and confirm nothing in the current `dev1` implementation is wrong before continuing to Day 3. Not a re-read of prior claims — a fresh, independent verification pass, same discipline as the Day 0/1/2 re-verification entries above.

**Where Week 2 actually stands right now, reconciled against `week2 (1).md` §38's revised execution order:**
- **Day 0** (§34–36, inventory-restock fix + web build/CI fix + bootstrap) — done, verified in the two 2026-08-27 entries above. Not re-litigated today beyond the standard full-suite/build re-run below.
- **Day 1** (Catalogue Discovery) — done, merged to `dev1`, independently re-verified once already (2026-08-27 entry above).
- **Day 2** (Collections + Wishlist) — done, merged to `dev1` (`4005416`), independently re-verified once already (2026-08-27 entry above) **and now again today** (see below).
- **Days 3–10** (Customer Account, Reviews+Media, Promotions+Shipping, Returns+Refunds, Admin Expansion, Homepage/Bundles/Notifications, SEO/A11y/Perf, Security/Final) — **not started.** No code for any of these exists on `dev1` yet. Flagging plainly since `week2 (1).md` §39's acceptance gate explicitly warns against marking anything complete "based only on code existence or journal entries" — the honest state is Day 0–2 of 10.
- **CI actually executed** (§35's own acceptance line) — still not confirmed. `git status`/`git log` show `dev1` clean and pushed, but this session did not check GitHub Actions' run history; carrying this forward as unverified rather than assuming it now runs.

**Independent re-verification performed today, from scratch, in this same worktree (not the agent's isolated one — Day 2's feature branch is already merged):**
1. Stood up this machine's dedicated Postgres (`:5433`)/Redis (`:6380`) — both were down at session start, started via the same `pg_ctl`/`redis-server` commands prior entries document.
2. `pnpm install` (no-op, already current) + `pnpm run bootstrap` — clean, Prisma client regenerated, `woobe_dev` had zero pending migrations.
3. `pnpm run lint` / `pnpm run typecheck` / `pnpm run boundaries:check` — all clean across all 9 workspace projects (263 modules / 699 dependencies, matching the last-recorded count exactly, confirming no drift since Day 2's merge).
4. `pnpm run test` — **142/142, run five times in a row, zero failures.** This directly probes the "~25% flaky" finding two entries up. Before the first run, found and fixed a real contributor to that flakiness (see finding below); after cleanup, five consecutive full-suite runs were all clean, and `woobe_test`'s row counts (10 products, 3 seeded users) returned to exact baseline after every run — the test suite's own `afterAll` teardown does work correctly when a run completes normally.
5. `pnpm run build`, all three apps, with **zero API/web/admin processes running** (killed everything first, `rm -rf` on both `.next` caches) — clean, `/` and `/collections/[slug]` both still render `ƒ Dynamic` in the route table (ADR-026 intact).
6. `node scripts/check-destructive-migration.mjs` — clean, exit 0.
7. **Source-level review** of the Day 2 code actually running on `dev1` (not just its tests): read `wishlist.repository.ts`, `add-wishlist-item.use-case.ts`, `remove-wishlist-item.use-case.ts`, `move-wishlist-item-to-cart.use-case.ts`, `collection.repository.ts`, `reorder-collection-products.use-case.ts`, and the admin collections router/controller. Confirmed: wishlist authorization is genuinely enforced by scoping every query to the caller's own `wishlistId` (never a request param) rather than a bolted-on ownership check; duplicate prevention rides the schema's own `@@unique` constraint, mapped to a clean 409; `move-to-cart` re-checks live stock at click time, not the view's own (potentially stale) `isAvailable` flag; the admin collections router applies `authGuard` + `requirePermission(MANAGE_CATALOG)` at the router level before any handler runs, so there's no route in that file reachable without both. No defects found — this matches what the Day 2 entries claim.
   - One minor, non-blocking observation (not a bug the plan requires fixing, flagging for awareness): `MoveWishlistItemToCartUseCase.execute` calls `cartWriter.addItem` and then `wishlistRepository.removeItem` as two separate, unwrapped calls, not one transaction. If the process died between them, the item would land in both the cart and the wishlist rather than being cleanly moved. Low practical risk (a duplicate wishlist entry pointing at something already in the cart is a cosmetic annoyance, not a money/inventory correctness issue — inventory and pricing are unaffected either way), and nothing in `week2 (1).md` §5 calls for this cross-module operation to be atomic. Noting it, not fixing it unasked.
8. **Live browser walkthrough** (chrome-devtools-mcp, 375px mobile viewport, isolated context): registered a fresh account → PLP "Save to wishlist" heart on Silk Scarf → `/wishlist` correctly shows the no-variant "No size selected" / "Choose a size" path and the BottomNav badge reads "1 Wishlist" → `/collections/new-drops` renders with real per-collection `<title>` metadata ("New Drops — Woobe") → back on `/wishlist`, clicked Remove → empty state ("Your wishlist is empty.") and badge disappear correctly. Zero console errors or warnings at every step. Test account and its wishlist row cleaned up from `woobe_dev` afterward.

**Two real findings, both fixed:**
1. **`woobe_test` had accumulated stale rows from a previously-interrupted test run** — 18 leftover `*@test.woobe.internal` users (mostly `wishlist-test-*`), 15 leftover `wishlist-test Product *` rows (inflating the product count to 25 against the seeded baseline of 10), and one orphaned `cart_items` row referencing a since-fixture'd variant, which was blocking a straightforward delete. This is almost certainly a contributor to (maybe the whole explanation for) the "~25% flaky full suite" finding two entries up — extra rows sharing category/product-name space with fresh test fixtures is exactly the kind of cross-run interference that would surface as an inconsistent, non-reproducible failure. Cleaned up via ordinary row deletes (`DELETE FROM cart_items WHERE variantId IN (...)`, then the orphaned products, then the stale users — cascades handled the rest), not a schema reset. **Not a code defect** — Prisma itself correctly refused a `migrate reset` I first reached for (it detected an AI-agent-invoked destructive-reset attempt and required explicit user consent per its own safety gate, which I did not have and did not seek — row-level cleanup was both sufficient and the least destructive option, so I used that instead). After cleanup, five consecutive full-suite runs stayed clean and the DB returned to exact baseline every time — the actual root cause of the historical flakiness looks more like "a killed/crashed test process leaves rows behind" than "the suite's own teardown logic is broken," since teardown demonstrably works correctly on a normal completion. Still worth the serialized/isolated-fixtures fix the earlier entry recommended, as a defense against exactly this class of leftover-data problem recurring after any future interrupted run — not done today, outside this audit's scope.
2. **`journal.md` itself had a byte-for-byte duplicated entry.** The full "2026-08-27 — Week 2 Day 2, attempt 2: Collections + Wishlist — built, tested, verified live" entry appeared twice, identically, with the separate "independently re-verified" entry sandwiched in between the two copies — confirmed with a direct `diff`, which came back empty. Removed the second (redundant) copy; nothing else in the file was touched. This was a journal-hygiene defect, not a code defect, but the user's own instruction for this session was explicitly to check "no mistake made in the current implementation," which I'm reading to include the record of it — a duplicated entry is exactly the kind of thing that would mislead whoever reads this file next about how many times Day 2 was actually built or reviewed.

**Conclusion:** no functional defect found in any Week 2 Day 0–2 code running on `dev1` today. The two things found and fixed this session were both hygiene issues (stale test-database residue, a duplicated journal entry) rather than application bugs — the actual collections/wishlist/catalogue-discovery/inventory-restock/web-build implementations all check out against `week2 (1).md`'s own requirements on direct source reading, not just by trusting their tests or prior journal claims.

**Follow-ups / known gaps (carried forward, none new beyond the two fixed above):**
- The vitest full-suite flakiness itself (serialized integration-test execution or per-file-isolated fixtures) is still not implemented — today's finding narrows the likely cause (leftover rows from an interrupted run) but doesn't add the defensive fix. Worth doing before Day 3 adds yet more integration test files.
- CI's actual GitHub Actions execution is still unconfirmed this session (see "Where Week 2 stands" above).
- Everything else already flagged in the Day 0/1/2 entries above (admin collection-management UI deferred to Day 7, PDP wishlist save is product-level only, `GET /wishlist/state/:productId` unused by the current frontend, collection pills on `/products` still behave as filters not links, no rate limiting on login/register, no BullMQ, abandoned-`PENDING_PAYMENT` inventory holds still unreleased, Razorpay real test keys still not configured) stands unchanged — none of it was in this audit's scope to fix.
- **Next:** Day 3 (Customer Account — profile + addresses, `week2 (1).md` §6–7) has not been started.

---

## 2026-08-28 — Week 2 Day 3: Customer Profile + Address Management — built, tested, verified live (one real bug found and fixed)

**Branch:** `dev1` — uncommitted (per this session's established process: only commit/push when explicitly asked). Built directly in this session, not dispatched to a worktree-isolated subagent (no scope large enough today to warrant the isolation/review overhead the Day 1/2 subagent pattern was for).

**Scope:** exactly `project_planning/week2 (1).md` §6 (Module 4 — Customer Profile) + §7 (Module 5 — Address Management) + the Day 3 bullets in §27/§38.

**Architecture decision made before writing any code:** `week2 (1).md` §2 lists a `users` module that didn't exist anywhere in Week 1 — `architecture.md` §3.3's original 17-module list never had one (`auth` was always the sole owner of `User`, per its own `AuthCredential`-is-separate design in `plan.md` §3). Rather than give the new `users` module its own competing repository onto `User` (two modules' `infrastructure` both writing the same table — a real ADR-010 violation in spirit even though the current dependency-cruiser rule only checks "does a module's own infrastructure import Prisma," not "does more than one module's infrastructure touch the same table"), `users` module:
- Owns `Address` outright — genuinely new territory, first module ever to touch it (Week 1's checkout deliberately left `Order.addressId` null and used its own `shippingSnapshot` JSON instead, per the Day 4 journal entry).
- For profile view/edit, imports `auth`'s own exported use-cases (`getCurrentUserUseCase`, and a new `updateUserProfileUseCase`) directly — same "reuse the other module's exported use-case, don't duplicate its write path" pattern `admin.module.ts` already established for staff login (`registerUserUseCase`/`loginUserUseCase`).

**No migration needed** — `Address` already existed in `schema.prisma` from Week 1 with exactly the fields `week2 (1).md` §7 lists (fullName/phone/line1/line2/city/state/pincode/isDefault), no `country` column (not present in the approved schema, and the plan says "use the approved schema if it already exists" — didn't add one).

**What changed — API (`apps/api`):**
- **`auth` module extended**: `AuthRepositoryPort.updateUserName` + impl, `UpdateUserProfileUseCase` (only `name` is writable — see the deliberate scope call below), exported from `auth.module.ts` for `users` to consume.
- **`users` module — new**, full `domain/application/infrastructure/interface` shape (ADR-010):
  - `GET /api/v1/users/me` (profile view, pass-through to auth), `PATCH /api/v1/users/me` (edit name).
  - `GET/POST /api/v1/users/me/addresses`, `PATCH/DELETE /api/v1/users/me/addresses/:id`, `POST /api/v1/users/me/addresses/:id/default`. Every route `authGuard`-mounted — addresses/profile are always personal, no guest/admin path.
  - **Authorization**: every address method scopes its query by `(id, userId)` together (`AddressRepository.update`/`deleteAndListRemaining` use `updateMany`/`deleteMany` with both conditions — a `count === 0` result is indistinguishable from "doesn't exist" and correctly 404s either way), same mechanism `WishlistRepository` already established.
  - **"Enforce one default address" (§7), decided and documented, not left implicit**: a customer's first-ever address is always the default regardless of what `isDefault` was sent (`CreateAddressUseCase`); creating a new address with `isDefault: true` unsets the previous default in the same transaction (`prisma.$transaction`); deleting the current default promotes the oldest remaining address to default via a new pure domain function, `selectPromotedDefault` (unit-tested in isolation, 3 tests) — a customer with saved addresses is never left with zero default.
  - `packages/validation`'s new `users.schema.ts`: `updateProfileSchema` (name only), `createAddressSchema`/`updateAddressSchema` (field shape mirrors `checkout.schema.ts`'s `checkoutAddressSchema` — same Indian-phone/6-digit-pincode rules — deliberately duplicated rather than imported, since checkout and the address book stay independent this week, nothing in the plan asks them to be wired together).
  - 23 new tests (20 integration against real `woobe_test` Postgres + 3 unit for `selectPromotedDefault`): profile view/edit/validation/email-not-editable, address create/first-is-always-default/second-respects-choice/new-default-unsets-old, update, delete (non-default, default-with-promotion, only-address), explicit set-default, and full authorization coverage (customer B can't list/update/delete/set-default customer A's addresses — all 404, not silent success or a leak).
- **Deliberate scope call, flagged prominently**: only `name` is editable via `PATCH /users/me`. §6 says "sensitive identity changes must use the approved verification flow" — this codebase has **no** approved email/phone verification flow (OTP is only "extensible for later" per `plan.md` §3, nothing built). Accepting an email/phone change here would mean inventing that flow rather than implementing an approved one, which `week2 (1).md` §1 explicitly forbids ("do not invent business rules"). `updateProfileSchema` enforces this at the request-shape level too — an email field sent alongside `name` is silently dropped, verified with a dedicated test.

**What changed — Frontend (`apps/web`):**
- `features/profile/{api,components}` (`ProfileForm` — name edit, email/phone shown read-only with an explanation, Save disabled until the form is actually dirty) and `features/addresses/{api,hooks,components}` (`AddressesProvider`/`useAddresses`, `AddressForm` shared for add/edit, `AddressCard`, `AddressBookPageContent`). Pages: `/account/profile`, `/account/addresses`. `AccountView` gained "Edit profile" and "Your addresses" links (same Card-link pattern as the existing "My orders").
- `AuthContext` gained `refreshUser()` (re-fetches `/auth/me`) so a name change made through the new `/users/me` endpoint is reflected immediately without a full re-login — `ProfileForm` calls it after a successful save.

**Bug found and fixed, live in the browser, not by inspection:** the very first thing tried (add an address) silently did nothing — a 201 came back from the API with the correct data, but the address book UI stayed on its empty state. Root cause: `useAddresses` was written as a bare hook (`useState` inside a function each caller invokes independently), not a Context provider — `AddressBookPageContent` (which renders the list) and `AddressForm`/`AddressCard` (which mutate) each got their **own private copy** of `addresses` state. A create in the form updated a state array nothing ever rendered, while the list-rendering component's own separate copy never refetched. Confirmed via the network tab (`POST .../addresses` → 201, immediate `GET .../addresses` → 200 with the new row in it — the API was never the problem) before touching any code. Fixed by converting `useAddresses` into a real `AddressesProvider` + `useAddresses()` Context pair, mirroring the exact shape `CartProvider`/`WishlistProvider` already use for this identical class of problem — mounted once by `AddressBookPageContent`, consumed by every child that reads or writes the list.

**Second bug found and fixed, live in the browser, only surfaced once the first one was fixed:** with the Context fix in place, clicking "Add address" on the (six-field) first-address form navigated straight to `/products` instead of submitting — no `POST` ever left the browser (confirmed via the network tab: zero request). Root-caused with `document.elementFromPoint()` at the button's own coordinates: the form is tall enough that, at a 375×812 viewport with no scrolling yet, the submit/cancel button row rendered mostly **behind** the fixed, `z-30` `BottomNav` (button `top:740 bottom:784` vs. nav `top:744` — only a 4px sliver poking out above the bar), so a tap there resolved to the nav's own "Shop" link underneath. The storefront layout's `pb-20` spacer correctly guarantees clearance **once the page is scrolled all the way down** (verified: at max scroll, the button clears the nav with 12px to spare, `elementFromPoint` correctly resolves to the button) — the gap is that this is the *first paint*, before anything triggers a scroll, and it's not a fringe automation-only artifact: a real thumb tapping the barely-visible 4px sliver of a button would hit the same result. Fixed with the direct remedy rather than fiddling with global padding: `AddressForm` now scrolls itself into view (`scrollIntoView({ block: "end" })`) the moment it mounts (revealing the form via "Add a new address"/"Edit" is exactly the moment a fresh scroll-to-reveal is expected UX, and it's a no-op when the form already fits on screen), combined with a new shared `SCROLL_MARGIN_ABOVE_BOTTOM_NAV_STYLE` (`layout-constants.ts`, alongside the existing `MOBILE_BOTTOM_NAV_HEIGHT_REM`/`ABOVE_MOBILE_BOTTOM_NAV_STYLE` from the Week 1 mobile-responsiveness fixes) so the browser's own scroll-into-view math stops with the nav's height held clear, not just "as visible as possible." Re-verified after the fix: the same click sequence now correctly auto-scrolls (confirmed `scrollY` moved from 0→40) and lands the `POST` with the address appearing in the list immediately.

**Verified live** (chrome-devtools-mcp, 375×812 mobile then re-checked at 1440×900 desktop, isolated context, this session's own dev servers): registered a real account → `/account/profile` pre-filled with name/email, Save correctly disabled until dirty → edited name, saved → `/account` immediately shows the new name (no reload) → `/account/addresses` empty state → added a first address → appears immediately, marked "Default" (server-forced, checkbox correctly hidden with an explanatory note instead) → added a second address unchecked → list re-sorts default-first, only one `Default` badge → clicked "Set as default" on the second → badges flip correctly, exactly one default at all times → deleted the now-default second address → the first address (previously non-default) is auto-promoted, confirmed in the UI → edited the remaining address's city → persisted correctly, other fields untouched. Zero console errors or warnings at any point. Both pages re-checked at 1440px desktop (BottomNav is `md:hidden`, so the scroll-collision bug is mobile-only) — clean, no layout issues. Test accounts cleaned up from `woobe_dev` afterward.

**Test counts:** 165 (Day 0/1/2 baseline 142 + 23 new: 20 `users.integration.test.ts` + 3 `select-promoted-default.test.ts`).

**Verification commands, all clean, run for real, repeatedly:** `pnpm run lint`, `pnpm run typecheck`, `pnpm run boundaries:check` (279 modules / 746 dependencies, up from 263/699 — zero violations), `pnpm run test` — **165/165, four consecutive full-suite runs, zero failures** (re-verifying the Day 2 flakiness finding stays fixed as more integration-test files are added, per that entry's own "worth addressing before Day 3 adds more files" flag — no recurrence this session), `pnpm run build` (all three apps, zero live services running, confirmed by killing every dev server first — `/account/profile` and `/account/addresses` both compile, both correctly `○ Static` since their data loads client-side through the same context pattern cart/wishlist use, not a server-side fetch).

**Follow-ups / known gaps:**
- Checkout still doesn't read from the saved address book (deliberate — nothing in `week2 (1).md` §7 or §6 asks for that wiring, and Week 1's checkout intentionally stayed independent; flagging so it isn't mistaken for forgotten if Week 3 wants "select a saved address at checkout").
- The scroll-margin/auto-scroll fix was applied to `AddressForm` specifically (where the bug was concretely reproduced) — `ProfileForm`'s shorter form wasn't at risk today, but if any other inline-reveal form on this route set grows taller, check it against the same `document.elementFromPoint()` technique before assuming it's fine.
- Email/phone remain non-editable this week (see the deliberate scope call above) — worth a real ADR if/when an OTP or email-confirmation flow is ever approved for Week 3+.
- `MoveWishlistItemToCartUseCase`'s non-atomic cart-add-then-wishlist-remove (flagged in the prior audit entry) is untouched — unrelated to this day's work.

---

## 2026-08-28 — Post-Day-3 audit: full responsive sweep (375/768/1024/1440) + codebase security/performance review — no code changes, nothing broken

**Branch:** `dev1` — no files changed by this entry itself (audit only, per the user's explicit ask to "check all viewport... and check the codebase for vulnerability that can affect performance").

**Responsive sweep, all four required breakpoints, with real content (not empty states):** registered a fresh account, added an address, a wishlist item, and cart items (enough weight to clear the checkout minimum), then swept every storefront page — home, `/products`, PDP, cart, checkout, wishlist, collection detail, account, `/account/profile`, `/account/addresses` — at 375×812, 768×1024, 1024×768, and 1440×900. Checked via `document.documentElement.scrollWidth > window.innerWidth` (automated, catches any horizontal overflow) plus visual screenshots at the higher breakpoints and a `list_console_messages` check after each page.

- **All four breakpoints clean** — zero horizontal overflow, zero console errors/warnings, on every page tested.
- **Specifically re-checked the two bugs fixed earlier this session for a scroll-margin regression** — `CheckoutForm`'s submit button (a similarly long form) was checked with the same `document.elementFromPoint()` technique used to find the `AddressForm` bug: at max scroll it resolves to the button itself, not `BottomNav` (checkout's content is far taller than the viewport — 847px of scroll room — so there's no ambiguous "barely peeking" zone the way `AddressForm`'s borderline height created). Confirms the bug found earlier was specific to a form whose height barely exceeds one viewport, not a general defect in every tall form on this route.
- **One pre-existing, not-new observation**: `/products`' "Filters" disclosure button renders identically at 1024/1440 as it does on mobile — already documented as a deliberate Day 1 simplification ("no Modal/Sheet primitive yet, same filtering behavior, different chrome"), not a regression from this session's work.

**Security review** (ran the `security-review` skill's methodology against the live codebase, not just the Day 3 diff, per the user's request to check "the codebase"):

- **No injection surface**: zero `dangerouslySetInnerHTML`/`eval`/raw SQL (`$queryRaw`/`$executeRaw`) anywhere — every DB access goes through Prisma's parameterized query builder. Confirmed by grep across `apps/api`, `apps/web`, `apps/admin`.
- **JWT**: `jwt.verify()` is called with a plain HMAC secret string throughout (`auth-guard.ts`, `optional-auth-guard.ts`, `jwt.service.ts`) — `jsonwebtoken` restricts verification to HMAC algorithms when given a plain string key, so this isn't vulnerable to the classic `alg:none`/algorithm-confusion attack (that requires either an explicit `algorithms` misconfiguration or an RSA/EC public key being handed to `verify()`, neither of which happens here).
- **Cookies**: every cookie (customer refresh, admin refresh, cart) is `httpOnly` + signed + `secure` in production; `sameSite: "lax"` for customer/cart, correctly upgraded to `"strict"` for the admin session (`admin-refresh-cookie.ts`).
- **CORS**: locked to an explicit `[WEB_ORIGIN, ADMIN_ORIGIN]` allowlist (`app.ts`) — not a wildcard or reflected-origin config.
- **IDOR**: re-verified wishlist, addresses (this week's own new module), and admin-collections all scope every query by the caller's own id (`userId`/`wishlistId` from the verified token, never a request param) — no cross-account read/write path found on re-inspection.
- **Webhook**: Razorpay signature verification still runs against the true raw request bytes (`capture-raw-body.ts`), not a re-serialized body.
- **Mass assignment**: Zod schemas strip unrecognized fields by default; every write path derives `userId` from the verified access token, never the request body — spot-checked against the new `createAddressSchema`/`updateAddressSchema`.
- **One thing checked and deliberately NOT reported as a vulnerability**: `GET /api/v1/admin/me` only requires `authGuard` (any valid token), not a staff-role check — a plain customer's own access token would pass it. No actual exploit path exists (it can only ever reflect the caller's own already-known data back to them, no cross-user leak, no privilege escalation), so this doesn't meet the "concrete, exploitable" bar — noted for awareness, not fixed.
- **Conclusion: no new high- or medium-confidence vulnerability found**, in Day 0–3's code or in the broader codebase sampled by this pass.

**Performance review:**

- **No N+1 queries** in anything customer-facing and performance-sensitive — wishlist, products, and the new addresses endpoints all batch their lookups (`Promise.all` + bulk `getProducts(ids)`/`getVariants(ids)` against id sets), confirmed by reading `GetWishlistUseCase` and the products list path directly, not assumed.
- **One pre-existing, minor inefficiency, not introduced this week**: `MergeGuestCartUseCase` (Week 1 Day 3) does one DB round-trip per cart line in a loop (`findItemByVariant` inside a `for` loop over guest cart items) when merging a guest cart on login. Low real-world impact — cart size is naturally small in normal use — not fixed, just flagged since it was found while grepping for `await`-in-loop patterns.
- **Pagination is bounded everywhere it matters**: `/products` caps at 50 (`productListQuerySchema`), admin order list caps at 100 (`admin.schema.ts`'s `pageSize`) — confirmed neither endpoint lets a caller request an unbounded result set.
- **The one real, already-known performance/abuse-resistance gap**: no rate limiting on `/login` or `/register`, flagged since Week 1 Day 2's journal entry and still open. Not in scope for a "vulnerability" finding (DoS/rate-limiting is an explicit exclusion in the security-review methodology), but it's real — worth building before Day 10's security-hardening pass if it isn't picked up sooner.

**Why no code changes accompany this entry:** this was a verification pass, not a build day — everything checked came back clean except the two things already fixed earlier in the Day 3 entry above (and the rate-limiting gap, which predates this week and wasn't in today's scope to fix unasked).

---

## 2026-08-28 — Week 2 Day 4: Reviews & Ratings + Media Management — built, tested, verified live

**Branch:** `dev1` — uncommitted (per this session's established process: only commit/push when explicitly asked). Built directly in this session.

**Scope:** exactly `project_planning/week2 (1).md` §8 (Module 6 — Reviews & Ratings) + §13 (Module 11 — Media Management) + the Day 4 bullets in §27/§38.

**Migration** (`20260828044147_reviews_moderation_and_media`, additive except one deliberate, documented destructive column drop):
- `Review`: replaced Week 1's plain `isApproved: Boolean` with a `ReviewStatus` enum (`PENDING`/`APPROVED`/`REJECTED`/`HIDDEN` — the admin "approve/reject/hide" bullet needs three outcomes, not one), added `isVerifiedPurchase Boolean`, `updatedAt`, and `@@unique([productId, userId])` (§8's own "prevent duplicate reviews" rule, enforced by the schema itself). The `DROP COLUMN "isApproved"` is marked `-- accept-data-loss:` per ADR-013 — safe because Week 1 never built the reviews feature and seed.ts never wrote to this table, so there was nothing to lose.
- New `Media` table + `MediaType` (`IMAGE` only — video/gif/360 have no approved consumer yet, deliberately deferred) + `MediaStatus` (`ACTIVE`/`DELETED`) enums.
- **Same raw-SQL-index gotcha flagged in Day 2's journal entry recurred exactly as predicted**: `prisma migrate diff`'s generated SQL again proposed `DROP INDEX "products_name_trgm_idx"` (the pg_trgm index Prisma's schema DSL has no record of). Caught by reading the generated SQL before writing the migration file — manually excluded that line, migration touches nothing on `products`. Verified via `psql \di` that the index survived on both `woobe_dev` and `woobe_test` post-migration.

**Reviews module** (built out from the Week 1 placeholder):
- Customer: `GET /api/v1/reviews?productId=` (public, no auth — APPROVED reviews only + a rating summary), `POST /api/v1/reviews` (submit, authGuard), `PATCH /api/v1/reviews/:id` / `DELETE /api/v1/reviews/:id` (edit/delete own, authGuard + ownership).
- **Deliberate scope call, flagged prominently**: no purchase gate on submission. `week2 (1).md` §8 lists "Enforce purchase eligibility where required" as a rule but separately lists "Verified-purchase indicator" as its own customer bullet — nothing in `plan.md`/`architecture.md` mandates "must have purchased to review," and §1 explicitly says "do not invent business rules." Any authenticated customer may submit; a live check against `orders` (new `HasPurchasedProductUseCase`, exported for `reviews` through a port — checks for a CONFIRMED/PROCESSING/SHIPPED/DELIVERED order containing the product) sets `isVerifiedPurchase` as a badge instead, the same pattern many real storefronts use.
- **New reviews start PENDING** — nothing is publicly visible or counted in the rating summary until an admin approves it. This is what makes "Moderation" and its own test bullet meaningful rather than vacuous; documented as an interpretation, not silently assumed.
- **Editing an own review resets it to PENDING** for re-moderation — otherwise a customer could get a review approved and then silently rewrite it into something that was never actually reviewed.
- Rating summary (`computeRatingSummary`, pure domain function, 4 unit tests) is fed by a `groupBy` query on APPROVED reviews only — never loads every review row into memory just to average them (same "aggregate in the DB, not in application code" discipline the rest of this codebase already follows).
- Admin (RBAC-gated `MANAGE_CATALOG`, mounted at `/api/v1/admin/reviews` inside the `admin` module per ADR-025, same thin-gateway shape `AdminCollectionsController` already established): list (filterable by status), approve/reject/hide — one parameterized `ModerateReviewUseCase` rather than three separate ones (a plain status flip has no distinct side effects per transition, unlike orders' ship/deliver splits).
- Authorization: every own-review mutation scopes by `(id, userId)` together (`updateMany`/`deleteMany`), same mechanism `AddressRepository`/`WishlistRepository` already established — a review id belonging to another account simply doesn't match.
- Review photos ("where approved") deliberately **not built** — no approved requirement names them as a concrete consumer, and Module 11's own "Support" list (below) doesn't include review media among what it names either. Flagged as a narrow, documented scope call, not an oversight.
- 21 new tests (4 unit for `computeRatingSummary` + 17 integration against real `woobe_test` Postgres): submission/validation/duplicate-prevention/eligibility(verified-purchase), public listing + rating aggregation, edit/delete + authorization, admin moderation + RBAC authorization.

**Media module** (new — `week2 (1).md` §2 adds it fresh, no Week 1 placeholder existed):
- Provider-independent `MediaStoragePort` (`save`/`delete`/`getUrl`) per §13's own architecture diagram, implemented today by `LocalDiskMediaStorage` — no S3/Cloudinary credentials are approved yet (same situation Razorpay was in before Week 1 Day 5's real keys), so rather than a dead "not configured" stub, this is a genuinely working adapter: files land under `MEDIA_UPLOAD_DIR` (default `uploads/`, gitignored), served back out via a new `express.static` mount in `app.ts`. Swapping in a real S3/Cloudinary class later means writing one more class against the same interface — nothing above that layer changes.
- New env vars (`API_PUBLIC_URL`, `MEDIA_UPLOAD_DIR`), both with sane defaults, documented in `.env.example`.
- **A real bug caught and fixed before it shipped, not by luck**: Helmet's default `Cross-Origin-Resource-Policy: same-origin` would have silently blocked `apps/web`/`apps/admin` (different origins/ports) from loading an uploaded image via a plain `<img src>` tag. Relaxed to `cross-origin` for the `/uploads` static mount specifically — everything else (the JSON API) keeps Helmet's stricter default, verified by an integration test that actually fetches the returned URL back and compares bytes, not just asserting the upload call itself returned 201.
- File validation (`validateUpload`, pure domain function, 6 unit tests): only `image/jpeg`/`image/png`/`image/webp`, max 5MB — video/gif/360 mime types deliberately excluded (no approved consumer). Multer's own size-limit rejection (`MulterError`) is mapped to a clean `ValidationError` rather than falling through to a generic 500 — caught by reading `error-handler.ts` and noticing it only maps `DomainError`, not multer's callback-style error.
- Admin-only surface this week (§13's own "Admin uploads" bullet): `POST /api/v1/media` (multipart upload, RBAC-gated `MANAGE_CATALOG`), `GET /api/v1/media/:id`, `DELETE /api/v1/media/:id` (soft-delete — `status: DELETED`, row kept for audit, actual bytes removed from disk). **No admin UI for this yet** — same deliberate deferral Day 2 gave collection management: `apps/admin` has no product-picker/media UI at all yet, that's Day 7 (Module 14 — Admin Product Management, which explicitly lists "Media" as one of its own operations); building a one-off upload UI today, disconnected from Day 7's real one, would be exactly the scope creep §32 warns against.
- Uploading twice into the DB (upload succeeds, `Media` row insert fails) cleans up the already-written file rather than leaking an orphan — best-effort, documented as astronomically unlikely (a key collision or dropped connection) but handled anyway since the cost of handling it was one `.catch()`.
- 15 new tests (6 unit for `validateUpload` + 9 integration against the real filesystem and `woobe_test`): valid upload + the returned URL genuinely serves the bytes back, disallowed mime type, oversized file, missing file, unauthenticated (401), wrong-role staff (403), get metadata, delete (soft-delete + file actually gone + 404 on refetch), delete unknown id.
- **A real test-hygiene bug caught by re-checking row counts, the same discipline the Day 2 audit established**: an early version of the "delete" test deliberately left its `Media` row out of the suite's own cleanup list ("harmless, it's already soft-deleted" — the comment's own reasoning), which meant every repeated local run left one more `DELETED` row behind in `woobe_test` forever. Caught by checking `select count(*) from media` after several repeated runs (5 rows after 5 runs, not 0) — fixed by tracking it for cleanup like every other test's fixture, verified back to 0 after subsequent runs.

**Frontend (`apps/web`):**
- `features/reviews/{api,components}`: `ReviewsSection` (mounted into `ProductDetail`, below the existing buy panel) — rating summary with a per-star breakdown bar, the review list, a "Write a review" disclosure form for a logged-in shopper, a "Log in to write a review" prompt for a guest. Every mutation reloads the server's own view (same "server is the only source of truth" rule cart/wishlist already established) rather than optimistically inserting a client-guessed row — appropriate here specifically because a submitted review is PENDING and genuinely isn't visible to anyone else yet, so optimistically showing it would misrepresent what's actually live.
- New `packages/ui` primitive: `Textarea` (mirrors `Input`'s exact styling) — the first multi-line text field this app has needed, added now rather than hand-rolled once, per this codebase's own stated "arrives when a real feature needs it" policy for primitives it doesn't have yet (`packages/ui/src/index.ts`'s own comment about Dialog/Select).
- No customer-facing media upload UI (review photos deferred, see above) and no admin media UI (Day 7) — `apps/web`/`apps/admin` don't reference the media module's endpoints at all this week; it's real, tested, working API surface waiting for a consumer.

**Verified live** (chrome-devtools-mcp, 375×812 then re-checked at 1440×900, this session's own dev servers, not asserted from code alone): registered a real account → opened the Silk Scarf PDP → empty "No reviews yet" state → wrote a 5-star review with title/body → submission correctly returns to the empty state (PENDING, not yet public) → confirmed via direct DB query the row exists with `status: PENDING` → approved it via a real `catalog@woobe.in` RBAC-token curl call → reloaded the PDP → rating summary (5.0, 1 review, full per-star breakdown), the review card, and its date all render correctly at both viewports, no "Verified Purchase" badge (correct — this account never bought it). Zero console errors/warnings at any point. Test account (and its cascade-deleted review) cleaned up from `woobe_dev` afterward.

**Test counts:** 201 (Day 0–3 baseline 165 + 21 reviews + 15 media).

**Verification commands, all clean, run for real, repeatedly:**
- `pnpm run lint`, `pnpm run typecheck`, `pnpm run boundaries:check` (313 modules / 860 dependencies, up from 279/746 — zero violations), `node scripts/check-destructive-migration.mjs` (clean; the accept-data-loss marker was verified manually too, since the script can't see an uncommitted file's own diff).
- `pnpm run test` — **201/201, four consecutive full-suite runs, zero failures**, `woobe_test` row counts (products, users, reviews, media) all confirmed back to exact baseline after every run.
- `pnpm run build` (all three apps) — confirmed with dev servers genuinely killed first (an earlier build in this session accidentally ran with stray processes still up from a prior step; caught before treating it as a valid zero-services verification, and re-run properly) — clean, `/products/[slug]` still compiles.

**Follow-ups / known gaps:**
- Review photos and a customer-facing upload path are both deferred — see the deliberate scope calls above. Revisit if/when product approval names a concrete photo-upload consumer.
- Admin UI for both review moderation and media management deferred to Day 7, same pattern as Day 2's collections — the APIs are complete, RBAC-gated, and tested; `apps/admin` has no page for either yet.
- `GET /api/v1/reviews` has no "has the current user already reviewed this product" check — the frontend just lets a second submission attempt fail with the API's own 409 message rather than pre-emptively hiding the form. A small, deliberate simplification, not a gap in the API's own correctness (the 409 and the schema constraint behind it are real and tested).
- `MoveWishlistItemToCartUseCase`'s non-atomic cart-add-then-wishlist-remove and the no-rate-limiting gap (both flagged in earlier entries) remain untouched — unrelated to this day's work.
- **Next:** Day 5 (Promotions + Shipping, `week2 (1).md` §9–10) has not been started.

---

## 2026-08-28 — Admin-side audit: two real bugs found and fixed (`apps/admin` had no mobile viewport at all)

**Branch:** `dev1` — uncommitted, per this session's established process.

**Context:** the user asked to check the admin side too, since every prior verification pass this session focused on `apps/web`. Logged into `apps/admin` as all three staff roles (`admin@woobe.in`/super_admin, `orders@woobe.in`/order_processing_staff, `catalog@woobe.in`/product_management_staff) and walked the full order lifecycle live, then swept every admin page at 375/768/1024/1440px.

**Functional walkthrough, all correct, nothing broken:**
- Placed two real orders via the storefront (guest COD) and drove one through `CONFIRMED → PROCESSING → SHIPPED (tracking number + carrier captured) → DELIVERED` via the admin UI — every transition, the timeline, and the disabled/enabled action buttons at each state were all correct.
- Cancelled the second order while still `CONFIRMED` — **re-verified the Day 0 inventory-restock fix still works end-to-end through the actual admin UI** (not just the test suite): `quantityAvailable` went 18→16 on order placement, then correctly back to 18 on cancellation. The "Refund needs manual follow-up" message shown is expected (no real Razorpay keys configured, per `DECISIONS_PENDING.md`) — not a bug.
- RBAC nav filtering confirmed correct for all three roles: super_admin sees every section, order_processing_staff sees only Orders/Returns, product_management_staff sees only Products/Inventory (both "coming soon" — see below).
- Server-side RBAC re-confirmed live (not just via the test suite): `order_processing_staff` correctly 403s on anything catalog-only; the reverse for `product_management_staff` on orders.

**Two real bugs found and fixed:**
1. **`apps/admin` had no `<meta name="viewport">` at all — the entire app silently rendered as a shrunk-down desktop layout on any mobile-width screen**, not just cosmetically cramped. `apps/admin/app/layout.tsx` never had the `viewport` export `apps/web` has had since Week 1; without it, a mobile browser (real device or Chrome's own emulation) falls back to a ~980px "desktop simulation" layout width — confirmed directly: `window.innerWidth` reported `992` while genuinely emulating a 375px-wide device. On top of that, `Sidebar` was a permanently-visible `w-56 shrink-0` column with no responsive breakpoint, so even once the viewport was fixed, real 375px content sat mostly off-screen requiring horizontal scroll to reach. Fixed both layers: added the missing `viewport` export, and made `Sidebar` collapse into a horizontal scroll strip above the content below `md:` (same "same controls, different chrome" approach the storefront's mobile filter panel already uses — not a new nav pattern invented for this), with `DashboardLayout` and `TopBar` adjusted to stack/truncate correctly at narrow widths. Verified after the fix: `375/768/1024/1440` all report zero horizontal overflow, confirmed by both `scrollWidth === innerWidth` checks and live screenshots at each breakpoint, zero console errors throughout.
2. **A real, concrete accessibility defect**: the orders-list status filter (`<select>`) and search box (`<input>`) had no `id`, `name`, or accessible name at all — flagged live by Chrome's own DevTools Issues panel ("A form field element should have an id or name attribute"), not hypothetical. Screen-reader users had no way to know what either control was for, and Chrome's autofill heuristics couldn't identify them either. Fixed with `aria-label` (accessibility) and `name` (autofill) on both — verified the DevTools issue count dropped to zero afterward, not just assumed fixed.

**One real, smaller UX bug found and fixed along the way:** `product_management_staff` has no live (non-"coming-soon") admin section at all yet (Products/Inventory are Day 7 scope) — logging in unconditionally redirected to `/orders` regardless of role, landing this staff role on a page they immediately get 403'd from, surfaced as a generic, alarming "Couldn't load orders." with no indication of why. Two changes: `LoginForm` now redirects to the first *live* nav entry the logged-in role actually has permission for (still falls back to `/orders` today since there's genuinely nowhere else for this role to go until Day 7, but this stops being a silent trap the moment Products/Inventory ship); `useAdminOrders` now distinguishes a `403` from a genuine load failure and shows "You don't have permission to view orders." instead — an honest, correctly-scoped message rather than a network-error-shaped one. Verified live: the message text changed exactly as expected on reload.

**Verification commands, all clean, run for real:** `pnpm run typecheck` / `lint` / `boundaries:check` (313 modules / 860 deps, unchanged — this session's changes were all `apps/admin` frontend, no backend/API touched) / `test` (**201/201**, unaffected as expected) / `build` (all three apps, zero live services running, confirmed by killing every dev server first).

**Minor housekeeping note, not a code issue:** `woobe_dev`'s orders list carries a few leftover manually-created test orders from earlier sessions (`restock.verify@woobe.internal`, `review.tester@woobe.internal`) that were never cleaned up after their own live-verification passes — didn't touch them (not created by this session, and deleting someone else's diagnostic data without being asked felt presumptuous), but flagging since they'll keep showing up in the admin orders list until someone clears them. This session's own two test orders (`admin-audit-order@test.woobe.internal`, `admin-audit-cancel@test.woobe.internal`) were cleaned up.

**Follow-ups / known gaps:**
- `product_management_staff` still has no functional admin page until Day 7 builds Products/Inventory — the login-redirect fix above makes that honest rather than silently broken, but doesn't resolve it; that's genuinely Day 7's job.
- The admin mobile layout fix is a minimal, functional treatment (horizontal-scroll nav strip), not a polished mobile redesign — appropriate for what's fundamentally an internal staff tool, but revisit if `apps/admin` ever gets a dedicated design pass.
- Everything else already flagged in prior entries (review photos/media admin UI deferred to Day 7, no rate limiting, `MoveWishlistItemToCartUseCase`'s non-atomic step) remains unchanged.

---

## 2026-08-28 — Follow-up: "is every admin page, every viewport, actually responsive?" — two more real bugs found, one of them caused by the previous entry's own fix

**Branch:** `dev1` — uncommitted, per this session's established process.

**Context:** the user asked directly whether every `apps/admin` page is responsive at every viewport, after the prior entry's fixes. Rather than assert yes from the checks already done, went back and systematically opened every distinct page/state (login, orders list populated, orders list empty/filtered, order detail in every actionable status, the inline cancel-reason form, the inline ship-tracking form, the 403 error state for a role with no permissions) across all three staff roles and all four required breakpoints (375/768/1024/1440), checking `document.documentElement.scrollWidth === window.innerWidth` plus a screenshot at each combination — not just re-asserting the previous pass's spot checks.

**Two more real bugs found, both fixed:**

1. **The previous entry's own `overflow-x-hidden` fix was hiding a genuine layout bug instead of preventing one.** Opening the "Cancel order" form on a `CONFIRMED` order at 375px showed the order-status badge truncated to "confir…" and the Reason input / Confirm button completely unreachable off the right edge — yet `scrollWidth === innerWidth` reported **no** overflow, because the `overflow-x-hidden` added to the dashboard's content wrapper (fixing the missing-viewport-meta bug) was silently *clipping* the overflowing content rather than either preventing it or making it scrollable. Root cause: `OrderStatusActions`' `CONFIRMED`-status branch put both action buttons *and* the revealed cancel-form in one non-wrapping `flex gap-2` row — the `PROCESSING` branch already had the correct structure (buttons in their own row, the ship-form stacked below), the `CONFIRMED` branch was the one place that didn't match it. Fixed the actual layout (`flex-col` wrapper, `flex-wrap` on the button row, matching the working branch) and **removed** the `overflow-x-hidden` band-aid — correctness now lives in each child component, not a wrapper that hides symptoms from the very check used to verify pages are responsive. Re-verified the cancel-form interaction clean (no clipping, no scrollbar) at all four breakpoints.
2. **A second, smaller overflow at exactly 768px** (`scrollWidth: 780` vs `innerWidth: 768`) surfaced only when logged in as `product_management_staff` — the longest of the three role-name strings ("product management staff"). Root cause: `min-w-0` had been added to the `TopBar`'s inner flex item so its user-info span could `truncate`, but the *outer* content column (`<div className="flex flex-1 flex-col">` in `DashboardLayout`, itself a flex item at `md:` and up) had no `min-w-0` of its own — a flex item's default minimum width is its content's intrinsic width, which overrides a descendant's `truncate` before truncation ever gets a chance to apply. Fixed by adding `min-w-0` to that outer column too. Verified clean afterward with the same (worst-case) role at all four breakpoints, plus re-confirmed `order_processing_staff`'s slightly-shorter role string was never actually affected (a real negative-control check, not assumed).

**Why the first bug specifically wasn't caught by the previous entry's own verification:** that pass's `scrollWidth`/`innerWidth` check is only a reliable signal when nothing between the content and the document root is clipping it — the very fix that pass made (`overflow-x-hidden`) broke that precondition for everything checked afterward in the same pass. This is now recorded as a general lesson, not just fixed in place: an `overflow-x-hidden` (or similar clipping) ancestor makes the scrollWidth-equality check give false negatives, so a real audit needs to either avoid such wrappers or specifically hunt for `elementFromPoint`/visual clipping the way the Day 3 `AddressForm` bug was originally found.

**Verification commands, all clean, run for real after both fixes:** `pnpm run typecheck` / `lint` / `boundaries:check` (313/860, unchanged — no backend touched) / `test` (**201/201**) / `build` (all three apps, zero live services, confirmed by killing every dev server first — one stray `tsx watch` process from an earlier step was caught and killed before this run, not left running unnoticed).

**Coverage achieved, stated plainly per the user's own question:** every distinct `apps/admin` page and interactive state — login, orders list (populated and empty/filtered), order detail in `CONFIRMED`/`PROCESSING`/`SHIPPED`/`DELIVERED`/`CANCELLED`, both inline action forms, and the permission-denied state — was checked at 375/768/1024/1440px, across all three staff roles where the role affects what renders. All clean as of this entry. This supersedes the previous entry's narrower claim (which had only spot-checked a subset of these combinations).

**Test orders created for this check** (`viewport-check-1@test.woobe.internal`, `viewport-check-2@test.woobe.internal`) were cleaned up from `woobe_dev` afterward, same as every other live-verification pass this week.

---

## 2026-08-28 — Week 2 Day 5: Coupons & Promotions + Shipping (`week2 (1).md` §9–10)

**Branch:** `dev1` — uncommitted, per this session's established process.

**Schema (`packages/database`):** `CouponProduct`/`CouponCategory` (product/category applicability, `@@unique([couponId, productId/categoryId])`, cascading FKs), `Cart.couponCode String?` (stores only the applied code — never a computed discount amount, same "store the reference, recompute live" rule every other Cart field already follows), `ShippingRule.estimatedDeliveryDaysMin/Max` (both `@default`, admin-configurable per ADR-023, not hardcoded). One migration, hand-written via `prisma migrate diff` (the usual TTY workaround) — again had to manually exclude a spurious `DROP INDEX "products_name_trgm_idx"` (3rd time this exact gotcha has shown up, Day 2/4/5) and folded in a harmless `reviews.updatedAt` default-drop left over from Day 4's migration. Applied to `woobe_dev` and `woobe_test`, pg_trgm index verified intact afterward.

**Coupons module — new, built from Week 1's placeholder:**
- Domain (pure, no I/O): `resolveCouponEligibility` (active/date-range/min-cart-value/usage-limit/per-user-limit checks, each independently rejecting with its own specific reason before product/category filtering — a customer never sees a generic "invalid coupon" when a more specific reason is available; product/category restriction is "matches ANY listed product OR ANY listed category", not a strict intersection, same reading Week 2 Day 1 gave size/color filters) and `calculateCouponDiscount` (percentage rounds down, capped by `maxDiscountPaise`, never exceeds the eligible subtotal itself).
- `CouponLineInput` carries a `variantId` the eligibility function itself never reads — added specifically so checkout can trace which returned `eligibleLines` entry maps back to which actual cart/order line, since two lines can share a `productId` (the same product in two sizes is two separate line items).
- Application: `PreviewCouponUseCase` (non-authoritative — cart's "Apply coupon" action and its own live redisplay) and `RedeemCouponUseCase`, split into `validateAndLock(input, tx)` / `finalize(couponId, userId, orderId, tx)` — split because `CouponRedemption.orderId` is required but the Order row doesn't exist yet at the point a coupon needs validating. `validateAndLock` runs first inside checkout's transaction (locks the Coupon row via raw-SQL `SELECT ... FOR UPDATE` on its unique `code` — the actual concurrency guard, same pattern Inventory already established), re-validates every rule from scratch (never trusts the cart page's earlier preview), and returns the discount; `finalize` runs after the Order row exists, same transaction, same still-held lock (Postgres holds a row lock for the whole transaction, not just one statement).
- **Deliberate scope call, flagged prominently: no admin coupon-management endpoints this week.** Unlike every other Week 2 module (Reviews, Collections, Media), `week2 (1).md` §9 has no "Admin:" subsection at all — the one structural signal in the plan that this is intentional, not an oversight. Coupons are created via `packages/database`'s seed script this week (four demo codes — see below); the full customer-facing validate → apply → checkout-redeem pipeline is real, tested, and fully wired regardless of there being no UI to create new ones yet.

**Cart integration:** `GetCartUseCase.execute(cartId, userId)` now takes the caller's id (needed to check per-user redemption counts) and returns `discountPaise`/`appliedCoupon: { code, isValid, reason }` — always recomputed live from the current coupon preview, never read back from a stored amount (`Cart` only ever stores the code). `ApplyCouponUseCase`/`RemoveCouponUseCase`, `POST`/`DELETE /api/v1/cart/coupon` (both `authGuard` — coupons require a real account, `CouponRedemption.userId` is non-null in the schema). A guest with a leftover applied code (e.g. from before logging out) sees it flagged `isValid: false, reason: "Log in to use a coupon"` rather than a crash or a silent drop.

**Orders/checkout integration — the substantial rework:** `CheckoutUseCase` moved GST calculation, order-item building, and the subtotal/tax/discount totals **inside** the checkout transaction (they weren't before) — the discount amount, needed before tax can be recalculated on the discounted value (§9's own "Calculate discount → Recalculate tax" ordering), only becomes known once the coupon's row lock is actually held. New pure domain function `allocateCouponDiscount` (`orders/domain`, 7 unit tests) splits a coupon's cart-level discount back across the specific eligible order lines, proportional to each line's share of the eligible subtotal, using the largest-remainder method so the allocated amounts always sum to *exactly* the discount (no paise lost or gained to independent per-line rounding — money must reconcile exactly). Tax is then recalculated per line on `lineTotalPaise − allocatedDiscount`, using the *undiscounted* per-unit price to pick the GST slab (a promotional discount doesn't change which price tier a product sits in).

**A real bug I caught and fixed proactively, not user-reported:** `Cart.couponCode` was being left on the row after checkout — neither `markCartConverted` nor `reactivateCart` cleared it. Since `Cart.userId` is unique (one cart row per user, ever) and a `CONVERTED` cart gets reactivated for the user's next shopping session, an applied coupon would have silently carried forward and re-applied itself to a completely unrelated future cart. Fixed by clearing `couponCode: null` in both places; caught before it ever shipped by reasoning through the cart lifecycle while writing the checkout-redemption integration test, then confirmed the stale-carry-forward scenario in the live browser check below (`cart.couponCode` was `null` immediately after the order I placed).

**Shipping module extensions:** `checkPincodeServiceability` (domain, pure) — every well-formed 6-digit pincode is serviceable today; **deliberate scope call**: no approved restricted-area list exists anywhere in `plan.md`/`DECISIONS_PENDING.md`, so this is the seam a real restricted-pincode table would plug into later, not a placeholder fabricating non-serviceable areas nobody approved. `GET /api/v1/shipping/estimate?pincode=` (public, no auth — same reasoning as reviews' public GET) returns serviceability plus the live `ShippingRule`'s delivery-day estimate. Also added `ShippingProviderPort`/`ManualShippingProvider` (validates non-empty tracking number/carrier) and wired it into `ShipOrderUseCase` via a new `ShipmentCreatorPort` — admin's "mark shipped" flow now goes through a real provider abstraction instead of trusting whatever the admin typed with no validation at all.

**Frontend (`apps/web`):** `CouponForm` on the cart page (apply/remove, shows the applied code + an invalid-with-reason state, prompts a guest to log in instead of rendering a form that would just fail) and a discount line on both the cart and checkout order summaries — checkout's own "Place order" button total and its summary card both now subtract `discountPaise`, which they silently didn't before this change existed to matter. A pincode delivery-estimate check on the checkout address form's Pincode field, checked on blur, purely informational (never blocks submission) — shows "Delivers in N–M days" or the serviceability rejection reason.

**Seed data (no admin UI to create coupons yet — see the scope call above):** `WELCOME10` (10% off, once per customer), `FLAT200` (₹200 off a ₹1,000+ cart), `SCARF15` (15% off, restricted to the Silk Scarf product), `ACCESSORIES20` (20% off Accessories, capped at ₹500) — one of each rule shape, so every branch of `resolveCouponEligibility` has a real, seeded code to exercise.

**Verified live** (chrome-devtools-mcp, this session's own dev servers, not asserted from code alone): registered a real account → added 17× Silk Scarf (1,020g, clears the ADR-021 minimum) → applied `SCARF15` on the cart page, saw the correct ₹183.60 discount (15% of ₹1,224) and the total drop to ₹1,090.40 → the checkout page's own summary and "Place order" button showed the same discounted total → filled the shipping address, blurred the Pincode field, saw "Delivers in 3-7 days" render from the live endpoint → placed the order (COD) → confirmation page showed the correctly tax-adjusted final total, ₹1,142.42 → independently verified against the database: `subtotalPaise 122400, discountPaise 18360, taxPaise 5202 (5% GST on the discounted 104040 base), shippingFeePaise 5000, totalPaise 114242` — hand-computed total matches exactly; a `CouponRedemption` row exists linking the coupon, user, and order; `Cart.couponCode` was `null` immediately after (the fix above, confirmed working, not just unit-tested). Test account, order, and cart rows cleaned up from `woobe_dev` afterward.

**Mandatory concurrency test** (per this codebase's established pattern for concurrency-critical features, same bar Day 4's inventory-reservation test set): two customers apply the same `usageLimit: 1` coupon to two different carts and check out simultaneously. Exactly one succeeds (`201`, real discount applied); the other's `validateAndLock` sees the limit already hit inside its own locked transaction and throws, rolling back the **entire** checkout (`422`, not a partial charge or a discount-less order) — confirmed the loser has zero orders and zero inventory reserved, and exactly one `CouponRedemption` row exists for the coupon afterward. Ran clean on the first attempt.

**Test counts:** **252 total** (up from the 201 baseline noted in the prior entry) — 36 test files, all passing. New this session: 14 (`resolveCouponEligibility`) + 6 (`calculateCouponDiscount`) + 4 (`RedeemCouponUseCase`) + 7 (`allocateCouponDiscount`) + 11 (coupons integration: apply/remove, unknown/expired/under-minimum/per-user-limit-exhausted codes, product-restriction, category-restriction, full checkout redemption with a real `CouponRedemption` row, the concurrent-redemption test above, and the multi-line discount-splitting/tax-recalculation test) + 3 (shipping estimate integration: serviceable, malformed pincode, missing query param) = 45 tests written and verified in this session directly; the remaining delta comes from `resolve-shipping.test.ts`'s `checkPincodeServiceability` suite and `ship-order.use-case.test.ts`'s provider-failure test, both added earlier in this same Day 5 session before a context-window compaction (their exact prior baseline wasn't independently re-confirmed by me, but the current full-suite total of 252/252 passing was).

**Verification commands, all clean, run for real:**
- `pnpm -r run typecheck` (all 9 workspace projects, zero errors), `pnpm run lint` (api/web/admin, `--max-warnings=0`, zero warnings), `pnpm --filter @woobe/api run boundaries:check` (**340 modules / 924 dependencies, up from 313/860** — zero violations; `orders` now depends on `coupons`, a new edge, still acyclic).
- `pnpm exec vitest run` (apps/api, against the real `woobe_test` Postgres) — **252/252**, run twice (once before the live-browser pass, once after, to confirm nothing in that pass left residue) — both clean, `woobe_test` row counts confirmed back to baseline both times.
- `pnpm -r run build` (all three apps, dev servers genuinely killed first) — clean.

**Follow-ups / known gaps:**
- No admin UI for coupon creation/management — the deliberate scope call above. Coupons live via the seed script until a future week's admin work covers it (nothing in the current plan names a date).
- `Cart.couponCode` is a single field — only one coupon can be applied at a time, no stacking. Nothing in `week2 (1).md` §9 suggests otherwise, but flagging the interpretation since it wasn't spelled out explicitly.
- `checkPincodeServiceability`'s "every 6-digit pincode is serviceable" is a deliberate placeholder (see above) — revisit the moment a real restricted-area/serviceability list gets approved.
- `ManualShippingProvider` is exactly what its name says — no real courier/tracking-API integration exists or was ever in scope; consistent with COD-first, no-payment-gateway-keys-configured state of the rest of this project.
- Everything flagged in prior entries (review photos/admin media UI deferred to Day 7, no rate limiting, `MoveWishlistItemToCartUseCase`'s non-atomic step) remains unchanged and untouched by this day's work.
- **Next:** Week 2 is now fully built out (Days 0–5). No further day-by-day scope remains in `week2 (1).md` as written — next steps would come from a new planning document.

---

## 2026-08-28 — Follow-up: Day 5's admin-side surface hadn't been checked at all — two real bugs found and fixed

**Branch:** `dev1` — uncommitted, per this session's established process.

**Context:** the user pointed out the Day 5 entry above only verified `apps/web` live, and asked that from now on every change gets checked on **both** `apps/web` and `apps/admin`, at every required viewport (375/768/1024/1440), not just one side. Went back and checked every surface Day 5 actually touches or affects on the admin side, which turned out to be exactly one page — `apps/admin`'s order detail view — since coupons have no admin CRUD this week (the deliberate scope call above) and shipping's only admin-facing change (the `ManualShippingProvider` wiring) doesn't add any new UI.

**Two real bugs found, both fixed:**
1. **Admin's order detail page never showed the coupon discount at all** — `OrderDetail.tsx`'s price breakdown had Subtotal/Shipping/Tax/Total but no Discount row, left over from when `discountPaise` was always 0 (pre-Day-5). With coupons now live, an admin looking at a discounted order would see Subtotal + Shipping + Tax not add up to Total, with zero explanation — looks like a bug to whoever's staffing support. Added a conditional "Coupon discount" row (only rendered when `discountPaise > 0`, same pattern as the storefront's own cart/checkout summaries) — verified live against a real discounted order, the numbers now reconcile exactly.
2. **A real, pre-existing mobile-width bug this fix's own live check exposed**: the same card's "Contact & shipping" `<dl>` used plain `flex justify-between` rows with no gap between label and value; a value long enough to wrap (a realistically long customer email, in this case) ran directly into its own label with no space — `Emailviewport-check-...` — because flex-without-gap only spaces two items that fit on one line. Fixed by adding `gap-3` and right-aligning every value in that block (matching the Address row's existing `text-right`, now applied consistently) plus `break-all` on the email specifically so a very long address can't overflow the card at all. Confirmed clean at 375px afterward, screenshot-verified.

**Verified live** (chrome-devtools-mcp, this session's own dev servers): registered a fresh account on `apps/web`, built a cart that clears the weight minimum, applied `WELCOME10`, checked the cart and checkout pages at all four breakpoints (zero overflow at every one, `scrollWidth <= innerWidth` confirmed programmatically, not just eyeballed) — including the pincode delivery-estimate widget rendering "Delivers in 3-7 days" cleanly at 375px. Placed the order, then switched to the already-logged-in `apps/admin` session (`order_processing_staff`) and opened that exact order's detail page at all four breakpoints — confirmed the new discount row and the fixed Contact & shipping spacing both render correctly at every size, zero horizontal overflow anywhere. Test account, order, and cart cleaned up from `woobe_dev` afterward.

**Verification commands, all clean, run for real after both fixes:** `pnpm -r run typecheck` (9/9 projects), `pnpm --filter @woobe/admin run lint` (`--max-warnings=0`), `pnpm --filter @woobe/api exec vitest run` (**252/252**, unaffected — this pass was frontend-only), `pnpm -r run build` (all three apps, dev servers killed first).

**Standing process change, going forward:** every future change gets a live responsiveness check on **both** `apps/web` and `apps/admin`, at all four breakpoints, not just whichever side the change's own code lives in — the discount-row gap above is exactly the failure mode a web-only check misses (the backend and web frontend were both genuinely correct; only admin's *display* of the same data was stale).

---

## 2026-08-28 — Week 2 Day 6: Returns & Refunds (`week2 (1).md` §11–12)

**Branch:** `dev1` — uncommitted, per this session's established process.

**Scope-conflict flag, resolved the same way every prior day has:** `plan.md` §3 says the full customer-initiated return-request flow "stays Week 4" (only the admin-cancellation refund path was meant to pull forward). But `week2 (1).md` §27's own "Recommended Execution Order" explicitly lists "Day 6 — Returns + Refunds" as this week's own scope, the same document every prior day this week has treated as authoritative — and the `Return`/`ReturnItem`/`Refund` schema plus `Order.hasActiveReturn` were already sitting in `schema.prisma`, clearly pre-provisioned for exactly this. Built it out per `week2 (1).md`; updated the stale "Week 4"/"placeholder" doc comments on `returns.module.ts` and `refunds.module.ts` to record the correction rather than leaving them contradicting the code next to them.

**Returns module — new, built out from Week 1's placeholder:**
- Domain (pure, no I/O): `resolveReturnEligibility` (order must be DELIVERED, request must fall inside a return window, per-line quantity can't exceed what's still unclaimed — "quantity validation" and "duplicate prevention" turn out to be the exact same arithmetic: an item's remaining returnable quantity already accounts for every prior request against it, a REJECTED one freeing its quantity back up, everything else — pending, approved, in-flight, or already REFUNDED — staying permanently claimed) and `calculateReturnRefundAmount` (returned units' own price plus their prorated share of that line's snapshot tax; deliberately excludes the shipping fee — nothing in §10/§12 says a return refunds shipping, flagged as the interpretation, not an oversight).
- **New pending decision, same established pattern as the ₹50 shipping fee/GST rate/₹/kg rate**: no return-window policy exists anywhere in the approved docs, so `RETURN_WINDOW_DAYS = 7` (a common apparel-return-window default) is a documented placeholder — added as `DECISIONS_PENDING.md` #5.
- Application: `RequestReturnUseCase` (customer), `ApproveReturnUseCase`/`RejectReturnUseCase` (admin — kept as genuinely separate transitions from issuing a refund, not folded together: plan.md §4's own finalized state machine keeps `RETURN_APPROVED` and `REFUND_INITIATED` as distinct states, and §11's own workflow diagram puts "Pickup/receipt where applicable" between them), `IssueRefundForApprovedReturnUseCase` (computes the refund amount from the order's own line items, advances `RETURN_APPROVED → REFUND_INITIATED` unconditionally, then attempts the actual refund — only success advances further to `REFUNDED`), and `MarkReturnRefundedUseCase` (the manual-completion path for a COD return or a resolved gateway failure — mirrors the existing "Refund needs manual follow-up" pattern from Day 0's cancellation flow, but modeled as an explicit state transition instead of a dangling UI flag). `Order.hasActiveReturn` (schema's own "denormalized for admin filtering only" comment) is kept in sync through a new narrow `orders`-owned use-case (`SetOrderHasActiveReturnUseCase`) — `returns` never writes to the Order table directly (ADR-010).
- No cross-module import cycle despite `returns` depending on BOTH `orders` and `refunds` directly (unlike the cancellation flow, which needed composing one level up in `admin` to dodge a real cycle) — nothing imports `returns` back, so this is a plain acyclic edge, confirmed by `boundaries:check`'s own `no-circular` rule.

**Refunds module extension:** new `IssueRefundForReturnUseCase` (idempotent per `returnId`, not `orderId` — an order can have several returns over its lifetime, each needing its own refund, unlike cancellation's one-per-order). A COD order (no captured Razorpay payment to call) still records an `INITIATED` Refund row rather than doing nothing, since a return's own state machine needs *something* for `REFUND_INITIATED` to point at while it waits for manual completion. `RefundRepositoryPort` gained `findByReturnId`/`markCompletedByReturnId` alongside the existing order-scoped methods.

**Admin surface:** `GET/POST /admin/returns/*`, gated by the existing `MANAGE_ORDERS` permission (its own doc comment already named "returns/refunds" as in scope) — list/detail/approve/reject/refund/mark-refunded, composed in `admin.module.ts` the same way reviews/collections already are. Every admin action now writes to the audit log (a new `AuditLoggerPort` on `returns` itself, not composed one level up — unlike cancellation, `returns` has no cycle to dodge, so this follows orders' own ship/deliver/start-processing pattern of logging directly inside the use-case, not the special-cased admin-composition pattern cancellation needed). Rejection accepts an optional reason that lands in the audit log only — the approved `Return` schema has no rejection-reason column, and inventing one wasn't this module's call to make.

**Frontend — new on both sides:**
- `apps/web`: a genuine customer order-detail page (`/account/orders/[id]`, distinct from `/order-confirmation/[id]`'s one-time post-checkout role) showing items, tracking, this order's own returns with status, and a return-request form once `DELIVERED`. Quantity pickers are capped at the *ordered* quantity, not the actually-remaining-returnable quantity — a deliberate, flagged simplification (computing the latter client-side would need every existing return's own line items); the server's real eligibility check is what actually enforces the limit, same "never trust the client, the server is the real check" posture this whole codebase already follows. The UI also only allows one return in flight per order at a time (the API itself is more granular, allowing a separate return per line even while another is pending) — also flagged as a UI-level, not API-level, simplification.
- `apps/admin`: a full Returns section — list (filterable by status) + detail page with the approve/reject/refund/mark-refunded actions, following `OrderStatusActions`' now-corrected `flex-col`/`flex-wrap` layout pattern from the start (the Day 5 audit's own lesson applied proactively, not found-and-fixed after the fact). The "Returns" nav entry flips from `coming-soon` to `live` in `nav-config.ts`.

**Verified live** (chrome-devtools-mcp, this session's own dev servers, checked on **both** `apps/web` and `apps/admin` at all four breakpoints per the standing process change from the entry above — not just one side): registered a customer, placed a COD order, drove it `CONFIRMED → PROCESSING → SHIPPED → DELIVERED` via the real admin UI, requested a return for 1 of 2 units from the web order-detail page (zero overflow at 375/768/1024px, including the item/quantity-picker row), watched it appear on admin's Returns list and detail page, approved it, issued the refund (correctly reported "Cash on Delivery — no gateway refund to process," matching the COD path), and confirmed it manually — verified in the database afterward that `Return.status` reached `REFUNDED`, `Order.hasActiveReturn` flipped back to `false`, and the `Refund` row's `amountPaise` (78,120 — one unit's ₹744.00 price plus its exact prorated half of the line's ₹74.40 tax) matched the hand-computed value exactly. Reloaded the web order-detail page afterward and confirmed it showed "refunded" and correctly re-offered the request-return form for the still-unreturned second unit. Test account/order/return/refund rows cleaned up from `woobe_dev` afterward.

**Test counts:** **298/298** passing (up from 252) — 43 test files. New this session: 13 (`resolveReturnEligibility`) + 5 (`calculateReturnRefundAmount`) + 3 (`RequestReturnUseCase`) + 5 (Approve/RejectReturnUseCase) + 6 (`IssueRefundForApprovedReturnUseCase`) + 3 (`MarkReturnRefundedUseCase`) + 11 (returns integration — eligibility rejections, ownership 404, the full COD approve→refund→mark-refunded lifecycle with duplicate-refund prevention, reject + re-request, the Razorpay-gateway-failure → manual-recovery path, and RBAC) = 46, plus 2 pre-existing tests updated for the extended `RefundRepositoryPort` mock shape.

**Verification commands, all clean, run for real:**
- `pnpm -r run typecheck` (9/9 workspace projects), `pnpm run lint` (api/web/admin, `--max-warnings=0`), `pnpm --filter @woobe/api run boundaries:check` (**372 modules / 1,056 dependencies, up from 340/924** — zero violations, confirmed acyclic despite `returns` importing both `orders` and `refunds`).
- `pnpm exec vitest run` (apps/api, against the real `woobe_test` Postgres) — **298/298**, `woobe_test` row counts confirmed back to baseline afterward.
- `pnpm -r run build` (all three apps, dev servers genuinely killed first) — clean; new routes (`/account/orders/[id]` on web, `/returns` + `/returns/[id]` on admin) compile.

**Follow-ups / known gaps:**
- Exchanges (plan.md §4's own note: "Exchanges follow the same Return sub-flow but resolve to a new Order linked via `exchangeOfOrderId` instead of a Refund") are not built — nothing in `week2 (1).md` §11/§12's own bullet list asks for them this week, and `Order.exchangeOfOrderId` already exists in the schema for whenever that lands.
- The web order-detail page's one-return-at-a-time-per-order UI simplification and its ordered-quantity (not remaining-quantity) picker cap are both flagged above — real gaps in the frontend's own convenience, not in the API's correctness.
- No pickup/courier-scheduling step exists for a physical return shipment back to the warehouse — `week2 (1).md` §11's own workflow diagram shows "Pickup/receipt where applicable" as a step with no corresponding state or field in the approved `Return` schema, so nothing was invented for it; the gap between `RETURN_APPROVED` and issuing the refund is exactly where that would slot in later.
- Everything flagged in prior entries (no coupon admin CRUD, no restricted-pincode list, review photos/admin media UI deferred, no rate limiting) remains unchanged and untouched by this day's work.
- **Next:** `week2 (1).md` §27's Day 7 — Admin Expansion (Products, Inventory, Orders, Customers).

---

## 2026-08-28 — Week 2 Day 7 (Part 1): Admin Products, Inventory & Collections UI (`week2 (1).md` §14–16)

**Branch:** `dev1` — uncommitted, per this session's established process.

**Scope split, flagged up front:** Day 7 as written is four sub-areas (Modules 14–17: Products, Inventory, Orders, Customers) — too much for one pass at this session's usual rigor (full tests + live multi-viewport verification on both apps). Did Products + Inventory first, since they're tightly coupled (a new variant always needs an inventory row), and folded in Collections' own admin UI alongside them — its full CRUD/assign/reorder API has been sitting done and tested since Day 2, explicitly deferred to "Day 7" by that module's own composition-root comment (not a new decision, just finally cashing in a debt this week already owed). Customers and Admin Orders' small return/refund-state surfacing remain for a follow-up pass.

**A real mistake caught and fixed before it shipped:** while adding the new admin product-management Zod schemas, a `Write` call on `packages/validation/src/products.schema.ts` overwrote the file instead of appending — silently deleting the existing customer-facing `productListQuerySchema`/`ProductSort`/`commaSeparatedList`/`booleanFlag` that the whole storefront listing page and `ListProductsUseCase` depend on. Caught immediately by reviewing `git diff --stat` before moving on (65 deletions where I expected only insertions), not by a later test failure — re-wrote the file as a proper merge (original content preserved verbatim, new admin schemas appended below), then confirmed with a clean diff showing only additions. Recorded here because it's exactly the kind of self-inflicted regression a "the tests still pass" check alone wouldn't have caught fast (the customer product-listing integration tests would have failed, but only much later in the verification pass) — checking the diff *before* moving to the next file is now the standing habit for every `Write` on a file that isn't brand new.

**Schema additions (nullable, additive, no destructive migration):** `Product.metaTitle`/`metaDescription` (week2 (1).md §16's "SEO metadata" admin operation — data-entry only this week, same "schema now, rendering later" pattern coupons' schema followed for Day 5; Module 19/SEO, a later day, is what actually renders these) and `ProductVariant.fabric`/`fit`/`measurements` (plain nullable text — no structure was ever specified for these beyond their names, so nothing invented beyond that). The 4th recurrence of the `products_name_trgm_idx` spurious-drop migration gotcha (Day 2/4/5, now Day 7) — excluded the same way as always.

**Admin Products (new):** full CRUD (create/edit/deactivate), variant management (SKU/color/size/weight/rate-override/fabric/fit/measurements, create/edit/deactivate), and media (upload via Week 2 Day 4's own `POST /media` — its first real consumer, exactly the gap that day's journal entry flagged — then attach/reorder/remove as a `ProductImage`). A new variant always gets its `effectivePricePaiseCache` computed live through the same `PricingReaderPort` every customer-facing price already goes through (never hand-entered) and a real `Inventory` row via a new `InitializeInventoryForVariantUseCase` (`inventory` module, exported for `products` to call — no direct Prisma access across the boundary). `Product.minPricePaiseCache` — the cache the customer listing's price/sort already reads — is recomputed after every variant create/update/activation-change that could move it.

**Admin Inventory (new):** a dashboard (available/reserved/sellable, low/out-of-stock indicators — `LOW_STOCK_THRESHOLD = 5`, a new documented placeholder, `DECISIONS_PENDING.md` #6, no confirmed policy exists) and manual stock adjustment. Validation (`validateInventoryAdjustment`, pure domain function) enforces the plan's own "never negative" rule two ways: available stock can't go below zero, and can't drop below what's already reserved either — every other inventory operation in this codebase already assumes `available - reserved` is the real sellable pool and never negative. Every adjustment attempt that's rejected writes nothing and logs nothing; every one that succeeds writes an `AdminAuditLog` row (who, what delta, why) — satisfies §15's "authorized, validated, transaction-safe, auditable" bar plainly, not just partially.

**Admin Collections UI (new, backend unchanged since Day 2):** list/create/edit/activate, a search-based product picker (reuses the new admin products list endpoint), and an assigned-products list with remove + move-left/right reordering — plain buttons, not drag-and-drop (no drag library exists in this codebase yet, and a collection's product count is small enough that this stays a one-click operation either way).

**Verified live** (chrome-devtools-mcp, this session's own dev servers, both apps at 375/768/1024/1440px): created a real product end-to-end from `apps/admin` — name/slug/category/SEO fields, a variant (confirmed its price and the product's own `minPricePaiseCache` matched the live rate exactly: 350g × ₹1,200/kg = ₹420.00), a real image upload through the actual media pipeline (not a placeholder URL) — then confirmed the SAME product, image, and price rendered correctly on the **customer-facing** `apps/web` listing and product-detail pages, proving the admin-created data flows through the exact same read paths real catalogue data does. On Inventory: tried an invalid -100 adjustment (rejected, 422, zero audit rows written), then a valid -15 (accepted, row updated to 5/0/5, exactly one `INVENTORY_ADJUSTED` audit row, "low stock" badge appeared, and the "Low stock only" filter correctly isolated it). On Collections: searched for and assigned the test product to "New Drops," moved it up one position (confirmed the `sortOrder` persisted in the database), then removed it, leaving the collection exactly as it started. Also confirmed RBAC live both ways — `product_management_staff` sees Products/Inventory/Collections in the nav and gets a real "no permission" message on Orders; `order_processing_staff` sees only Orders/Returns, the new sections don't even render in its sidebar. Test product, its uploaded file, and its `Media`/audit-log rows all cleaned up from `woobe_dev` afterward.

**Test counts:** **329/329** passing (up from 298) — 47 test files. New this session: 11 (`validateInventoryAdjustment`/`isLowStock`/`isOutOfStock`) + 2 (`AdjustInventoryUseCase`) + 18 (admin-products integration: RBAC, CRUD, slug-conflict, deactivation removing a product from the customer listing, variant creation/price-cache recompute/inventory initialization, excluding a deactivated variant from `minPrice`, image attach/reorder/remove) + 7 (admin-inventory integration: RBAC, low-stock filter, restock + audit log, the two never-negative rejections, zero-delta rejection) = 38.

**Verification commands, all clean, run for real:**
- `pnpm -r run typecheck` (9/9 workspace projects), `pnpm run lint` (api/web/admin, `--max-warnings=0`), `pnpm --filter @woobe/api run boundaries:check` (**398 modules / 1,160 dependencies, up from 372/1,056** — zero violations).
- `pnpm exec vitest run` (apps/api, against the real `woobe_test` Postgres) — **329/329**; a single flaky failure appeared once during a full-suite run (an unrelated, pre-existing `admin.integration.test.ts` checkout call got a transient 422) and did not reproduce across two immediate re-runs or when isolated with the new files — logged here as observed-and-investigated, not silently ignored, but not attributable to this session's own code.
- `pnpm -r run build` (all three apps, dev servers genuinely killed first) — clean; new routes (`/products`, `/products/new`, `/products/[id]`, `/inventory`, `/collections`, `/collections/new`, `/collections/[id]` on admin) all compile.

**Follow-ups / known gaps:**
- Modules 16 (the small return/refund-state addition to Admin Orders — most of Admin Orders already exists from Day 0/this week) and 17 (Admin Customers, entirely new) remain — the rest of Day 7.
- `fabric`/`fit`/`measurements` are plain free-text fields, not structured data (e.g. no separate bust/waist/hip measurement columns) — nothing in the approved docs specifies a structure beyond the field names.
- The admin image reorder UI is move-left/move-right, not drag-and-drop — a deliberate, low-effort choice given the small image counts involved, flagged above.
- No admin category CRUD — categories aren't part of week2 (1).md §16's own operations list this week (the product form's category field is a dropdown over the existing, seed-fixed 5 categories via the already-public `GET /api/v1/categories`).
- Everything flagged in prior entries (coupons/returns scope calls, no restricted-pincode list, no rate limiting) remains unchanged and untouched by this day's work.
- **Next:** finish Day 7 — Admin Customers (new) and Admin Orders' return/refund-state display (small addition to existing views).

---

## 2026-08-28 — Week 2 Day 7 (Part 2): Admin Customers + Orders return-state display (`week2 (1).md` §17, §16's leftover)

**Branch:** `dev1` — uncommitted, per this session's established process.

**Scope call, flagged:** `MANAGE_CUSTOMERS` (new RBAC permission) is granted to `super_admin` only. Neither staff role's own quotation description in plan.md §3 mentions customer-account access, and this is exactly the kind of PII-adjacent surface (email, phone, order history, address book) not worth quietly widening an existing staff grant for without it being asked for.

**Admin Customers (new):** list (search by name/email, filter by active/deactivated) + detail (contact info, "basic activity," full order list, saved addresses, activate/deactivate). Scoped to `role: CUSTOMER` throughout — a staff/admin account id 404s here the same way an unknown id does, both for the list search and the detail lookup; this is not how a staff account gets managed.
- **"Basic activity"** is interpreted minimally, per §19's own wording: order count, total spent (sum of `totalPaise` across every order including cancelled ones — this is "activity," not a revenue figure that would need to exclude them), and most-recent order date — all three derived from the customer's own already-fetched order list, not a separate aggregate query.
- **Cross-module composition, reusing an established pattern:** the detail view needs `auth` (the customer record), `orders` (their order history), and `users` (their addresses) all in one response. `users` already imports `auth` (its profile-edit endpoint reuses `updateUserProfileUseCase`), so an `auth -> users` edge back would close a cycle — composed instead in `admin`, which sits above all three and is imported by nothing, same reasoning `CancelOrderWithRefundUseCase` (Day 0) already established for exactly this shape of problem. `orders.module.ts` and `users.module.ts` each export one more already-existing use-case (`listMyOrdersUseCase`, `listAddressesUseCase`) for this same composition — no new business logic in either module, no ownership gate baked into either export (the caller, `admin`, is what's trusted to pass the right `userId`).
- **"Account status" already has real teeth without adding any new enforcement**, discovered rather than built: `LoginUserUseCase` and `RefreshTokenUseCase` both already check `isActive` (Week 1) — flipping the flag here means a deactivated customer can't log in again and their next refresh-rotation fails on its own; only their current short-lived access token keeps working until it naturally expires. Verified this live, not just asserted from reading the code (see below).

**A real bug caught only by live browser verification, not by any test:** the customer-detail page's "Joined" field rendered `Invalid Date`. Root cause: `GetCustomerForAdminUseCase` and `SetCustomerActiveUseCase` both originally returned the bare `UserEntity` (id/email/phone/name/role/isActive — no `createdAt`, by design, since nothing else in `auth` needs it), while the *list* endpoint already had its own richer `CustomerSummary` shape (same fields plus `createdAt`) built for exactly this admin surface. The frontend's single `AdminCustomerDetail` type assumed both endpoints returned the same shape; they didn't. Fixed by making the admin-only customer lookup and the activate/deactivate mutation both return `CustomerSummary` throughout (`AuthRepositoryPort` gained `findCustomerSummaryById`, `setUserActive`'s return type changed from `UserEntity` to `CustomerSummary`) — `UserEntity` itself is untouched, since login/register/etc. never needed `createdAt` and shouldn't gain a field they don't use. Added a regression assertion directly in the integration test (`new Date(res.body.customer.createdAt)` must not be `Invalid Date`, on both the detail fetch and the deactivate response) — this is the kind of gap an integration test can close permanently once it's been seen once, which automated coverage alone hadn't caught before it reached the browser.

**Admin Orders — return/refund-state display (the last of §16's own leftover from Part 1):** `OrderEntity` gained `hasActiveReturn: boolean` (a read-only passthrough of the column that already exists and is already kept in sync by `returns`' own `SetOrderHasActiveReturnUseCase` from Day 6 — no new write path). The admin order-detail page now shows a "return requested" badge next to the status badge whenever it's true, linking to `/returns?orderId=<id>` — a small, targeted extension to the existing returns admin queue rather than a new page: `ListReturnsFilter`/`listReturnsQuerySchema` both gained an optional `orderId`, mirroring the customer-facing `listMyReturnsQuerySchema`'s own `orderId` narrowing from Day 6. The returns list page reads it from the URL via `useSearchParams` (wrapped in a `Suspense` boundary — Next.js 15 requires one for any client component that calls it, confirmed by the production build failing without it and passing with it) and shows "Showing returns for this order only" when present.

**Verified live** (chrome-devtools-mcp, this session's own dev servers, both apps at 375/768/1024/1440px): registered a customer, added an address, confirmed both list and detail render correctly on `apps/admin` at every breakpoint (caught and fixed the `Invalid Date` bug here, live, before it was known from reading code alone) — toggled deactivate/reactivate and watched the badge and button flip in place. Separately placed a real COD order as that customer, drove it to `DELIVERED` via the admin UI, requested a return on one line, and confirmed the order-detail page's new "return requested" badge appeared, wrapped correctly at 375px alongside the status badge (`flex-wrap` on that row), and its link correctly narrowed the returns queue to that one order (confirmed both the "showing returns for this order only" copy and that exactly one row appeared). Confirmed RBAC live: `order_processing_staff`'s sidebar shows no "Customers" entry at all (nav is permission-filtered) and the API 403s the endpoint directly. Test customer, order, return, and address rows deleted from `woobe_dev` afterward; the two variants' `quantityAvailable` restored to their pre-purchase values.

**Test counts:** **341/341** passing (up from 329) — 49 test files. New this session: 3 (`GetCustomerDetailUseCase` — activity derivation, zero-orders case, error propagation without calling the order/address readers) + 9 (`admin-customers` integration — RBAC, list+search never returning staff accounts, detail with real orders/addresses/activity, 404 for a staff id, 404 for an unknown id, deactivate blocking login with `createdAt` regression guard, reactivate restoring it) = 12; plus 2 new assertions added to an existing Day 6 returns-integration test (admin order-detail's `hasActiveReturn` and the new `orderId` returns-queue filter) rather than new `it()` blocks, so the count there is unchanged. Five pre-existing order-lifecycle unit tests (`cancel-order-with-refund`, `cancel-order`, `deliver-order`, `ship-order`, `start-processing-order`) needed their `OrderEntity` test fixtures updated for the new `hasActiveReturn` field — a type-level ripple from `OrderEntity`, not a behavior change.

**Verification commands, all clean, run for real:**
- `pnpm -r run typecheck` (9/9 workspace projects), `pnpm run lint` (api/web/admin, `--max-warnings=0`), `pnpm --filter @woobe/api run boundaries:check` (**406 modules / 1,193 dependencies, up from 398/1,160** — zero violations; `admin` importing a type from `auth`'s own `application/ports` for the first time added no new edge, since `admin` already imports several of `auth`'s use-cases).
- `pnpm exec vitest run` (apps/api, against the real `woobe_test` Postgres) — **341/341**; `woobe_test` row counts confirmed back to baseline afterward.
- `pnpm -r run build` (all three apps, dev servers genuinely killed first) — clean; new routes (`/customers`, `/customers/[id]` on admin) compile, and `/returns`'s new `useSearchParams` usage compiles as a static page thanks to its `Suspense` wrapper.

**Follow-ups / known gaps:**
- Week 2 Day 7 (all four sub-areas — Products, Inventory, Orders, Customers) is now fully done across both this entry and Part 1's.
- Everything flagged in prior entries (coupons/returns scope calls, `fabric`/`fit`/`measurements` as free text, no admin category CRUD, no restricted-pincode list, no rate limiting) remains unchanged and untouched by this day's work.
- **Next:** `week2 (1).md` §27's Day 8 (not yet scoped in this session).

---

## 2026-08-28 — Follow-up: admin responsiveness re-sweep finds and fixes a real Inventory bug

Asked whether the admin side is responsive at every viewport. Rather than re-assert the earlier spot-checks, did a fresh full sweep of every admin page (Orders list/detail, Products list/new/detail with variants+images, Inventory, Collections list/new/detail with the product picker, Customers list/detail, Returns) at 375/768/1024/1440 — including expanding forms and interacting with them, not just loading pages and looking.

**Real bug found and fixed:** Inventory's inline "Adjust" row broke at 375px once the Reason field had realistic content typed into it — the browser's normal caret-follow-on-type behavior scrolled the table's own horizontal container far enough right that the Adjustment field, Reason field, and Confirm/Cancel buttons all disappeared off-screen simultaneously, leaving no visible way to finish or cancel the action. Root cause: this table's `flex-wrap` on the expanded row's form never actually triggers, because the flex container's available width is the *table's* real rendered width (≥720px, from `min-w-[720px]`), not the visible viewport — a `flex-wrap` div can't wrap around edges it never sees as constrained.

**First fix attempt failed, caught before it shipped:** tried pinning the expanded `<td>` with `position: sticky; left: 0` so it would stay visible regardless of the table's scroll position. Measured it directly (`getBoundingClientRect()` before and after scrolling the container programmatically) rather than trusting a screenshot, and confirmed sticky was a complete no-op — moving the container's `scrollLeft` moved the "sticky" cell exactly as far as a plain, non-sticky one would. This is a known real CSS limitation: `position: sticky` doesn't work on table cells when the table has `border-collapse: collapse`, which this table does.

**Actual fix:** two changes, verified independently: (1) an explicit `max-w-[320px] sm:max-w-none` on the form's own flex container — a width tied to the viewport instead of the table, so `flex-wrap` has something real to wrap around below the `sm` breakpoint, while staying single-line on every wider viewport where the table already has room; (2) `startAdjusting` now resets the table's own scroll container to `0` when a row is expanded, since a user could just as easily open "Adjust" from an already-scrolled-right position (the row's own "Adjust" button lives in the table's rightmost column) independent of the typing-caret issue. Confirmed via direct DOM measurement (not just a screenshot) that after the fix, `document.body.scrollWidth` stays equal to `window.innerWidth` at 375px even with a long reason typed in, and that opening the form from a deliberately-scrolled-right position lands it fully visible.

**Verified:** `pnpm --filter @woobe/admin run typecheck`/`lint` clean, `pnpm --filter @woobe/admin run build` clean (all 16 routes), and a live re-check confirming the wider breakpoints (768/1024/1440) were unaffected — `sm:max-w-none` correctly hands the form back its previous single-line layout there, matching how the rest of this table's own columns already behave at those widths (a horizontal scroll to reach the rightmost columns, an established, consistent pattern across every table in this app — not something this fix needed to change).

Everything else checked in this sweep (Orders, Products' variant/image editors, Collections' product picker/reorder, Customers, the login page) was already correct at every breakpoint — this was the one real gap, and it's now fixed and verified, not just reasserted.

---

## 2026-08-28 — Follow-up: DIP inconsistency between `admin`'s two composition-root use-cases

Asked to review this week's work for bugs and SOLID adherence. No open functional bugs found (the one real bug this session hit — Day 7 Part 2's `Invalid Date` — was already fixed with a regression test). One real, worth-fixing inconsistency: `CancelOrderWithRefundUseCase` (Day 0) and `GetCustomerDetailUseCase` (Day 7 Part 2) both compose across modules from `admin` to dodge a cycle — the exact same architectural situation — but with different DIP postures. `CancelOrderWithRefundUseCase`'s constructor was typed against the concrete `CancelOrderUseCase`/`IssueRefundForCancelledOrderUseCase`/`RecordAuditLogUseCase` classes, with its own doc comment arguing "no new port interfaces are needed." `GetCustomerDetailUseCase` instead depends on narrow `execute`-shaped interfaces.

Checking the existing unit test exposed that the "depends on concrete classes" claim was already a fiction at the one place it mattered: every mock in `cancel-order-with-refund.use-case.test.ts` was a plain `{ execute: vi.fn() }` object forced through an `as unknown as ConcreteClass` cast — i.e., the real DI contract was already structural, just hidden behind a type assertion.

**Fix:** gave `CancelOrderWithRefundUseCase` three private, unexported interfaces (`OrderCanceller`, `CancelledOrderRefundIssuer`, `AuditLogger`) matching exactly the one method each collaborator calls, and retyped the constructor against those instead of the concrete classes — same pattern `GetCustomerDetailUseCase` already uses, so both `admin`-composition use-cases are now consistent. `admin.module.ts`'s wiring is untouched — it still passes the same real singletons, which satisfy the interfaces structurally — so this is a type-level-only change with zero behavior difference. The test file's three `as unknown as ...` casts came out as a direct consequence (the plain mock objects now satisfy the narrower interfaces on their own), which is a small, real readability win, not just a side effect.

**Verified:** `pnpm -r run typecheck` (9/9 clean), `pnpm --filter @woobe/api run lint` (clean), `pnpm --filter @woobe/api run boundaries:check` (**406 modules / 1,188 dependencies, down from 1,193** — the drop is the two concrete-class edges this removed, replaced by imports of `orders`' and `refunds`'/`audit`'s own port/result types instead, which is exactly the abstraction seam DIP asks for), full suite **341/341** unchanged, and the real end-to-end HTTP cancellation flow in `admin.integration.test.ts` re-run in isolation to confirm the actual composed singletons still work through the new interface types, not just the unit test's mocks.

---

## 2026-08-28/29 — Week 2 Day 8 (Part 1): Notifications (`week2 (1).md` §20) — Homepage/Bundles deferred for now

**Branch:** `dev1` — uncommitted, per this session's established process.

**Scope split, flagged up front:** Day 8 as written is Homepage Expansion (Module 12) + Build Your Look/Bundles (Module 13) + Notifications (Module 18) — three genuinely different-shaped pieces of work. Did Notifications first: it's real, novel infrastructure (a BullMQ-backed queue/worker, not yet wired anywhere in this codebase), and Homepage Expansion mostly consumes it and other already-built modules for display, so building the producer before the consumer is the natural order. Homepage Expansion is the next pass.

**Module 13 (Build Your Look/Bundles) — deferred, not built.** Its own spec text is explicit: "Implement only if approved requirements and data exist." Neither does — no `Bundle`-shaped model anywhere in `schema.prisma`, no approved "which products are compatible with which" rule, nothing to recommend from. Building a fabrication here (invented compatibility rules, invented bundle pricing logic) would violate this exact module's own stated gate, so nothing was built — same posture as Day 6's deferred Exchanges (`exchangeOfOrderId` sits unused in the schema for the same reason).

**A real doc-vs-schema mismatch, resolved the schema's way (same call this session has made every time it's come up):** §20's own text lists "Email / SMS / WhatsApp" as possible channels, but the already-migrated `NotificationChannel` enum has `EMAIL / SMS / PUSH` — no `WHATSAPP` value exists to write. Treated the schema as authoritative (altering an already-migrated enum for a channel nothing this week actually sends through is exactly the kind of unforced schema churn this session avoids elsewhere); only `EMAIL` is actually wired.

**A real design decision, not obvious from the module list:** §20 lists "Order confirmed" and "Payment successful" as two separate possible events. For this system they're the same instant — both `ConfirmOrderUseCase` (COD) and the Razorpay `payment.captured` webhook path land on the exact same state transition at the exact same moment — so sending two near-simultaneous "your order/payment succeeded" messages would be invented spam on top of the spec, not something it actually asks for. Folded into one `ORDER_CONFIRMED` event. Six events wired in total: `ORDER_CONFIRMED`, `PAYMENT_FAILED`, `ORDER_SHIPPED`, `ORDER_DELIVERED`, `RETURN_APPROVED`, `REFUND_PROCESSED`.

**Architecture (week2 (1).md §20's own diagram: API -> domain event -> BullMQ -> worker -> Provider), built from scratch:**
- New `notifications` module, built out from the Week 1 placeholder (`bullmq` added as a new dependency — nothing in this repo used a job queue before). Leaf module, zero dependencies on any other module (same posture `audit` already documents for itself) — every caller defines its own local `NotificationEnqueuerPort` and adapts it to the real `enqueueNotificationUseCase`, exactly mirroring how `AuditLoggerPort` already wires to `recordAuditLogUseCase` everywhere.
- `EnqueueNotificationUseCase`: persists a `Notification` row (status `PENDING`), then adds a BullMQ job carrying only that row's own id — never the payload itself (the worker always re-reads from Postgres before sending; Redis stays disposable per ADR-017, "never the source of truth"). Called by every trigger site AFTER its own transaction has committed, same "external side effect only once the real state change is durable" posture this codebase already uses for refund issuance.
- `ProcessNotificationJobUseCase` (the actual per-job work): idempotent by construction — an already-`SENT` or already-terminally-`FAILED` notification is left alone, not re-sent, since BullMQ's own "at least once" delivery means this can genuinely run twice for the same row. Deliberately has zero awareness of BullMQ's own retry/attempts machinery — it just attempts once and re-throws on failure, leaving "was this the last attempt" entirely to the worker (infrastructure), keeping the application layer framework-agnostic (ARCHITECTURE.md §3.1).
- `StubEmailProvider`: no email/SMS/WhatsApp credentials exist anywhere in this repo (checked `.env`/`.env.example` before writing this — `DECISIONS_PENDING.md` #7 records it). Unlike `RazorpayRefundService`'s own stub (which always fails, since that gateway call has no meaningful success without real keys), this one succeeds whenever there's a real `contactEmail` to send to — the point of this module is to prove out the whole pipeline (retry, idempotency, failure handling), not gate that proof behind credentials nobody has approved yet. A genuinely missing `contactEmail` is the one case it legitimately, permanently fails on, via a new `NotificationDeliveryError(message, retryable)` — `retryable: false` for that case.
- `worker.ts`: a genuinely separate process from `server.ts` (`pnpm --filter @woobe/api run worker:dev`/`run worker`), not a background task inside the API's own event loop — satisfies "no unnecessary blocking of checkout/order requests" by construction, not by care taken inside a shared process. Converts a non-retryable `NotificationDeliveryError` into BullMQ's own `UnrecoverableError` (the one BullMQ-specific concept the worker — not the use-case — is allowed to know about), stopping retries immediately instead of burning through all 3 configured attempts on a failure retrying can never fix. Everything else retries via BullMQ's own exponential backoff (5s/10s/20s); only once a job's own last attempt fails does the worker call a small `MarkNotificationFailedUseCase` to record the terminal state.
- **DEVELOPMENT_RULES.md #8 ("no ad-hoc `console.log` ... until the real structured logger lands, Week 4") vs. §20's own "structured logging" requirement — resolved by treating the DB row itself as the structured record.** `markFailed` merges `lastError` into the notification's own `payload` JSON rather than logging it anywhere — queryable, permanent, and doesn't violate #8's letter while still satisfying its spirit. `worker.ts` only logs its own startup/shutdown lines, exactly mirroring `server.ts`'s existing precedent (same `eslint-disable-next-line no-console` pattern, nothing per-job).
- **The one real boundary problem this surfaced:** `payments`' own `OrderPort` deliberately excludes contact PII (`GetOrderForPaymentUseCase`'s own doc comment: "no contact/address PII since this is consumed by the payments module"). `ORDER_CONFIRMED`/`PAYMENT_FAILED` fire from `payments` (`ConfirmCodOrderUseCase`, `HandleRazorpayWebhookUseCase`), but building the notification needs `contactEmail`, which `orders` — not `payments` — owns. Resolved with a new `orders`-owned `NotifyOrderEventUseCase`: `payments` calls it post-commit through a new `OrderPort.notifyOrderEvent(orderId, type)` method that carries only an orderId and an event-type string, never PII of its own; `orders` re-reads the order fresh (including `contactEmail`) and builds the actual payload. `ShipOrderUseCase`/`DeliverOrderUseCase` call the same use-case directly (no `payments`-style boundary problem there — same module). `REFUND_PROCESSED` on the cancellation-refund path has the identical shape of problem (`refunds`' own `IssueRefundForCancelledOrderUseCase` only ever sees a Payment record, ADR-025) — resolved the same way this session already resolved every `orders`+`refunds` composition problem: in `admin`'s `CancelOrderWithRefundUseCase`, which already has the full order (contactEmail included) from the cancellation step.
- `RETURN_APPROVED`/`REFUND_PROCESSED` (return-refund and manual-completion paths) wired directly inside `returns`' own `ApproveReturnUseCase`/`IssueRefundForApprovedReturnUseCase`/`MarkReturnRefundedUseCase` — no boundary problem there, `returns`' own `OrderReaderPort`/`ReturnOrderView` already existed and only needed two more fields (`contactEmail`, `orderNumber`) added to what it already returns.

**Live-verified end-to-end, not just unit-tested** (this session's own dev Postgres/Redis, a real running `worker.ts` process, not a mock): drove a real COD order through `PENDING_PAYMENT -> CONFIRMED -> PROCESSING -> SHIPPED -> DELIVERED`, then a real return through `RETURN_REQUESTED -> RETURN_APPROVED -> REFUND_INITIATED -> REFUNDED` (COD manual-completion path) — all 5 notification rows landed `SENT` in Postgres, each with the correct type/payload, confirmed by querying the table directly, not by trusting the HTTP response. Separately proved both worker failure paths for real: inserted a notification with no `contactEmail` and enqueued it directly — reached terminal `FAILED` with the structured `lastError` in well under the ~35s three-attempt backoff window would have taken, confirming `UnrecoverableError` genuinely stops retries immediately; then enqueued a job for a nonexistent notification id (an ordinary, retryable failure) and read the job's own state back from BullMQ afterward — `attemptsMade: 3`, `finishedOn` set, confirming the retry/backoff machinery isn't just configured, it actually ran. Along the way, confirmed dev and test already sit on separate Redis logical databases (`REDIS_URL` in `vitest.config.ts` uses `/1`, dev's `.env` uses the default `/0`) — a pre-existing test-harness decision, not something this session needed to add, so a locally-running dev worker never picks up jobs the test suite enqueues. All test data (the order, return, notifications, the temporary probe rows) and the two adjusted inventory units were cleaned up from `woobe_dev` afterward.

**Test counts:** **350/350** passing (up from 341) — 52 test files. New this session: 1 (`EnqueueNotificationUseCase`) + 5 (`ProcessNotificationJobUseCase` — send+mark-sent, idempotent-on-SENT, idempotent-on-FAILED, re-throws without self-marking-failed, 404 on unknown id) + 3 (`StubEmailProvider`) = 9, plus extended assertions (not new `it()` blocks) on 5 pre-existing tests (`CancelOrderWithRefundUseCase`'s two, `ApproveReturnUseCase`'s one, `IssueRefundForApprovedReturnUseCase`'s two, `MarkReturnRefundedUseCase`'s one) to cover the new notification-enqueue behavior at each site, plus constructor-signature updates (new required collaborators, no behavior change) propagated to every test that instantiates these five use-cases.

**Verification commands, all clean, run for real:**
- `pnpm -r run typecheck`/`lint` (all 9 workspace projects, `--max-warnings=0`), `pnpm --filter @woobe/api run boundaries:check` (**424 modules / 1,243 dependencies, up from 406/1,188** — zero violations, confirmed acyclic despite the new `notifications` edges into `orders`/`payments`/`returns`/`admin`).
- `pnpm --filter @woobe/api run test` (against the real `woobe_test` Postgres) — **350/350**. One full-suite run hit a single transient failure in an unrelated, pre-existing test (`collections.integration.test.ts`, a test whose own scenario deliberately triggers a caught FK-violation log line) that passed cleanly both in isolation and on an immediate full-suite re-run — same "full-suite parallel Postgres contention, not a regression" pattern already logged in Day 7 Part 1's own entry, not silently ignored here either.
- `pnpm -r run build` (all three apps) — clean; `apps/api`'s own `tsc` build now also compiles `worker.ts` as a separate entrypoint (`node dist/worker.js`, wired as the `worker` package.json script alongside the existing `start`).

**Follow-ups / known gaps:**
- Module 13 (Build Your Look/Bundles) stays deferred — no approved data/requirements exist, per that module's own explicit gate. Nothing invented.
- No admin-facing view onto notifications (a log/retry-history page) — §20's own requirements list (retry, idempotency, failure handling, structured logging, non-blocking) never asks for one, so none was built; the `notifications` module's HTTP router stays an empty placeholder, mounted only so `apps/index.ts`'s existing `/notifications` path didn't need touching.
- Only `EMAIL` is wired as a channel — `SMS`/`PUSH` exist on the schema enum but nothing sends through them, since no provider/credentials exist for either (same `DECISIONS_PENDING.md` #7 gap).
- Everything flagged in prior entries (coupons/returns scope calls, no restricted-pincode list, no admin category CRUD, no rate limiting) remains unchanged and untouched by this day's work.
- **Next:** Day 8 Part 2 — Homepage Expansion (Module 12), using only real data already sitting behind existing endpoints (orders for New Arrivals/Best Sellers, Collections for Featured Collections, approved Reviews for Customer Reviews) — explicitly skipping Offers/Shop-by-Vibe/UGC/Instagram unless real data for them turns out to exist, per that module's own "do not invent" list.

---

## 2026-08-29 — Week 2 Day 8 (Part 2): Homepage Expansion (`week2 (1).md` §12) — Day 8 complete

**Branch:** `dev1` — uncommitted, per this session's established process.

**Scope, per Part 1's own handoff:** New Arrivals (real, already existed — `GET /products?sort=newest`), Best Sellers (real sales aggregate — new), Featured Collections (real active collections — new export only, no new query), Customer Reviews (real approved reviews, site-wide — new). Offers/Shop-by-Vibe/UGC/Instagram/Build Your Look stay deferred — no approved data source for any of them exists (checked `schema.prisma` and the seed data before writing this, same gate Part 1 already applied to Bundles).

**A real, if minor, pre-existing bug fixed along the way:** the homepage's one product rail was calling `listProducts({ limit: 8 })` with no `sort`, which defaults to `price_asc` (`productListQuerySchema`'s own default) — yet the rail was labeled "New drops." It was never actually sorted by recency. Replaced outright rather than patched, see below.

**New `home` module — a fifth top-level composition root, same posture as `admin`:** no `domain/` or `infrastructure/` layer of its own (owns no Prisma model), only `application/use-cases/` + `interface/http/` + `home.module.ts`. Sits above `products`/`orders`/`collections`/`reviews` and is imported by nothing — the same "compose above the modules that would otherwise cycle" reasoning `admin`'s `GetCustomerDetailUseCase`/`CancelOrderWithRefundUseCase` already established for cross-module read composition (see their own doc comments), applied here for a public, unauthenticated view instead of an admin one. `GetHomePageUseCase`'s constructor depends on narrow `execute`-shaped structural interfaces per collaborator (`NewArrivalsLister`, `BestSellingVariantsReader`, `VariantProductResolver`, `ProductsByIdsReader`, `ActiveCollectionsLister`, `TopApprovedReviewsReader`), not the concrete use-case classes — same DIP posture as `admin`'s two composition use-cases, confirmed still consistent (see the 2026-08-28 DIP-inconsistency follow-up entry above, which fixed the one place this wasn't yet true). `GET /api/v1/home` — one public, unauthenticated call for the whole homepage instead of one round trip per section.

**One new capability per owning module, each exported the same way every other cross-module use-case already is (ADR-025's pattern, not a new one):**
- `orders`: `GetBestSellingVariantQuantitiesUseCase` → `OrderRepository.findBestSellingVariantQuantities(limit)` — a single `groupBy` on `OrderItem.variantId`, summed and ordered by quantity, filtered to the exact same "counts as a real sale" status set (`CONFIRMED/PROCESSING/SHIPPED/DELIVERED`) `hasUserPurchasedProduct` already established — pulled into one named `SOLD_STATUSES` constant so the two can't drift apart. Grouped at the variant level, not product level: `OrderItem` has no `productId` of its own, and `orders` doesn't reach into `product_variants` for anything beyond its own foreign key (ADR-010).
- `products`: `ResolveProductIdsForVariantsUseCase` → `ProductRepository.findProductIdsForVariantIds(variantIds)`, a batched `productVariant.findMany` (no N+1) returning a `Map<variantId, productId>` — the collapsing step orders' variant-level sales need to become the product-level rail `products` already knows how to render. A variant that's since been deleted/reassigned is simply absent from the map, never an error.
- `products`: `listProductsUseCase` — already existed, just wasn't exported; exported for `home`'s New Arrivals (`sort: "newest"`).
- `collections`: `listCollectionsUseCase` — same story, already existed, now exported for `home`'s Featured Collections. No `isFeatured` flag exists anywhere in the schema (checked before writing this) — "featured" here honestly means "every active collection," the same "don't invent a flag that isn't there" call `CategoryTiles.tsx` already made for "Shop by Vibe."
- `reviews`: `ListTopApprovedReviewsUseCase` → `ReviewRepository.listTopApproved(limit)` — APPROVED only, highest rating first (ties broken by newest).

**The one piece of real application-layer logic (not just port wiring) — collapsing variant-level sales to product-level, in `GetHomePageUseCase` itself:** sums `quantitySold` per `productId` across every variant that resolves to it (a product selling 5+4 across two colourways outranks one selling 8 in a single colourway), ranks descending, then drops any product that's gone inactive since it sold (no dead-end links in a "shop now" rail) — same rule applied to Customer Reviews' enrichment step. Both sections overfetch before collapsing/filtering (60 variant rows for an 8-item Best Sellers rail, 18 reviews for a 6-item rail) so a few inactive/deleted results along the way don't silently shrink the rail below its target size. Every one of these rules is covered by a dedicated unit test with mocked ports (ranking, dedup, inactive-filtering, empty-section independence, review enrichment shape) rather than only exercised incidentally through the integration test.

**Customer Reviews shows no reviewer name — not an oversight, an existing convention followed, not invented:** checked `ReviewCard.tsx` (the product-page review list) before building this — it already shows no reviewer identity, `ReviewEntity` has no name field, nothing in this app has ever asked for or stored one. `HomeReviewView`'s `product` field carries only `{id, slug, name, image}`, matched by an explicit unit-test assertion that the shape carries no `userId`.

**`apps/web`:** `features/home/api/home.client.ts` (`getHomePage()`), two new components (`FeaturedCollections` — text/description cards, not imagery: `Collection` has no media field in the schema, so a card built around a placeholder image would be exactly the kind of invented content this module's own "do not invent" list rules out; `CustomerReviewsSection` — reuses `StarRatingDisplay` from the existing reviews feature rather than a second star-rendering implementation). `app/(storefront)/page.tsx` now makes one `getHomePage()` call instead of the old separate `listProducts({limit:8})`, rendering New Arrivals/Best Sellers/Featured Collections/Customer Reviews each in their own `<Reveal>` scroll-fade wrapper, same as the existing category tiles. `ProductRail` (existing component) already handled an empty list by rendering nothing, reused as-is for both new product rails without modification.

**Verified live in a real browser** (chrome-devtools-mcp, this session's own dev servers against real Postgres, 375/768/1024/1440px): confirmed `GET /api/v1/home` end-to-end against `woobe_dev`'s real data — New Arrivals (8 real products), Best Sellers (2 real products ranked by actual past-session order history), Featured Collections (the 2 real seeded collections). Customer Reviews was legitimately empty in `woobe_dev` (no approved reviews exist there yet) and correctly rendered nothing rather than an empty section shell — confirmed the section's own empty-state behavior by temporarily inserting one real APPROVED review directly in Postgres, reloading, and screenshotting the fully-populated section (star rating, title, body, product thumbnail + name, linking through to the real product page) at all four breakpoints, then deleting the temporary row afterward. Clicked through a Featured Collections card to its real collection detail page and confirmed it rendered the real product grid. Zero unexpected console errors throughout (only the pre-existing, expected guest `/auth/refresh` 401). A first full-page screenshot at 375px appeared to show a large blank gap below the trust strip — investigated rather than dismissed: this is `Reveal`'s existing `whileInView` scroll-fade behavior (sections start at `opacity:0` until scrolled into view), present since Week 1's original homepage and unrelated to this day's work — confirmed by scrolling and re-screenshotting, not just asserted.

**Environment note, not a code defect:** this session's local Docker Desktop wasn't running at the start (both `woobe-postgres`/`woobe-redis` containers were stopped, and the daemon itself needed restarting once mid-session after it stopped responding) — `woobe_test` was also several migrations behind (`Product.metaTitle` etc. from Week 2 Day 7) and had no seed data at all, both silently inherited from wherever the last session left the machine. Fixed by starting Docker, running `prisma migrate deploy` against both `woobe_dev`/`woobe_test`, and re-seeding `woobe_test`. Flagging because the full suite genuinely failed for environmental reasons before any of this day's code ran, not because of a regression — worth a `docker compose up -d` + migration/seed check as the very first step of any session, ideally scripted (`pnpm run bootstrap`'s own remit per `week2 (1).md` §36, not re-solved here).

**Test counts:** **366/366** passing (up from 350) — 54 test files. New this session: 10 (`GetHomePageUseCase` — newest-sort passthrough, variant-to-product ranking/collapse, inactive-product filtering on both Best Sellers and Customer Reviews, missing-variant tolerance, empty-best-sellers short-circuit (no wasted calls), Featured Collections cap, review enrichment shape with no reviewer name, review-limit cap, independent per-section failure isolation) + 6 (`home.integration.test.ts` — public/no-auth, New Arrivals shape, Best Sellers ranking against real `CONFIRMED` orders excluding an unpurchased product, `PENDING_PAYMENT` orders never counted, APPROVED-vs-PENDING review visibility, inactive-product exclusion from both Best Sellers and reviews) = 16.

**Verification commands, all clean, run for real:**
- `pnpm -r run typecheck` (9/9 workspace projects), `pnpm -r run lint` (`--max-warnings=0`), `pnpm --filter @woobe/api run boundaries:check` (**433 modules / 1,270 dependencies, up from 424/1,243** — zero violations, confirmed acyclic despite the new `home` module's edges into all four of `products`/`orders`/`collections`/`reviews`).
- `pnpm --filter @woobe/api run test` (against the real `woobe_test` Postgres, after the environment fix above) — **366/366**. The first couple of full-suite attempts this session showed 10–90 scattered, non-deterministic timeout/connection failures across unrelated modules (a fresh/cold Docker Postgres container under 54 parallel test files) — same "full-suite parallel Postgres contention, not a regression" pattern already logged in Day 7 Part 1's and Day 8 Part 1's own entries; a clean re-run once the containers were warm came back fully green, and every module this session actually touched (`home`, `orders`, `products`, `collections`, `reviews`) was additionally re-run in isolation to confirm.
- `pnpm -r run build` (all three apps, dev servers genuinely killed first, `.next`/`dist` cleared) — clean; `/` (the homepage) compiles as `ƒ` (dynamic), same as before this day's work.

**Follow-ups / known gaps:**
- Week 2 Day 8 (Notifications + Homepage Expansion) is now fully done across this entry and Part 1's. Module 13 (Build Your Look/Bundles) remains deferred per Part 1's own entry — still no approved data/requirements.
- Featured Collections shows every active collection (currently 2), capped at 4 — there's no way to curate a subset without a schema-level `isFeatured`/ordering concept that doesn't exist yet; revisit only if the catalogue grows enough active collections that "all of them" stops being a reasonable "featured" set.
- No dedicated caching for `GET /api/v1/home` — every request recomputes live (same `ADR-026`/`force-dynamic` posture the rest of the storefront already uses). Fine at current catalogue/order-history scale (a handful of aggregate queries, no N+1); revisit only against the same trigger `ADR-012` already sets for catalogue search (real p95 latency or scale evidence, not speculative).
- Everything flagged in prior entries (coupons/returns scope calls, no restricted-pincode list, no admin category CRUD, no rate limiting) remains unchanged and untouched by this day's work.
- **Next:** `week2 (1).md` §27's Day 9 (SEO / Accessibility / Performance) — not yet scoped in this session.

---

## 2026-08-29 — Week 2 Day 9: SEO / Accessibility / Performance (`week2 (1).md` §21-23)

**Branch:** `dev1` — uncommitted, per this session's established process.

**Environment, again, before any of this day's work:** Docker was stopped at session start (same as Day 8 Part 2's own entry) and stopped responding again once mid-session, needing a second restart — `docker compose up -d` + a fresh `prisma migrate deploy`/reseed cycle both times. Flagging again rather than treating it as resolved; `pnpm run bootstrap` scripting this (per `week2 (1).md` §36) is still open.

### SEO (§19/§21's own numbering — this session follows the revised §38 order, "Day 9")

**Product detail (`/products/[slug]`) — the biggest real gap, now closed end to end:** the customer-facing `GET /products/:slug` never exposed `metaTitle`/`metaDescription` at all, despite admin being able to set them since Day 7 — the schema's own comment on those columns literally said "Module 19 (SEO, a later day) is what actually renders these... until then these are just admin-editable fields with no customer-facing effect." Wired the full path: `ProductDetailEntity` (domain) → repository `findBySlug` mapping → `GetProductBySlugUseCase`'s result → the web `ProductDetail` type → `generateMetadata` (falls back to `name`/`description` when admin never set them, never a bare fallback string alone). Added real `Product` JSON-LD (`AggregateOffer` with live lowest/highest price across variants + `InStock`/`OutOfStock` availability, `AggregateRating` from the same rating-summary endpoint the review section already exposes, `Brand`) via a new small `JsonLd` component (`JSON.stringify` with `<` escaped to `<` — the standard fix for a `</script>`-breakout risk when the embedded data includes admin/user-entered text). Canonical + full OpenGraph added too.

**Products listing (`/products`) — real per-filter metadata, plus one real cross-page SEO decision:** a category filter (`?category=`) gets a real title + a canonical scoped to just that param (dropping price/size/color/sort/search/page — refinements, not distinct pages). A **collection** filter (`?collection=`) instead canonicalizes to `/collections/[slug]` — that page already exists and shows the same products, so `/products?collection=X` is a duplicate view of it, not a second canonical one; consolidating avoids splitting ranking signal between two URLs for the same content. A search (`?q=`) gets `noindex, follow` — unbounded query strings aren't worth indexing, but links found there still get followed.

**Collections detail (`/collections/[slug]`)** — already had partial metadata from Week 2 Day 2; added canonical + completed the OpenGraph fields (`type`, `url`).

**Root layout** — `metadataBase` (was unset — every relative OG/canonical URL on every page was silently falling back to `http://localhost:3000` regardless of environment until this), a title template (`%s — Woobe`, every page's own title now composes into it instead of hand-appending "— Woobe" per page), default OpenGraph/Twitter card, explicit `robots: {index:true, follow:true}` default. New `NEXT_PUBLIC_SITE_URL` env var (`.env.example`/`.env.local`) backs a small `siteUrl()`/`absoluteUrl()` helper, same "fail fast if unset" posture `api-client.ts`'s own `apiBaseUrl()` already uses.

**`noindex` where it's actually correct, not a blanket default:** `/account/*` (one shared `layout.tsx`, covers profile/addresses/orders/order-detail in one place), `/cart`, `/checkout`, `/wishlist`, `/login`, `/register`, `/order-confirmation/[id]` — every one of these is either one customer's own private data or a dead-end utility page with no unique indexable content. Each also got a real page-specific `<title>` (previously all inherited the generic site default).

**`robots.ts`/`sitemap.ts` (new)** — the disallow list in `robots.ts` mirrors the per-page `noindex` set above (belt-and-suspenders for a crawler that hasn't rendered the page's own meta tag yet). `sitemap.ts` pulls real product/category/collection URLs from the live catalogue (bounded at 500 product URLs, same "revisit past a real scale trigger" posture ADR-012 already uses for search — comfortably past this catalogue's actual 10 products).

**A real Day-0-style bug caught and fixed before it shipped, not after:** `sitemap.ts` fetches live catalogue data, and — same trap `week2 (1).md` §35 already fixed once for the homepage — a `next build` only succeeds with a live `apps/api` reachable *during the build*, true by coincidence locally (a dev server happened to still be running) and false in CI. Reproduced the exact failure deliberately (killed the API, rebuilt, got `TypeError: fetch failed / ECONNREFUSED` on `/sitemap.xml`, exiting the build) before fixing it, not just assumed the risk — added `export const dynamic = "force-dynamic"` (ADR-026, same as the homepage), then reproduced the clean success with the API still cold to confirm.

**A Lighthouse SEO finding investigated to a real root cause, not shrugged off as a tool quirk:** `/products/[slug]` and `/collections/[slug]` scored a `meta-description` failure even though the tag was verifiably present — confirmed three independent ways (a live browser `evaluate_script` DOM query, a plain `curl` of the full response, and a production `next start` build, not just dev mode) before concluding it wasn't a real defect. Root cause, found by reading Next.js 15's own installed source (`next/dist/server/lib/streaming-metadata.js`, `is-bot.js`): pages with an async `generateMetadata` stream their `<head>` in via a client-side patch for regular browsers, EXCEPT for a hardcoded allowlist of real crawler user-agents (`HTML_LIMITED_BOT_UA_RE` — Googlebot, Bingbot, facebookexternalhit, Twitterbot, Slackbot, WhatsApp, and literally `Chrome-Lighthouse` by name), which get fully-blocking, complete metadata instead. This MCP's own Lighthouse integration apparently doesn't send the `Chrome-Lighthouse` UA string the real Lighthouse CLI does, so it experiences the same streamed-metadata path a regular browser would — a false negative specific to this tool, not a defect, and not something real search engines or the standalone Lighthouse CLI would ever see. Documented here rather than "fixed" with a workaround, since Next's own bot-detection is the actual, correct mitigation and already covers every crawler that matters.

### Accessibility (§20)

**A real, systemic color-contrast failure, found and fixed at the design-token level:** a Lighthouse accessibility audit on the homepage found 11 failing elements, all tracing back to two of `packages/ui/src/tokens/colors.ts`'s five color tokens (`primary`, `text.secondary`) — the token file's own prior comment had already warned "verify with a contrast checker before shipping real content, don't eyeball it" (Week 1), and this was that verification, finally done. `primary` was in use as small body/label text (nav labels, the header logo) and as a button background under white text, both under WCAG AA's 4.5:1; `text.secondary` (trust-strip captions, inactive nav labels) was short too. Computed real replacement hex values with the actual WCAG relative-luminance contrast formula (not eyeballed, not "looks darker now") — same hue/saturation, lower lightness — then verified via a second Lighthouse pass, which required a full dev-server restart to pick up (a Tailwind-preset-only change didn't hot-reload; a stale-CSS false negative on the first re-check, caught by comparing computed styles directly before trusting the audit again).

**A second Lighthouse pass on `apps/admin` (same shared token file) found two more real pairings the first pass never exercised:** the admin sidebar's active-nav-link styling puts `text-primary` on `bg-primary-tint` — a lighter, rosier background than either pairing already fixed — which still failed at 3.91:1; `primary` darkened again (a second, larger step) until it cleared white, `background`, AND `tint` simultaneously. Also found `success`/`error` (Badge's `success`/`error` variants, their own tinted backgrounds) failing specifically when composited over `background` (admin's table rows) rather than `surface`/white (where they'd been implicitly, incorrectly assumed to render) — alpha-composited both by hand (`fg×alpha + bg×(1-alpha)`) to confirm the actual rendered color before darkening either. Every final value re-verified against every real pairing found across both apps before considering this closed: `primary` vs white/background/tint, white-on-primary (button fill), `text.secondary` vs white/background, `success`/`error` vs their own tints composited over both white and background. One color deliberately left alone: the "coming soon" sidebar nav item's `opacity-50` text — genuinely a non-interactive `<span>` (no href/onClick), which WCAG 1.4.3's own Understanding doc exempts from the contrast requirement; what the audit's automated check couldn't see is that nothing marked it as inactive for assistive tech either, so it got a real `aria-disabled="true"` instead of a contrast fix that would have undone the "not available yet" visual signal for sighted users.

**Skip-to-content link (new)** — WCAG 2.4.1 "Bypass Blocks" (Level A), genuinely absent before this (checked; zero hits repo-wide). One `sr-only focus:not-sr-only` link at the top of the shared storefront layout, targeting a shared `#main-content` id on that layout's own content wrapper — covers every storefront page in one place rather than needing the id repeated per page. Verified live: first Tab on any page lands on it, screenshotted visibly on-focus (with the newly-fixed contrast colors) at 375px.

**Six admin `<table>`s given real `scope="col"` header associations; one real orphaned header fixed:** a Lighthouse `td-has-header` finding on `/inventory` traced to one specific empty `<th />` (the "Adjust" action column) — no text, no `scope`, so every row's action cell had no accessible header association at all for a screen reader. Fixed with a real `sr-only` "Actions" label (visually still blank — an action-button column is self-evident to a sighted user) plus `scope="col"` on every header in that table. Swept the other five admin tables (products/orders/customers/returns/collections) for the same pattern — none had an empty header, but none had explicit `scope="col"` either, so added it everywhere for consistency and to close the same class of gap before it recurs.

**Verified, not just fixed-and-assumed:** ran Lighthouse's `mode: "navigation"` audit against every distinct page shape in the app — home, product detail, products listing (bare and category-filtered), collections detail, cart, wishlist, login, register (`apps/web`, mobile viewport) and login, products, orders, inventory, customers, returns, collections (`apps/admin`, desktop). Final tally: **Accessibility 100 on every single page in both apps** (was 96 on the homepage, 96 on `apps/admin`'s products table, before this day's fixes). Remaining non-accessibility findings on a handful of pages are exactly the expected/deliberate ones — the pre-existing guest `/auth/refresh` 401 (`errors-in-console`, unrelated to this day) and the intentional `noindex` on private/utility pages (`is-crawlable`, `meta-description` — both correct, not gaps). Also ran the a11y-debugging skill's own snippets directly: zero orphaned form inputs on checkout/register (every input has a real label), `lang="en"` and a sane viewport meta present globally, tap targets on the bottom nav (97×97) and wishlist-heart buttons (32×32) both clear WCAG 2.5.8's actual 24×24 AA minimum (not just eyeballed as "probably fine").

### Performance (§21)

**Database — two justified index additions, not speculative ones:** `Review.status` (two real, unbounded-growth query paths now filter by status alone — admin moderation queue, and Day 8 Part 2's new site-wide `listTopApproved`) and a composite `CouponRedemption(couponId, userId)` (the exact shape `countGlobalRedemptions(InTx)`/`countUserRedemptions(InTx)` already filter by, run on every coupon-applying checkout **inside the row-locked transaction** — an unindexed scan there directly extends lock hold time). Both traced to real call sites before adding, per §21's own "don't add without a query/use-case reason" rule.

**A real migration-tooling bug caught and fixed before it reached a shared database:** `prisma migrate dev` generating the above indexes also generated an unprompted `DROP INDEX "products_name_trgm_idx"` — the raw-SQL GIN trigram index from Week 2 Day 1 (ADR-012) has no representation in `schema.prisma`'s declarative DSL, so Prisma's diff engine sees it as undeclared drift on every future migration and tries to drop it. Caught by reading the generated `migration.sql` before treating the `prisma migrate dev` run as done — it had already applied once by that point, actually dropping the index in `woobe_dev` for real. Fixed in two layers: (1) immediately restored the index and hand-wrote a corrected `migration.sql` containing only the two real index additions, applied via `migrate resolve --applied` (dev) / `migrate deploy` (test) rather than trusting the tool's own diff a second time; (2) the actual root-cause fix — declared the trigram index AND its `pg_trgm` extension properly in `schema.prisma` itself (`previewFeatures = ["postgresqlExtensions"]`, `datasource.extensions = [pgTrgm(map: "pg_trgm")]`, `@@index([name(ops: raw("gin_trgm_ops"))], type: Gin, map: "products_name_trgm_idx")` — the `map:` matching the existing raw-SQL index by name so it reads as already-satisfied, not new). Verified the fix by generating a throwaway migration afterward and confirming it came back genuinely empty (zero drift) before deleting it — not just assumed correct. `woobe_dev`/`woobe_test` both end this day with clean `migrate status` and the trigram index intact in both.

**Frontend images — native `loading="lazy"`/`decoding="async"`, not `next/image`, and that's a deliberate call, not a shortcut:** `apps/admin`'s own `ProductsTable`/`ProductImages` thumbnails already carried a documented reason for staying plain `<img>` — `Product.images[].url` is an arbitrary admin-entered URL (`addProductImageSchema` has no domain restriction), and `next/image`'s `remotePatterns` needs a fixed hostname allowlist that can't cover an arbitrary future URL without either restricting what admins can paste or accepting a wildcard pattern (a real SSRF/abuse-surface anti-pattern Next's own docs warn against). The exact same data source feeds every customer-facing product image too, so the same reasoning applies there — converted to `next/image` selectively would work today (only `placehold.co` is real) and silently break the moment a real CDN or a differently-hosted image got added. Applied the win that doesn't have that downside instead: `loading="lazy"` + `decoding="async"` on every below-the-fold image (`ProductCard`, cart/wishlist line items, the homepage review thumbnail); the one likely LCP candidate (`ProductDetail`'s main image) deliberately kept eager, with `fetchPriority="high"` added instead — confirmed with a real performance trace (see below), not assumed.

**Skeleton loading states (new) — the one Performance bullet with literally zero prior implementation, despite an existing, already-styled, already-used-elsewhere `Skeleton` primitive (`packages/ui`) sitting unused for this:** every dynamically-rendered route (`/`, `/products`, `/products/[slug]`, `/collections/[slug]`) had no `loading.tsx` at all — a client-side navigation into any of them showed nothing until the full server round trip completed. Added four, each shaped to match its real page's actual layout (product-grid skeleton mirrors `ProductGrid`'s own grid classes and `ProductCard`'s `aspect-[4/5]`, etc.) using the same `Skeleton` component `CartPageContent`/`WishlistPageContent`/`MyOrdersList`/`OrderDetail` already use for their own client-side loading states — one consistent loading treatment across the whole app, not a new pattern invented for this.

**Real performance measurement, not just applying changes and assuming they helped:** ran `performance_start_trace` against a production (`next build` + `next start`, not dev mode) build of `/` and `/products/[slug]`. Results: **LCP 141ms / 94ms, CLS 0.00** on both — confirms the aspect-ratio image containers and skeleton states aren't introducing any layout shift, and that `fetchPriority="high"` on the product page's main image is doing its job (load duration measured at 53μs, effectively instant).

**Backend, reviewed and left alone where "leave it alone" is the correct call:** grepped for await-in-loop patterns across every module — the only real hits are `inventory`'s row-lock reservation loop (one `SELECT ... FOR UPDATE` per line item, inherent to pessimistic per-row locking, already reviewed in Day 0/4/5) — no new N+1 introduced. No additional response-shape caching added: every dynamic page is already `force-dynamic` (ADR-026) specifically because pricing/inventory must be read live (`DEVELOPMENT_RULES.md` #1) — adding a cache layer on top would fight that rule, not complement it; left alone deliberately, not overlooked.

**Test counts:** unchanged at **366/366** — this day's work was schema/index/token/markup/metadata changes, not new business logic requiring new test coverage; existing suites (products, reviews, coupons) re-run to confirm the entity/index changes didn't regress anything.

**Verification commands, all clean, run for real:**
- `pnpm -r run typecheck` (9/9), `pnpm -r run lint` (`--max-warnings=0`), `pnpm --filter @woobe/api run boundaries:check` (433 modules / 1,270 dependencies — unchanged from Day 8 Part 2, this day added no new cross-module edges).
- `pnpm --filter @woobe/api run test` — **366/366**, real `woobe_test` Postgres.
- `pnpm -r run build` — all three apps, **API left deliberately stopped** for the `apps/web` build specifically (the whole point of the sitemap fix above) — clean.
- Lighthouse (`mode: navigation`) across every distinct page shape in both apps, mobile for `apps/web` (375px, §20's own primary target) and desktop for `apps/admin` — **Accessibility 100 everywhere**, SEO 100 on every indexable page, the only non-100 SEO scores are the intentional `noindex` pages.
- One real performance trace per app on the two richest pages (`/`, `/products/[slug]`) — LCP <150ms, CLS 0.00 on a production build.

**Follow-ups / known gaps:**
- The Lighthouse `meta-description` false-negative on async-metadata pages (see above) is a tool/UA-detection limitation, not an app defect — flagging so a future session doesn't "fix" it again by accident.
- `sitemap.ts`'s 500-product-URL cap will need a paginated/multi-sitemap split (the standard next step, per the sitemap protocol's own 50k-URL ceiling) once the catalogue actually approaches that scale — not needed at today's 10 products.
- No `isFeatured`/curation concept for collections still means Day 8 Part 2's "Featured Collections = every active collection" call stands; unrelated to this day's work but adjacent to the SEO canonical decision made here for `/products?collection=`.
- Everything flagged in prior entries (coupons/returns scope calls, no restricted-pincode list, no admin category CRUD, no rate limiting, Docker/bootstrap not yet scripted) remains unchanged and untouched by this day's work.
- **Next:** `week2 (1).md` §27's Day 10 — Security / Integration / Final Verification. Not yet scoped in this session.

---

## 2026-08-29 — Week 2 Days 3–9 independent re-verification + 4 review-fix re-implementations

**Branch:** `dev1` — parent commit `ef4107d` (Day 9). Committed locally, not pushed (per instruction).

**Why this entry exists:** an independent "Technical Review & QA — Week 2 Days 3–9" was run against `dev1 @ ef4107d` and produced a report claiming one P0 and three P1 fixes on 20 files (+202/−36), "left for review, not committed." A fresh repository verification this session found **those fixes were never in the tree** — `dev1` sat at exactly `ef4107d`, working tree clean, `git stash`/`reflog` empty, and the suite was **366/366, not the report's 369**. So the P0 + P1s were re-verified against the actual code (all confirmed live) and re-implemented from scratch.

**Verification method:** 7 parallel subagents (Sonnet for financial/security/DB judgement calls, Haiku for mechanical sweeps) against the live environment, plus a from-scratch baseline: `pnpm typecheck` 9/9, `pnpm lint` 9/9 (`--max-warnings=0`), `pnpm boundaries:check` **433 modules / 1270 deps / 0 violations**, `pnpm build` all 3 apps clean (`apps/web` with the API deliberately stopped — the Day-9 `force-dynamic` fix holds), baseline test suite **366/366**.

**What the re-verification confirmed as still-correct (no change made):** weight→price integer-paise formula; coupon largest-remainder allocation (sums to exact discount); GST computed on the post-discount base with the slab still picked from the undiscounted per-unit price; order price/tax/discount snapshots immune to later coupon/rule edits; Razorpay webhook two-layer idempotency (unique `(provider,eventId)` + conditional state transition, signature verified before any state change); coupon `SELECT … FOR UPDATE` row-locking held to commit; admin inventory-adjust vs checkout-reservation serialising on the same `inventory` rows (`ORDER BY id`, no deadlock); return/refund state machine idempotency via atomic conditional `updateMany`; migration ledger clean with **zero drift** (`migrate diff` empty, `pg_trgm` trigram index intact, no phantom `DROP INDEX`); Clean Architecture layering intact across every Day 3–9 module; no N+1 / unbounded queries / sensitive logging in new code; live adversarial auth pass — cross-account IDOR on addresses/reviews/returns/wishlist/orders all correctly 404, full RBAC matrix for both staff roles correct, no secret/token leakage.

**Fixed (4 — all straight from the review's own findings, all re-confirmed live before fixing):**

1. **P0 — returns over-refunded on any coupon-discounted order.** `calculateReturnRefundAmount` multiplied `OrderItem.unitPricePaise` (always the *undiscounted* price); the per-line coupon discount was computed at checkout for the GST recompute and then discarded — only the aggregate `Order.discountPaise` was persisted. Worked example (from the review, re-reproduced): 5×₹264.00 line + WELCOME10 → customer paid ₹1,247.40, a full return refunded ₹1,379.40 — over-refund of ₹132.00, exactly the coupon discount, on every coupon order.
   - Fix: `OrderItem.discountPaise Int @default(0)` (additive migration `20260829124632`), populated in `CheckoutUseCase.buildOrderItems` from the `allocateCouponDiscount` map that was already being computed, threaded through `OrderItemEntity` → `order.repository` (`createWithItems` + `toEntity`) → returns' `OrderReaderPort`/`returns.module.ts` → `RefundableOrderItem`. `calculateReturnRefundAmount` now subtracts the returned units' prorated share of `discountPaise` (`round(discountPaise · qty / orderedQuantity)`) — same proration already used for tax; tax itself needs no adjustment (it was already computed on the post-discount base at checkout). Shipping is still deliberately excluded (unchanged interpretation).
   - Tests: 2 new unit cases in `calculate-return-refund-amount.test.ts` (full + partial coupon return), existing cases given `discountPaise: 0`; 1 new end-to-end integration test in `returns.integration.test.ts` — real coupon checkout → deliver → full return → approve → refund, asserting `Refund.amountPaise === subtotal − discount + tax` and that the over-refund delta equals exactly the coupon discount.

2. **P1 — shipping fee snapshotted before the checkout transaction.** `shippingReader.evaluate()` ran once *before* `transaction.run(...)` and its `shippingFeePaise` was used unchanged inside — unlike GST, which is recomputed inside. An admin editing `ShippingRule` mid-checkout could commit an order against a stale fee.
   - Fix: the pre-transaction call is kept only as the fast-fail on the minimum-weight rule (renamed `preCheckShipping`); an authoritative `shippingReader.evaluate()` now runs *inside* the transaction and its result is what the order snapshots — mirroring GST's existing in-transaction pattern and §9's "Calculate discount → Recalculate tax/shipping → Final total" ordering.

3. **P1 — notification worker could genuinely double-send.** `ProcessNotificationJobUseCase` did `if (status !== "PENDING") return;` then `provider.send()` then `markSent()` with no atomic claim between — a worker crash / BullMQ at-least-once redelivery in that window left the row `PENDING`, so a retry called the provider again for real.
   - Fix: `NotificationStatus.SENDING` (additive `ALTER TYPE … ADD VALUE`, same migration `20260829124632`); new `claimForSending(id)` = conditional `UPDATE … SET status='SENDING' WHERE id=? AND status='PENDING'` returning whether this caller won; taken **before** `provider.send()`, a lost race returns and does nothing. `markSent` now guards on `status='SENDING'`; `markFailed` guards on `status IN ('PENDING','SENDING')` so it can never clobber a row that already reached `SENT`. A retryable send failure calls `releaseClaim` (`SENDING → PENDING`) so BullMQ's own retry still works. `NOTIFICATION_STATUS` in `@woobe/types` updated to match.
   - Tests: `process-notification-job.use-case.test.ts` reworked — new "does not send when the claim is lost", "claim strictly precedes the provider send" (invocation-order assertion), "releases the claim and re-throws on provider failure"; `enqueue-notification.use-case.test.ts` mock updated for the two new port methods.

4. **P1 — integration suite flaky under parallel file execution.** Re-confirmed reproducible (~60%, scattered assertion / FK-violation / HTTP-parse failures across different files each run, no single culprit) — every integration test hits the one real `woobe_test` Postgres by this repo's own convention.
   - Fix: `fileParallelism: false` in `apps/api/vitest.config.ts` (tests within a file still run concurrently). Clean-DB measurement after the fix: **5/6 full-suite runs green**; the one failure was a pre-existing intra-file race in `home.integration.test.ts` (Best-Sellers ordering) that passes 6/6 in isolation. Honest framing: a substantial improvement, not full elimination — true isolation needs a per-file database, out of scope here.

**Deliberately NOT fixed — verified, then documented as deferred (not required by `week2 (1).md`; instruction was not to expand scope):**
- **P2** `Refund.returnId` has no DB-level unique constraint — duplicate refund is already prevented one level up by the atomic `RETURN_APPROVED → REFUND_INITIATED` conditional `updateMany`; not a live risk. Partial `@unique` is defense-in-depth backlog.
- **P2** wishlist→cart move is two non-transactional writes — no money/inventory impact (cart-add doesn't reserve stock); worst case is a dangling wishlist item on a crash.
- **P2** review moderation has no conditional-transition guard and writes no `AdminAuditLog` row (every other admin mutation does). Not in the review's own findings, no correctness/security/financial impact — recommended follow-up for audit-trail parity, but left untouched this pass to hold scope.
- **P2** `GET /admin/auth/me` uses bare `authGuard` rather than a staff-permission check — re-confirmed **non-exploitable** live (a customer token gets only that customer's own profile; every `/admin/*` data route is `requirePermission`-gated). Defense-in-depth.
- **P2/P3** orders' ownership check is fetch-then-compare rather than in-query — functionally correct (guest-order support needs this shape), structurally inconsistent with addresses/reviews/wishlist.
- **P3** order confirmation / detail pages show only the grand total, not the subtotal/discount/tax/shipping breakdown the API already returns — no `week2 (1).md` requirement for that UI; net-new work.
- **P3** dependency CVEs (`pnpm audit`: 1 critical / 5 high / 5 moderate) — **all** pre-existing transitive dev-tooling (vitest, vite, sharp, postcss, deepmerge-ts via next/prisma). The two new Week 2 runtime deps, **bullmq and multer, carry no advisory**. Routine upstream-patch backlog.
- **P3** no login/register rate limiting — open since Week 1, `week2 (1).md` §37 explicitly names it a known deferred gap and says not to auto-pull it into Week 2.
- **P3** media upload trusts the client `Content-Type` (no magic-byte check); `add-product-image` stores an arbitrary caller `url`; the public reviews endpoint exposes a reviewer `userId` UUID — all admin-gated or opaque, low impact.
- **Zero frontend test coverage** in `apps/web` / `apps/admin` — pre-existing known gap, not a Days 3–9 regression.
- **Newly surfaced, not fixed:** a theoretical concurrent-double-submit at checkout (`cart.markConverted` is an unconditional update, cart read outside the txn) — pre-existing Week 1 flow, the review explicitly cleared it; and a reported admin `/inventory` 375px overflow that on inspection is the table's existing, correct `overflow-x-auto` contained-scroll pattern (not a body-level overflow).

**CI — still never executed.** `.github/workflows/ci.yml` triggers only on `pull_request` and `push` to `main`; all Week 2 work has been committed to `dev1` and never pushed or PR'd, so GitHub Actions has never run for this repo (consistent with every prior journal entry). The workflow itself is sound — services, `check:migrations`, `db:migrate:deploy`, `migrate:diff:check`, lint, typecheck, boundaries, test, build. It **cannot be verified without a push** (explicitly disallowed this session). To verify: push `dev1` and open a PR to `main`, then check the Actions tab.

**Exact results (all re-run after every change):**
- `pnpm typecheck` — **9/9 clean**.
- `pnpm lint` — **9/9 clean**, `--max-warnings=0`.
- `pnpm boundaries:check` — **433 modules / 1270 dependencies / 0 violations** (unchanged — no new cross-module edges).
- `pnpm --filter @woobe/api run test` — **370/370** (366 baseline + 4 net new: 2 refund-discount unit + 1 refund-discount integration + 1 net notification-claim), 54 files, ~48s. Clean-DB determinism: runs A–F = ✓ / ✗¹ / ✓ / ✓ / ✓ / ✓ (the one failure `home.integration` Best-Sellers ordering, passes 6/6 isolated). `apps/api` fixed/adjacent modules in isolation (returns, notifications, orders, coupons, payments, admin): **27 files / 148 tests green**.
- `pnpm build` — **all 3 apps clean**, `apps/web` built with the API deliberately not running.
- `pnpm --filter @woobe/database run migrate:diff:check` (shadow DB) — **No difference detected** (zero drift).
- Migration `20260829124632_week2_review_fixes_orderitem_discount_and_notification_claim` — additive only (`ADD VALUE 'SENDING' BEFORE 'SENT'`, `ADD COLUMN "discountPaise" … DEFAULT 0`), passes `check:migrations`, applied to `woobe_dev` and `woobe_test`.

**Environment notes:** `woobe_test` was reset (`prisma migrate reset --force`, gated behind Prisma's AI-action consent + explicit in-the-moment user approval) and reseeded once mid-session, after a tool-timeout-killed test run polluted it — needed for a trustworthy determinism measurement. `woobe_dev` was migrated (`migrate dev`) for the new column/enum but otherwise untouched. Nothing pushed.

**Next:** `week2 (1).md` §27's Day 10 — Security / Integration / Final Verification — remains unstarted. CI verification (a push + PR) is the one outstanding Week 2 acceptance-gate item that can't be done locally.

---

## 2026-08-29 — Week 2 Day 10: Security / Integration / Final Verification — CI still not executed

**Branch:** `dev1` @ `f9acd5a` (the Day 3–9 re-verification + P0/P1 fix commit). Working tree clean. Local snapshot tag `week2-day10-baseline` set (local only). Committed locally, **not pushed**. `origin/dev1` remains at `ef4107d`.

**Scope of this pass:** run the full Week 2 acceptance checklist against the current committed state, verify the CI workflow configuration, and determine the safest way to execute the real GitHub Actions run. No feature work. No changes to already-verified Week 2 functionality. Week 3 not started.

### CI workflow — verified locally, step by step

`.github/workflows/ci.yml` is unchanged this session (last touched Day 1, `519829e`; `git diff ef4107d..HEAD -- .github/` is empty). Every step was run locally against the current commit:

| CI step | Local command | Result |
|---|---|---|
| Install (frozen lockfile) | `pnpm install --frozen-lockfile` | PASS — "Already up to date"; `pnpm-lock.yaml` tracked and unchanged since `ef4107d` (no deps added) |
| Destructive-migration guard | `pnpm run check:migrations` | PASS — the new migration `20260829124632` is additive only (`ADD VALUE 'SENDING'`, `ADD COLUMN discountPaise … DEFAULT 0`); the script diffs branch-added migrations vs `origin/main` and, with full history in CI, will scan it and pass |
| Prisma generate | `pnpm run db:generate` | PASS |
| Deploy migrations | `pnpm run db:migrate:deploy` | PASS — `woobe_test`: 12 migrations, "up to date" |
| Shadow DB + drift check | `pnpm --filter @woobe/database run migrate:diff:check` | PASS — **"No difference detected"** (zero drift; trigram index intact) |
| Lint | `pnpm run lint` | PASS — 9/9, `--max-warnings=0` |
| Typecheck | `pnpm run typecheck` | PASS — 9/9 (see environment note below) |
| Module boundaries | `pnpm run boundaries:check` | PASS — **433 modules / 1270 dependencies / 0 violations** |
| Tests | `pnpm run test` | **370/370 in 8 of 10 full runs this session**; 2 runs had 2–3 failures each, every one passing in isolation — the pre-existing shared-`woobe_test` parallel-contention flake, reduced by `fileParallelism: false` but not eliminated. A real CI run can go red on this and need a re-run; it is not a code defect. |
| Build | `pnpm run build` | PASS — all 3 apps; `apps/web` built with the API deliberately not running (Day-9 `force-dynamic` holds); static pages generated (14/14 web, 13/13 admin) |

**Environment note (not a code defect):** the first Day-10 `pnpm typecheck` failed only in `apps/admin` with `TS2300: Duplicate identifier` on `.next/types/cache-life.d 2.ts` / `routes.d 2.ts` — macOS "… 2.ts" **duplicate copies** of Next's generated type files inside the gitignored `apps/admin/.next/` (and matching " 2." dupes in `apps/web/.next/server/`), an artifact of the earlier browser-subagent's dev-server/build runs. `.next` is gitignored, so a fresh CI checkout never has it. Cleared `apps/{web,admin}/.next` and re-ran — typecheck 9/9 clean. No repo file touched.

### Safest way to execute the real CI run

The workflow triggers on `pull_request` (any) and `push` to `main` only. `dev1` cannot trigger it as-is. The minimal, safe path:

1. `git push origin dev1` — a clean **fast-forward** (`origin/dev1` is `ef4107d`; local adds only `f9acd5a` on top). No force, no history rewrite, `main` untouched.
2. Open a pull request **`dev1` → `main`** on GitHub. That fires the `pull_request` job against the full Week 2 branch.
3. Watch the Actions tab; on a flake-only failure of the "Unit + integration tests" step, re-run that job. Record the successful run URL + commit SHA in this journal.

Pushing directly to `main` is rejected (branching model + not authorised). A throwaway branch + PR is equivalent to the above with more noise.

**This requires a push, which is not authorised in this session. Per the Git rule, no workaround was attempted.** CI has therefore **not** been executed and GitHub Actions remains **unverified**. Local execution of every step is not a substitute and is not being claimed as one.

### Week 2 acceptance checklist (final)

| Item | Status |
|---|---|
| Tests | 370/370 (8/10 clean full runs; residual pre-existing flake, all isolatable, non-blocking) |
| Lint | PASS 9/9 |
| Typecheck | PASS 9/9 |
| Dependency boundaries | PASS — 433/1270/0 |
| DB migrations / drift | PASS — 12 applied, `migrate:diff:check` "No difference detected" |
| Production builds (×3) | PASS — api, web, admin |
| Security | PASS — no P0/P1; live IDOR sweep (5 resource families) all 404, full RBAC matrix correct, webhook signature enforced, no secret leakage; backlog items all defense-in-depth |
| Mobile UI | PASS — `apps/web` 375px all Day 3–9 pages, no overflow (prior overflow report was a stale-cache false positive); `apps/admin` renders at both viewports |
| Test determinism | IMPROVED, not perfect — `fileParallelism: false` takes it from baseline 0/3 clean to ~8/10; full determinism needs per-file test DBs (out of scope) |
| Secrets scan | CLEAN — `git diff ef4107d..HEAD` has no credentials (only test fixtures / doc prose) |
| Git cleanliness | CLEAN — `dev1` @ `f9acd5a`, +1/−0 vs `origin/dev1`, working tree clean, commit is 24 intended files, no stray/unrelated files |
| **CI actually executed** | **NO — blocked; requires a push + PR (see above)** |

### Deferred P2/P3 — re-checked, none is a Week 2 blocker

`Refund.returnId` unique constraint (defended upstream by the atomic state transition), wishlist→cart transactionality (no money/inventory), review-moderation transition guard + audit-log parity, `/admin/auth/me` staff check (non-exploitable), order-breakdown UI (no requirement), dependency CVEs (all pre-existing transitive; bullmq/multer clean), login rate limiting (§37 explicit deferral), zero frontend test coverage (pre-existing). Left unchanged.

### Verdict

**WEEK 2 NOT COMPLETE — CI VERIFICATION BLOCKED.** Days 0–9 are functionally complete, the P0 + three P1 defects are fixed and verified, and every locally runnable acceptance check passes. The sole outstanding acceptance-gate item — an actual successful GitHub Actions run — cannot be satisfied without a push, which is not authorised. Week 2 is not marked complete. Week 3 not started.

---

## 2026-08-29 — Week 2 Day 10 (cont.): CI ran for the first time — failed on a test-config bug, fixed

**Branch:** `dev1`. Parent `7e681e2`. Local commit only — **not pushed**. PR #1 (`dev1 → main`) stays open, **not merged**.

### First real GitHub Actions run — FAILED

- **PR:** #1 — https://github.com/jsurya114/Wobee-ecommerce/pull/1 (dev1 → main)
- **Workflow:** `CI` (`.github/workflows/ci.yml`), event `pull_request`
- **Run #1 (attempt 1):** https://github.com/jsurya114/Wobee-ecommerce/actions/runs/33255736084 — **conclusion: failure**
- **Commit tested:** `7e681e2c0ed48893083f8073c092d0a22c90a5e0`
- **Step results:** Set-up / install / `check:migrations` / `db:generate` / `db:migrate:deploy` / create-shadow-db / **`migrate:diff:check` PASS** / **Lint PASS** / **Typecheck PASS** / **Module boundary check PASS** / **"Unit + integration tests" → FAILURE (exit 1)** / Build → skipped.
- **Failure shape:** 182 unit tests pass, **43 integration tests fail**, 145 skipped. Errors: `connect ECONNREFUSED 127.0.0.1:6380` (Redis) and `Can't reach database server at localhost:5433` (Postgres). This is **NOT** the shared-`woobe_test` contention flake noted earlier — it is deterministic.

### Root cause

`apps/api/vitest.config.ts`'s `test.env` block **hardcoded** this machine's docker-compose host-port remap:

- `DATABASE_URL: "…@localhost:5433/woobe_test…"`
- `REDIS_URL: "redis://localhost:6380/1"`

Vitest's `test.env` is injected into `process.env` for the run and **overrode** the values GitHub Actions supplies (`.github/workflows/ci.yml`: Postgres service on `:5432`, Redis on `:6379`, with matching `DATABASE_URL`/`REDIS_URL`). So every CI integration test dialled `:5433` / `:6380`, which don't exist on the runner. It had never surfaced because CI had never run before.

### Fix — `apps/api/vitest.config.ts` only (1 file, +18 / −8)

Each `test.env` value that legitimately differs between local and CI now reads `process.env.X ?? "<existing local default>"` — `DATABASE_URL`, `REDIS_URL`, `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `COOKIE_SECRET`, `RAZORPAY_WEBHOOK_SECRET`. `NODE_ENV` stays forced to `"test"`. No test values changed; no application code touched. `env.ts`'s own best-effort root-`.env` loader already "never overrides an already-set var", so a local run still targets `woobe_test` (not `woobe_dev`).

### Local verification of the precedence (all three cases proven, same integration file)

| Case | `process.env` supplied | Result |
|---|---|---|
| CI-style **wrong** ports | `DATABASE_URL=…:5999…`, `REDIS_URL=…:6999` | tests fail with `ECONNREFUSED …:6999` — **`process.env` wins over the hardcoded default** (the 5433/6380 fallback never appears) |
| CI-style **correct** ports (supplied via env) | `DATABASE_URL=…:5433…`, `REDIS_URL=…:6380/1` | 3/3 pass — supplied values honoured |
| Local (nothing supplied) | — | 3/3 pass on `:5433` / `:6380` — local dev behaviour preserved |

### Full local checklist after the fix

- `pnpm run lint` — **PASS** (9/9, `--max-warnings=0`)
- `pnpm run typecheck` — **PASS** (9/9)
- `pnpm run boundaries:check` — **PASS** (433 modules / 1270 deps / 0 violations)
- `pnpm --filter @woobe/api run test` (local defaults) — **370/370**; determinism: **2 of 3 full runs green**, the one failure a *different* pre-existing intra-file contention flake (`wishlist` IDOR) that passes 16/16 in isolation — the known, documented `fileParallelism: false` residual, unchanged by this fix.
- `pnpm run test` (workspace-wide — the exact CI command) — **PASS**: `@woobe/utils` 12/12, `@woobe/validation` 7/7, `@woobe/api` 370/370.
- `pnpm run build` — **PASS**, all 3 apps.
- `migrate:diff:check` — unchanged, still **"No difference detected"** (this fix adds no migration).
- Secrets scan of the diff — clean (only env-var names + doc text; the throwaway `test-*-secret` strings are unchanged in value).

### CI status

**A new GitHub Actions run is required.** Run #1 failed on `7e681e2`; the fix is a new local commit (new SHA) that the remote PR does not yet have. Getting CI to re-run against the fix needs `git push origin dev1` (updates PR #1's head), which is not authorised in this session. Until that push + a green run, **CI remains unverified for the fixed tree** and Week 2 is **not** complete.

---

## 2026-08-29 — Week 2 Day 10 (cont. 2): CI Run #2 — config fix worked, exposed a missing seed step

**Branch:** `dev1`. Parent `cb5731b`. Local commit only — **not pushed**. PR #1 (`dev1 → main`) stays open, **not merged**.

### Second real GitHub Actions run — FAILED (but progressed past Run #1's wall)

- **PR:** #1 — https://github.com/jsurya114/Wobee-ecommerce/pull/1 (dev1 → main)
- **Workflow:** `CI` (`.github/workflows/ci.yml`), event `pull_request` (push of `cb5731b` → `synchronize`)
- **Run #2:** on commit `cb5731b97515c9d850175b62e31c75a84dcd9f69` — **conclusion: failure** (run URL not captured — GitHub REST API rate-limited from this IP at the time; failure analysed from the full runner log).
- **Step results:** install / `check:migrations` / `db:generate` / `db:migrate:deploy` / create-shadow-db / **`migrate:diff:check` PASS** / **Lint PASS** / **Typecheck PASS** / **Module boundary check PASS** / **"Unit + integration tests" → FAILURE (exit 1)** / Build → skipped.
- **The `cb5731b` config fix worked:** Run #1's `connect ECONNREFUSED 127.0.0.1:6380` / `Can't reach database server at localhost:5433` errors are **completely gone**. CI now connects to the runner's Postgres `:5432` / Redis `:6379`. Prisma queries execute.
- **New failure shape:** `@woobe/api` — 16 files failed / 38 passed; **18 tests failed / 207 passed / 145 skipped (370)**. Two signatures, one cause:
  - **`beforeAll` throws `PrismaClientKnownRequestError` P2025 "No record was found"** — `prisma.category.findFirstOrThrow()` (admin, cart, collections, home, admin-inventory, orders, payments, admin-products, returns, reviews, wishlist), `prisma.warehouse.findFirstOrThrow()` (products), `prisma.shippingRule.findFirstOrThrow()` (shipping); `coupons` throws its explicit `Error: test setup: need at least 2 active categories seeded`.
  - **`AssertionError: expected 401 to be 200`** from `loginAdmin` / `loginStaff` — the seeded `admin@woobe.in` / staff accounts don't exist (`admin-customers` 9, `media` 8).
  - Suites that passed (`users` 20/20, `auth` 8/8) are exactly the ones that register their own users via the API and need no pre-seeded rows.

### Root cause

`.github/workflows/ci.yml` ran `db:migrate:deploy` (schema only) and went **straight to `pnpm run test`** — **no seed step**. The Postgres service container is fresh per run, so `woobe_test` on the runner had all 35 tables but **zero rows**. Integration `beforeAll` hooks read pre-seeded reference data (warehouse, shipping rule, GST slabs, admin + 2 staff accounts, 5 categories, 10 products / 22 variants). `ci.yml` had exactly one commit in its history (`519829e`, Week 1 Day 1) and had **never** contained a seed step; it only mattered now because Run #1 was the first CI run ever and died before the test step. **NOT the shared-`woobe_test` contention flake** — deterministic (empty DB → every seed-dependent `beforeAll` throws).

### Fix — `.github/workflows/ci.yml` only (1 file, +7 / −0)

New step, immediately after "Deploy migrations to the test database" and before the shadow-DB / lint / test steps:

```yaml
      - name: Seed the test database
        run: pnpm run db:seed
```

Uses the existing project command (`db:seed` → `node scripts/with-root-env.mjs pnpm --filter @woobe/database run seed` → `tsx prisma/seed.ts`). No application code, no `vitest.config.ts` change; the `cb5731b` fix is untouched. `seed.ts`'s non-idempotent `.create` calls (pricingSetting / shippingRule / gstSlab) are safe here because the CI service container is fresh each run — seed executes exactly once against the new schema.

### Local verification — true from-scratch CI simulation on a throwaway DB

Created `woobe_ci_verify` (+ `woobe_ci_verify_shadow`) on the local Postgres, ran the exact CI sequence against it, then dropped both. `woobe_dev` and `woobe_test` untouched.

| Step | Result |
|---|---|
| `pnpm install --frozen-lockfile` | PASS ("Already up to date") |
| `pnpm run check:migrations` | PASS (destructive marker on the Day-8 reviews migration accepted; exit 0) |
| `pnpm run db:generate` | PASS |
| `pnpm run db:migrate:deploy` (fresh `woobe_ci_verify`) | PASS — 35 tables; **categories/warehouses/shipping_rules/users all = 0** (reproduces Run #2's empty-DB state exactly) |
| **`pnpm run db:seed`** (fresh, freshly-migrated DB — the new step) | **PASS** — after: categories=5, warehouses=1, shipping_rules=1, gst_slabs=2, products=10, variants=22, users=3 (`admin@woobe.in`/SUPER_ADMIN, `orders@woobe.in`, `catalog@woobe.in`) |
| `pnpm --filter @woobe/database run migrate:diff:check` (exact CI cmd) | PASS — "No difference detected" |
| `pnpm run lint` | PASS (9/9, `--max-warnings=0`) |
| `pnpm run typecheck` | PASS (9/9) — after clearing stale gitignored `apps/{web,admin}/.next` macOS `" 2.ts"` duplicate files (not a code defect; a fresh CI checkout never has them) |
| `pnpm run boundaries:check` | PASS — 433 modules / 1270 deps / 0 violations |
| `pnpm run test` — against the migrated **+ seeded** `woobe_ci_verify`, `DATABASE_URL` supplied via env | **PASS — 54 files, 370/370** (`@woobe/utils` 12, `@woobe/validation` 7, `@woobe/api` 370→ all 370, incl. every previously-failing integration suite). Also re-proves `cb5731b`'s precedence: env-supplied `DATABASE_URL` wins over the local `:5433` default. |
| `pnpm run build` | PASS — api, web, admin |
| Secrets scan of the diff | CLEAN — added lines are one comment + `run: pnpm run db:seed`; no credentials. The `PGPASSWORD=woobe_dev_password` visible on the adjacent shadow-DB line is a pre-existing unchanged context line. |
| Unrelated changes | NONE — `git status` shows only `M .github/workflows/ci.yml`; no untracked files; generated Prisma client is gitignored. |

### CI status

**A new GitHub Actions run is still required.** Run #2 failed on `cb5731b`; this seed-step fix is a new local commit the remote PR does not have. A green CI run needs `git push origin dev1` (updates PR #1's head), not authorised in this session. Until that push + a fully green run (all steps through Build), **CI remains unverified** and **Week 2 is NOT complete**.

---

## 2026-08-29 — Week 2 Day 10 (cont. 3): CI Run #3 — seed step works, 370/370 on CI, Build failed on a missing NEXT_PUBLIC_* var

**Branch:** `dev1`. Parent `04e1a6a`. Local commit only — **not pushed**. PR #1 (`dev1 → main`) stays open, **not merged**.

### Third real GitHub Actions run — 15/16 steps green, Build failed

- **PR:** #1 — https://github.com/jsurya114/Wobee-ecommerce/pull/1 (dev1 → main)
- **Workflow:** `CI` (`.github/workflows/ci.yml`), event `pull_request` (push of `04e1a6a` → `synchronize`)
- **Run #3:** https://github.com/jsurya114/Wobee-ecommerce/actions/runs/33264517743 — commit `04e1a6afc990eee0a9788b6b8e4927e706760c77`, attempt 1 — **conclusion: failure**
- **Per-step results (job `ci`, 3m18s):**

  | # | Step | Result |
  |---|---|---|
  | 6 | Install dependencies | ✅ success |
  | 7 | Check for unreviewed destructive migrations (`check:migrations`) | ✅ success |
  | 8 | Generate Prisma client (`db:generate`) | ✅ success |
  | 9 | Deploy migrations to the test database (`db:migrate:deploy`) | ✅ success |
  | 10 | **Seed the test database (`db:seed`)** | ✅ **success** — the Run-#2 fix works |
  | 11 | Create shadow database | ✅ success |
  | 12 | Check schema matches migration history (`migrate:diff:check`) | ✅ success |
  | 13 | Lint | ✅ success |
  | 14 | Typecheck | ✅ success |
  | 15 | Module boundary check (`boundaries:check`) | ✅ success |
  | 16 | **Unit + integration tests** | ✅ **success — 370/370** (Run #2's P2025 / 401 seed failures gone) |
  | 17 | **Build** | ❌ **failure** |

  First time CI ever reached Build (Run #1 died at DB connect, Run #2 at the test step).

### Exact Build failure

`apps/web` `next build`, "Collecting page data" phase:

```
[Error: Failed to collect configuration for /products]
  [cause]: Error: NEXT_PUBLIC_SITE_URL is not set —
           copy apps/web/.env.example to apps/web/.env.local
      ...  ← apps/web/src/lib/site-url.ts:10
> Build error occurred
[Error: Failed to collect page data for /products]
```

`apps/api` (`tsc`) and `apps/admin` (`next build`) built fine.

### Root cause

`apps/web/src/lib/site-url.ts` (Week 2 Day 9 SEO — `metadataBase`, canonical URLs, `robots.txt`, `sitemap.xml`) throws at build time if `NEXT_PUBLIC_SITE_URL` is unset; `apps/web/src/lib/api-client.ts` does the same for `NEXT_PUBLIC_API_URL`. Locally these come from `apps/web/.env.local` (gitignored). CI has no `.env.local` and `.github/workflows/ci.yml` set no `NEXT_PUBLIC_*` vars. Pre-existing `ci.yml` gap, deterministic — never hit before because CI never reached Build. **Not** the shared-DB test flake.

### Fix — `.github/workflows/ci.yml` only (1 file, +8 / −0)

Three non-secret vars added to the job `env:` block, values mirroring `apps/{web,admin}/.env.example` exactly:

```yaml
      NEXT_PUBLIC_API_URL: http://localhost:4000
      NEXT_PUBLIC_SITE_URL: http://localhost:3000
      NEXT_PUBLIC_ADMIN_API_URL: http://localhost:4000
```

`NEXT_PUBLIC_*` is inlined into the client bundle at build time — `apps/web/.env.example` states nothing secret belongs there. No application code, no `vitest.config.ts`, seed step unchanged.

### Value cross-check

| Var | ci.yml | `apps/web/.env.example` | `apps/admin/.env.example` |
|---|---|---|---|
| `NEXT_PUBLIC_API_URL` | `http://localhost:4000` | `http://localhost:4000` | — |
| `NEXT_PUBLIC_SITE_URL` | `http://localhost:3000` | `http://localhost:3000` | — |
| `NEXT_PUBLIC_ADMIN_API_URL` | `http://localhost:4000` | — | `http://localhost:4000` |

(User's request note said `:3001` for the admin var; corrected to `:4000` on review to match `apps/admin/.env.example` — `:3001` is the admin app's own port, not the API it calls. Confirmed with the user before committing.)

### Local verification

| Check | Result |
|---|---|
| CI-equivalent build (`NODE_ENV=test`, CI env vars **+ the 3 new `NEXT_PUBLIC_*`**, `apps/{web,admin}/.env.local` moved aside) | **PASS** — all 3 apps build; `/products` (the Run-#3 failure) now builds as `ƒ` dynamic |
| CI-equivalent build **without** the 3 vars (control) | FAILS with the exact Run #3 error — confirms root cause |
| `pnpm run lint` | PASS (9/9, `--max-warnings=0`) |
| `pnpm run typecheck` | PASS (9/9) |
| `pnpm run boundaries:check` | PASS — 433 modules / 1270 deps / 0 violations |
| `pnpm run test` | **370/370** (run 2 of 2). Run 1 had 1 failure — `admin-customers` RBAC `socket hang up` + scattered unique-constraint errors on different tests — the documented shared-`woobe_test` `fileParallelism: false` residual flake; green on rerun. Unrelated to this `env:`-only change (those vars aren't read by the API test suite). CI Run #3 itself passed this step 370/370 on a fresh seeded DB. |
| `pnpm run build` (standard local) | PASS — api, web, admin |
| Secrets scan of the diff | CLEAN — added lines are three `http://localhost:*` URLs + a comment; the word "secret" appears only in the explanatory comment |
| Unrelated changes | NONE — `git status` shows only `M .github/workflows/ci.yml` |

### CI status

**A new GitHub Actions run (Run #4) is still required.** Run #3 failed on `04e1a6a`; this build-env fix is a new local commit the remote PR does not have. A green run needs `git push origin dev1`. Until Run #4 goes fully green through Build, **CI remains unverified** and **Week 2 is NOT complete**.

---

## 2026-08-29 — Week 2 Day 10 (cont. 4): CI Run #4 — FULLY GREEN. GitHub Actions verified.

**Branch:** `dev1` @ `c5d0a28` (= `origin/dev1`). PR #1 (`dev1 → main`) **open, NOT merged**. This journal entry is a local commit — **not pushed**.

### Fourth real GitHub Actions run — SUCCESS

- **PR:** #1 — https://github.com/jsurya114/Wobee-ecommerce/pull/1 (dev1 → main), state `open`, `merged: false`, `mergeable_state: clean`
- **Workflow:** `CI` (`.github/workflows/ci.yml`), event `pull_request` (push of `c5d0a28` → `synchronize`)
- **Run #4:** https://github.com/jsurya114/Wobee-ecommerce/actions/runs/33265303448 — attempt 1 — **conclusion: `success`**
- **Commit tested:** `c5d0a284290ea8f675fe15c730543a3da985220e`
- **Duration:** 2026-08-29T17:19:00Z → 17:22:02Z (≈3 min)

### Every step (job `ci`) — all green

| # | Step | Result |
|---|---|---|
| 1 | Set up job | ✅ |
| 2 | Initialize containers (postgres:16-alpine :5432, redis:7-alpine :6379) | ✅ |
| 3 | `actions/checkout@v4` (fetch-depth 0) | ✅ |
| 4 | `actions/setup-node@v4` (node 22) | ✅ |
| 5 | Enable corepack (pnpm) | ✅ |
| 6 | Install dependencies (`pnpm install --frozen-lockfile`) | ✅ |
| 7 | Check for unreviewed destructive migrations (`check:migrations`) | ✅ |
| 8 | Generate Prisma client (`db:generate`) | ✅ |
| 9 | Deploy migrations to the test database (`db:migrate:deploy`) | ✅ |
| 10 | **Seed the test database (`db:seed`)** | ✅ |
| 11 | Create shadow database | ✅ |
| 12 | Check schema matches migration history (`migrate:diff:check`) | ✅ |
| 13 | Lint (`pnpm run lint`) | ✅ |
| 14 | Typecheck (`pnpm run typecheck`) | ✅ |
| 15 | Module boundary check (`boundaries:check`) | ✅ |
| 16 | **Unit + integration tests (`pnpm run test`)** | ✅ **370/370** |
| 17 | **Build (`pnpm run build`) — api, web, admin** | ✅ |
| — | Post-setup-node / post-checkout / Stop containers / Complete job | ✅ |

### Fix history that got here

1. `cb5731b` — `vitest.config.ts`: `process.env.X ?? <local default>` so CI-supplied `DATABASE_URL`/`REDIS_URL` win (Run #1 fix).
2. `04e1a6a` — `ci.yml`: added `db:seed` step after `db:migrate:deploy` (Run #2 fix).
3. `c5d0a28` — `ci.yml`: added `NEXT_PUBLIC_API_URL` / `NEXT_PUBLIC_SITE_URL` / `NEXT_PUBLIC_ADMIN_API_URL` to the job `env:` so `apps/web` `next build` completes (Run #3 fix).

All three are test/CI-configuration only. Zero application-code changes across the entire Day 10 CI-verification effort.

### CI status

**VERIFIED.** GitHub Actions Run #4 (`33265303448`) executed against `c5d0a28` and passed all 16 pipeline steps — tests 370/370 and all three production builds included. `origin/dev1` = `c5d0a28`, `origin/main` unchanged at `4f456df`. PR #1 remains open and unmerged (for review; per instruction, not to be merged here).

**Week 2 is ready to be considered complete, pending final sign-off.**

---

## 2026-08-29 — Post-Week-2: admin shell UI polish (mobile nav + role badge)

**Branch:** `dev1`. Local working-tree changes (uncommitted at time of writing) — `apps/admin` only, no API / schema / test changes.

Follow-up tweaks after the Week 2 close, driven by a mobile-view review of the admin shell:

1. **Mobile nav is now a hamburger menu.** `Sidebar.tsx` below `md:` was a horizontal `overflow-x-auto` strip that clipped its own tail off-screen at 375px ("Collections" cut mid-word). Replaced with a collapsible menu behind a hamburger toggle (`useState` + `useEffect` that closes it on route change; `aria-expanded` / `aria-controls` / labelled toggle). At `md:` and up it is the unchanged permanent `w-56` side column.
2. **Divider lines removed** from the mobile hamburger bar and the mobile nav panel (`border-b`). Only the desktop `md:border-r` column separator and the desktop `TopBar` `border-b` remain (both hidden on mobile).
3. **`TopBar` is desktop-only** now (`hidden md:flex`) — on mobile the Sidebar's hamburger bar is the top bar, so the two no longer both render.
4. **Nav order changed** in `nav-config.ts` to the requested priority: Customers, Products, Collections, Orders, Inventory, Staff, Returns, Settings. Nothing removed — Collections kept (slotted with the catalog group after Products); Staff & Settings stay `coming-soon`.
5. **"super admin" role text → star badge.** New `BrandMark.tsx` renders the "Woobe Admin" wordmark plus a filled-star badge (`role="img"`, `aria-label="Super admin"`) when `user.role === "SUPER_ADMIN"`. It replaces the old "· super admin" text in both the desktop `TopBar` and the mobile hamburger bar. The standalone "Woobe Admin · super admin" identity line inside the hamburger menu was removed.
6. **Desktop `TopBar` de-duplicated.** The seeded super-admin's `user.name` is literally "Woobe Admin", so the TopBar showed "Woobe Admin ★" (BrandMark) *and* "Woobe Admin" (the name span) side by side. Dropped the name span entirely — the TopBar is now just `<BrandMark />`, right-aligned (`justify-end`). `TopBar` no longer needs `useAdminAuth` / `"use client"`.

**Verification:** `pnpm --filter @woobe/admin run lint` PASS, `typecheck` PASS, `build` PASS (13/13 pages). Files touched: `apps/admin/src/features/shell/nav-config.ts`, `.../components/Sidebar.tsx`, `.../components/TopBar.tsx`, new `.../components/BrandMark.tsx`. Committed `e77ec5f`, pushed to `origin/dev1`. (PR #1 was merged to `main` at 17:32 UTC via merge commit `13977ee`; this commit lands after that merge point, so `dev1` is now 1 ahead / 1 behind `main`.)

## 2026-08-29 — Post-Week-2: storefront footer + category ticker

**Branch:** `dev1`. `apps/web` only — no API / schema / test changes.

1. **Storefront footer added.** `(storefront)/layout.tsx` had `SiteHeader` + `BottomNav` (mobile only) and *nothing* at the bottom on desktop. New `SiteFooter.tsx` — `hidden md:block` (mobile keeps the fixed `BottomNav`, which a footer would collide with), `max-w-6xl` to match the header. Conventional storefront layout: brand + tagline, a newsletter capture (`NewsletterSignup.tsx` — client, validates + inline confirm, no backend yet), Instagram/Facebook icon links (inline SVG — `lucide-react` dropped its brand glyphs), three link columns (**Shop** / **Account** / **Help** — internal links to real routes only, `mailto:` for contact), and a bottom row `© <year> Woobe · Prices in INR · Secure checkout`. The inner rule above the copyright was removed at the user's request — only the footer's own top border remains. Rendered between `#main-content` and `<BottomNav />`.
2. **"Shop by category" now centres properly.** `CategoryTiles.tsx` used `justify-[safe_center]` — the `safe` keyword isn't honoured widely, so on a wide viewport it fell back to flex-start and looked left-shifted. Swapped to `flex flex-wrap justify-center` (with `mx-auto max-w-4xl`): centred at every width, wraps to more centred rows on narrow screens instead of overflowing or needing a scroll. No animation. (A right-to-left marquee was tried first, then removed at the user's request — plain centring only.)
3. **Hero measure widened.** `Hero.tsx` headline was `max-w-xl` and the sub-copy `max-w-md`, so on desktop both stacked into narrow 3-line columns. Headline → `max-w-4xl`, sub-copy → `max-w-2xl` (+ `text-pretty` instead of `text-balance` on the sub-copy). Lines now run wider before wrapping.
4. **Sticky footer.** `(storefront)/layout.tsx` was a bare fragment, so on short pages the footer floated up mid-screen (inconsistent per page). Wrapped in `flex min-h-dvh flex-col` with `flex-1` on `#main-content` — the footer now always sits at the bottom edge, same on every page.

**Verification:** `pnpm --filter @woobe/web run lint` PASS, `typecheck` PASS. (No `build` — web dev server was live; iCloud-synced `.next` corrupts under a concurrent prod build, see the cache note below.) Files: new `apps/web/src/features/auth/components/SiteFooter.tsx` + `.../NewsletterSignup.tsx`, `apps/web/app/(storefront)/layout.tsx`, `apps/web/src/features/home/components/CategoryTiles.tsx`, `apps/web/src/features/home/components/Hero.tsx`. (`apps/web/tailwind.config.ts` was touched for the marquee, then reverted — no net change.)

**Environment note:** repo lives on an iCloud-synced Desktop. Rapid `.next` rewrites (a `pnpm build` run while `next dev` is also writing) race iCloud and spawn `" 2"` duplicate files/dirs — seen as `cache-life.d 2.ts` "Duplicate identifier" typecheck failures and unstyled/500 dev pages (React #418) this session. Empty `" 2"` source dirs (`apps/web/src/features/*/components 2`, etc.) are harmless local debris, untracked. Fix when it bites: stop dev, `rm -rf apps/*/.next apps/*/.turbo` + `find apps -path "*/.next/*" -name "* 2*" -delete`, restart. Long-term: move the repo off iCloud.

## 2026-08-30 — Post-Week-2: login / register redesign (split-panel, responsive)

**Branch:** `dev1`. `apps/web` only — **presentation only, zero behaviour change**. No API / schema / `@woobe/ui` / `useAuth` / routing / provider changes.

Rebuilt `/login` and `/register` to the supplied mock: split panel (`lg:grid-cols-2`) with a text-only brand panel (`hidden lg:flex` — no product photography exists), a centred form column, a trust-icon row underneath. Responsive at every width (single column < `lg`, brand panel drops out).

- **New (all presentational):**
  - `AuthShell.tsx` (server) — the split layout + `BrandPanel` + `AuthTrustRow` (Secure Shopping / Free Shipping / Premium Quality / Customer Support). No client JS. `BrandPanel` takes a `src` prop — `/login` uses `public/auth-hero.jpg`, `/register` passes `public/auth-hero-register.jpg` (both user-supplied boutique photos, downscaled to 1240px / ~215 KB via `sips`; `AuthShell`'s `image` prop defaults to the login one). Full-bleed with a soft scrim; brand copy sits over it in a `backdrop-blur` frosted card (`bg-background/70`) so it stays legible regardless of the photo behind it. Plain `<img loading="eager" decoding="async">` per the repo's ProductCard convention (no `next/image`). Social row is Google only (Apple dropped).
  - `AuthField.tsx` (client, `forwardRef`) — labelled `@woobe/ui` `Input` with a leading icon and an optional password reveal toggle (`revealable`; `aria-pressed` / `aria-label`). **Same forward-props/ref contract as `FormField`** — `@woobe/ui`'s `FormField` was left untouched to keep the blast radius off every other form.
  - `SocialAuthButtons.tsx` (client) — "or continue with" divider + Google / Apple buttons. Federated sign-in isn't wired, so a click fires an honest `toast.info("Social sign-in is coming soon.")` rather than a dead or fake control.
- **Changed:** `LoginForm.tsx` / `RegisterForm.tsx` — **JSX only**. The `useForm` + `zodResolver(loginSchema/registerSchema)` + `onSubmit` + `useAuth().login/register` + `router.push("/account")` + `ApiError` branching + toasts are byte-for-byte the same. Login gains a visual "Remember me" checkbox (unwired — sessions are already httpOnly-cookie) and a "Forgot password?" `mailto:` link (no reset flow yet). `login/page.tsx` / `register/page.tsx` now compose `<AuthShell>` + heading + form + the cross-link.
- **Perf:** no new deps; chrome is server-rendered; no images/fonts added (brand panel is a CSS gradient + text); one ~0.4 KB inline Google glyph. Icons tree-shake from the existing `lucide-react`.
- **Note:** on desktop the auth pages now show `AuthTrustRow` *and* the global `SiteFooter` (link columns + newsletter). Left as-is — suppressing the global footer here would need a route-group split, which the "don't touch architecture" constraint rules out.

**Guest nav — Wishlist hidden for logged-out visitors.** Every wishlist endpoint requires auth (there's no guest wishlist), so for a guest the nav "Wishlist" link only ever bounced to `/login`. `SiteHeader` (desktop nav) and `BottomNav` now render the Wishlist entry only when `status === "authenticated"` (BottomNav drops to 4 tabs for guests). **Bag/cart is untouched** — guests have a real cart (ADR-011 `cart_id` cookie, merged on login).

**Guest wishlist tap → explanatory toast.** `WishlistButton` (still shown to guests on cards/PDP) previously did a silent `router.push("/login")`. Now a guest tap fires `toast.info("Log in to save items to your wishlist", { action: "Log in" })` — no forced navigation; the toast's action goes to `/login`. Only branch changed; the authenticated toggle path is untouched.

**"Add to bag" on the shop grid.** New `QuickAddToBagButton` (client island, `features/cart/components/`) under each PLP card. `ProductSummary` carries no variant data and the cart is variant-addressed, so a click fetches `getProductBySlug` on demand: single-variant → `useCart().addItem(variant, 1)` + success toast; multi-variant → toast + `router.push('/products/[slug]')` to choose; no stock → error toast. `ProductCard` gained `showQuickAdd?: boolean` (default `false`); `ProductGrid` opts in, the homepage `ProductRail` does not. No new API, no cart-contract change; `ProductCard` stays a Server Component; the detail fetch is click-time only (zero added browse cost). Button is `variant="primary"` (solid rose CTA) and shares a `flex justify-between` row with `PriceTag` (wraps under it on very narrow cards).

**Verification:** `pnpm --filter @woobe/web run lint` PASS, `typecheck` PASS (after clearing iCloud `" 2"` dupes in `.next/types`). No `build` — web dev server was live.

---

## 2026-08-30 — Reconciliation: two independent sessions had duplicated the Week 2 review fix, plus a new cart bug fixed

**Branch:** `dev1`, fast-forwarded to `65e1359` then one new commit on top (this entry's own fix). Not yet pushed.

**What happened:** this session had independently done the exact same "Week 2 Days 3–9 review" work described in the `2026-08-29 — Week 2 Days 3–9 independent re-verification…` entry above — 7 parallel subagents, the same P0 (returns over-refunding on coupon-discounted orders) and the same three P1s, all uncommitted, sitting in this same iCloud-synced working tree. By the time this session went to commit, `origin/dev1` had already moved 8 commits ahead with an independent re-implementation of those exact fixes (`f9acd5a`), plus genuinely new work this session hadn't seen: admin nav restructuring, a storefront footer/newsletter section, and Week 2 Day 10 CI verification. Confirmed by diffing both sides file-by-file that the two implementations were functionally equivalent (same formulas, same migration shape) before doing anything — the pushed version was adopted as authoritative rather than attempting a manual merge of two independently-written but equivalent fixes, since it's additionally slightly more complete (an end-to-end integration test this session's own fix didn't have, and a `packages/types` `NOTIFICATION_STATUS` update this session's own fix had missed). This session's local uncommitted diffs for every overlapping file were discarded (backed up to `/tmp` first, compared, confirmed genuinely redundant, not needed) and `dev1` fast-forwarded cleanly to `origin/dev1` — zero conflicts, since this session had never committed anything locally to diverge with. `project_planning/week3.md` (a same-day, still-uncommitted planning doc from the other developer, present on disk via the same iCloud sync) was left completely untouched throughout.

**Bug fixed — cart "Cart item not found" on quantity +/-, reported live by the user.** `useCart.tsx`'s mount effect resolves the caller's cart (`getCart()` for a guest, `mergeCart()` when authenticated). `next.config.mjs` has `reactStrictMode: true`, which deliberately double-invokes every effect in dev mode (setup → cleanup → setup) — and the effect's `cancelled` flag only stopped the discarded invocation from calling `setCart()`, it never stopped the discarded invocation's **network request** from actually firing. For a guest with no `cart_id` cookie yet, there's nothing server-side to atomically dedupe two concurrent cart-resolving requests against (`GetOrCreateCartUseCase`'s guest branch: no matching cookie → `createCart({})` → a new cart + a fresh `Set-Cookie`) — two concurrent requests each create their own separate cart with their own separate cookie, and whichever `Set-Cookie` the browser applies last silently wins, regardless of which response ended up in React state. Reproduced directly, independent of the browser: two truly concurrent `GET /api/v1/cart` calls with no existing cookie produced two different `cartId`s and two different `Set-Cookie` headers.

**Fix:** share one in-flight promise across the StrictMode double-invoke (`useRef` holding the pending `getCart()`/`mergeCart()` promise; the second invocation reuses it instead of firing its own request) — there is only ever one real cart-resolving request per mount, not one per effect run. Ref clears itself once the request settles, so a genuine later re-run (real login/logout) still fires fresh.

**Verified, not just written:**
- Full reconciliation re-verified from scratch after the fast-forward: `pnpm run typecheck`/`lint` (9/9 clean, both), `boundaries:check` (433/1270, zero violations, unchanged), `pnpm --filter @woobe/api run test` (**370/370**, matching the other developer's own reported count exactly, both `woobe_dev`/`woobe_test` reset to the new consolidated migration first), `pnpm run build` (all three apps, zero live services running).
- Cart fix specifically: live browser reproduction, before and after — a fresh cookie-less context loading a product page now fires exactly **1** cart-resolving request on mount (was 2), then add-to-bag → cart page → increment → decrement all completed with real `200`s and zero "Cart item not found" errors.

**Related, not fixed this pass:** `useAuth.tsx`'s own silent-refresh-on-mount effect has the identical shape — and since the refresh-token flow has its own reuse-detection (a second concurrent call presenting the same now-rotated cookie is treated as token reuse and revokes every session for that user), the failure mode there could be worse than a 404. Flagged to the user directly; not fixed here since it wasn't the reported bug.

**Follow-ups:** `dev2`'s abandoned Day 4 worktree (flagged in the prior reconciliation-adjacent entry) is now doubly moot — both `dev2` and the local uncommitted duplicate of this session's own review work are superseded. Worth a cleanup pass (delete the worktree/branch) next time someone's in there, not urgent.

---
